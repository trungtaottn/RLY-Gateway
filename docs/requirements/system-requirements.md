# System Requirements Specification

## Purpose and boundary

This SRS defines externally verifiable system behavior for the local RLY Gateway product. Detailed business rationale belongs to the [BRD](./business-requirements.md); detailed flows belong to the [FRS](./functional-requirements.md); architecture decisions belong to accepted ADRs.

## Actors and external systems

- Owner/operator using CLI or local UI.
- Claude Code as the coding harness; Codex CLI only through `rly run codex`.
- Direct provider APIs and subscription-backed OAuth services.
- Optional attested local bridges.
- Existing Codex/Cline credential stores used only through explicit import/interoperability modes.
- macOS process, filesystem, loopback network, and optional Keychain facilities.

## Functional requirements

| ID | System requirement | Priority | Verification |
| --- | --- | --- | --- |
| SR-F-001 | Decode and encode supported Anthropic Messages semantics without silent loss. | Must | Protocol contracts and Claude E2E |
| SR-F-002 | Decode and encode supported OpenAI Responses semantics without reducing them to Anthropic ordering. | Must | Responses contracts and Codex E2E |
| SR-F-003 | Derive required capabilities before any upstream invocation and reject incompatible routes. | Must | Capability-negative tests |
| SR-F-004 | Manage provider definitions and evidence-dated model/capability records. | Must | Management/API validation |
| SR-F-005 | Manage account metadata independently from credential secrets. Account identity is unique per `(provider, pseudonym)`. | Must | Schema and DTO inspection |
| SR-F-006 | Explicitly import credentials without modifying the source store by default. | Must | Import immutability tests |
| SR-F-007 | Perform project-owned OAuth login, refresh, logout, and revoke through a central credential broker. | Must | OAuth lifecycle tests |
| SR-F-008 | Persist credential generations atomically and recover the last valid state after interruption, including reclaiming crashed credential locks without stealing live owners. | Must | Crash/corruption recovery tests |
| SR-F-009 | Manage profiles that bind harness, provider/pool, model roles, capability policy, and launch policy. The profile name is the canonical Claude Code launch alias (`rly <profile>`). | Must | Profile validation/E2E |
| SR-F-010 | Filter account eligibility before applying a pool selection strategy. | Must | Pool invariant tests |
| SR-F-011 | Support manual pin, round-robin, fill-first, bounded affinity, pause, and cooldown. | Must | Deterministic selector tests |
| SR-F-012 | Create one immutable request-scoped EffectiveRoute containing policy revision, capability snapshot, account pseudonym, and the credential generation frozen after refresh and before invoke. | Must | Route isolation tests |
| SR-F-013 | Permit bounded retry/account rotation only before the first response byte or tool event. | Must | Retry-boundary tests |
| SR-F-014 | Update health, quota class, cooldown, and audit outcome transactionally. Success restores healthy; quota exhaustion is ineligible only while cooling and becomes a recovery probe afterward. | Must | Outcome/crash tests |
| SR-F-015 | Launch Claude Code via `rly <profile>` or `rly run claude`, and Codex CLI via `rly run codex` only, with transient configuration that preserves global client configuration. | Must | Before/after hash E2E |
| SR-F-016 | Expose authenticated CLI management and a versioned local management API. | Must | Auth/mutation contract tests |
| SR-F-017 | Provide a local UI that consumes secret-free management DTOs only. | Must | Browser/network/privacy tests |
| SR-F-018 | Detect/reuse only attested project processes and fail closed on foreign listeners. | Must | Lifecycle/port tests |
| SR-F-019 | Record exact provenance and required notices for substantially reused source. | Must | Release provenance inspection |
| SR-F-020 | Produce a public release snapshot without connecting private `dev` history to public `main`. | Must | Git topology verification |
| SR-F-021 | Apply versioned retention and deletion policy to logs, audit, continuation state, backups, revoked credentials, and migration artifacts. | Must | Retention/deletion acceptance tests |
| SR-F-022 | Bootstrap and operate a per-user resident runtime service: idempotent `rly init`, macOS LaunchAgent / Linux `systemd --user` registration without root, a service-owned lease that prevents zero-lease idle shutdown while the service is intentional, independent launch/session leases, an identity/version handshake, crash/stale-record recovery, and bounded explicit shutdown that never signals an unknown port owner. | Must | Service-manager, lifecycle, and CLI tests |
| SR-F-023 | Operate the `rly config` user control plane: durable `~/.rly` configuration resolution (no CWD `gateway.config.toml` on the normal installed path), resident-runtime ensure/recover, secret-free status, loopback UI bootstrap, and provider/account/pool/profile operations through the same management API and policy revision as `rly admin`. | Must | Config CLI unit/lifecycle tests |
| SR-F-024 | Compose a durable RLY-owned Claude configuration overlay for RLY-launched Claude sessions: one private namespace under the durable RLY home (`<control-plane>/claude`); native user Claude config is read/compose-only input composed through a typed allowlist (settings merge, user agents/commands/skills, plugin enablement declaration); RLY-only gateway model state persists only inside the overlay so a later plain `claude` launch never inherits RLY gateway env, auth, or projection model ids; RLY session/history state survives RLY launches; composition is atomic, deterministic, and concurrent-RLY-launch safe; native Claude config is never rewritten and `~/.claude.json` and project-local `.claude` remain untouched. | Must | Overlay unit/lifecycle and before/after hash tests |
| SR-F-025 | Capture Claude Code agent attribution headers (`X-Claude-Code-Session-Id`, `X-Claude-Code-Agent-Id`, `X-Claude-Code-Parent-Agent-Id`) at the gateway ingress as typed runtime context (never from prompt content and never as authorization), maintain a session-scoped in-memory execution-context registry bound to the launch session/lease (access provider, frozen physical model, model family, effective tier, resolution revisions; removed on lease revocation or runtime restart; never credentials/account identity), and resolve subagent model/tier requests in the parent agent's execution context through the deterministic tier (#69) → capability (#68) → reasoning (#70) → account-pool stages while leaving the parent/main session model, profile mapping, and global Claude configuration unchanged; unknown tiers, missing evidence, unsatisfied capabilities, reasoning-with-tools gaps, or an undeterminable parent/session family fail closed with actionable typed errors and no silent substitution or global subagent override. | Must | Subagent lifecycle/unit/E2E and privacy tests |

| SR-F-025 | Expose the trusted RLY model universe to Claude Code through an authenticated `GET /v1/models` gateway discovery surface matching the supported Claude Code wire contract, projecting only trusted registry models from enabled configured provider→pool bindings (`VERIFIED` by default, `EXPERIMENTAL` only through an explicit config opt-in, `BROKEN`/unreviewed never), with stable Claude-compatible `claude-rly-*` projection ids and an explicit reverse mapping to one exact access-provider/model target + pinned pool; exact projected selections route through #68/#70 validation and the pinned pool's account selector, fail closed on unknown/removed/ineligible targets, are pinned per session (policy/registry revision) so policy changes never silently remap an issued id, and are enabled child-only via `CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY=1` without mutating the parent environment or native Claude configuration. | Must | Model-projection unit, gateway models-route lifecycle, child-launcher, overlay, and privacy tests |
| SR-F-026 | Operate a runtime compatibility evidence gate that (a) reports the exact installed Claude Code / Codex CLI version separately from binary `found` state (parsed from the client's own `--version` output; never inferred from timestamps or paths; `unknown` when unparseable), (b) pins the supported client baseline's wire contract with redacted fixtures (Claude Code session/agent/parent attribution headers, `GET /v1/models` request/auth/response selection incl. the `claude`/`anthropic` id-prefix rule and startup cache, `fable`/`haiku`/`sonnet`/`opus` alias semantics, subagent/session `effort` additive field, streaming framing, `--no-session-persistence`), (c) runs a deterministic fake gate matrix (text, streaming, cancellation, single/multi/parallel tool loops, reasoning, reasoning+tools, model discovery, session attribution, subagent routing, parallel subagents, effort signal, long-running session) per exact access path and classifies it `VERIFIED`/`EXPERIMENTAL`/`BROKEN`/`unknown` with typed reasons, (d) never treats a newly installed unknown client version or a single successful text request as supported (visible `unknown/not-tested` status), (e) emits secret-free machine-readable evidence artifacts consumable by #23/#67 review tooling without mutating trusted registry evidence, and (f) feeds canary-derived compatibility state into the #72 projection gate (VERIFIED default, EXPERIMENTAL opt-in, BROKEN/unreviewed never); live provider runs remain opt-in and skipped ≠ pass. | Must | Canary matrix/evidence unit, CLI canary, diagnostics, projection gate, and privacy tests |
| SR-F-027 | Operate a safe zero-downtime runtime update lifecycle that (a) separates candidate installation from activation through durable secret-free states, (b) reports the actual serving runtime version/schema/update state through the attested `/identity` handshake (never the package version alone), (c) keeps active launch sessions on the old process until a safe zero-session drain point (launch-session count, not TCP) unless an explicit destructive `--force` path is requested, (d) restarts only attested resident runtimes through the per-user service manager (#33/#34 `restart()`), (e) refuses new launch-session issuance once drain begins and applies the documented CLI/runtime compatibility launch policy while activation is pending (compatible pairs continue; incompatible pairs refuse only new launches with an actionable message), (f) accepts the candidate only after authenticated identity/readiness/state-open verification and rolls back once to the previous known-good version on failure (verified, reported, never looped), (g) blocks forward-only/unrollbackable migrations before destructive activation, (h) serializes concurrent updates with an ownership-aware lock and recovers crash/reboot states deterministically, (i) fails closed on foreign/unattested port owners without signaling them, (j) exposes allowlisted version/update metadata in `status`/`doctor`, (k) stores installed deployments immutably and content-addressed under `<control-plane>/runtime/versions/<artifactId>` (SHA-256 over the exact candidate bytes/build tree; semantic version is metadata only, never the storage key; byte-distinct candidates with the same semantic version never share a directory; reinstall of the identical artifact is an idempotent no-write; a completed deployment layout is validated before any reference exposes it), (l) maintains explicit `staged`/`active`/`previous` references under `runtime/refs/` where installation updates only `staged` and never changes the serving `active` reference (INSTALL != ACTIVATE; activation switches `active` and preserves the displaced deployment as `previous`), (m) replaces references atomically (temp-create + rename + parent fsync, never `rm + symlink`) so readers observe a valid old or new reference, and (n) migrates legacy `runtime/current`/`previous` + `versions/<semver>` layouts in place without deleting a serving known-good runtime before the new ref state is durable (bytes renamed, never removed; durable `migrating`/`committed` marker resumes a crashed migration idempotently; malformed legacy state fails closed with a doctor recovery path); update state, logs, diagnostics, deployment manifests, and refs never contain credentials, tokens, prompts, responses, or account identity. | Must | Update lifecycle, update state/policy/installer unit, CLI update, diagnostics, and privacy tests |
| SR-F-028 | Classify every incoming model selector into one typed model intent before routing, separating the client-native alias vocabulary (owned by Claude/Codex) from the RLY logical selector namespace: explicit `rly-tier:<tier>` namespaces RLY logical tiers (`haiku`/`sonnet`/`opus`/`fable`) so bare client-native alias strings are never RLY policy selectors by string equality (`fable != rly-tier:fable`); unknown `rly-tier:` values fail closed (`unknown-namespace`) and are never reinterpreted; the #69 provider/family tier resolver runs only for an explicitly typed tier intent (or a client-native alias mapped through the explicit traceable client-alias contract); exact projected selection (#72) stays exact and is dispatched before profile resolution; persisted exact model ids and `profile.modelRoles` tier keys keep their meaning (never reinterpreted); precedence among exact projection, explicit RLY tier, client alias, profile override, inherit, and default is deterministic; the typed failure taxonomy (unknown namespace, unsupported client alias, unknown exact model, invalid projection, tier unavailable, conflicting selector sources) maps onto the existing profile error contract; and route diagnostics expose selector kind/source/resolved target only — never prompts, credentials, account identity, or settings contents. | Must | Model-intent unit, selector-routing lifecycle, tier/profile regression, and privacy tests |
| SR-F-029 | Preserve wire-significant native protocol state through a versioned fidelity envelope without letting the semantic core interpret or drop provider-owned opaque continuation artifacts: the native protocol rail (encoder/decoder wire shapes) remains the source of wire truth for same-protocol traffic; `CanonicalRequest`/`CanonicalEvent` stay the semantic routing projection; a versioned fidelity envelope (`src/core/fidelity.ts`) carries typed opaque artifacts (Anthropic thinking `signature`/`signature_delta`; OpenAI Responses reasoning item identity + `encrypted_content`) with stable item/block associations plus translation provenance (`preserved-native`/`translated`/`ignored`/`unsupported`); Anthropic thinking signatures survive decode → canonical stream (`signature-delta` ordered relative to thinking/content events) → aggregate → same-protocol encode; OpenAI Responses reasoning identity + encrypted content survive decode → continuation storage → subsequent `previous_response_id` request construction with the exact artifact-to-item association retained and never reconstructed from summary text; unknown additive native fields are recorded as intentionally ignored rather than silently discarded; opaque artifact kinds remain a typed extension point (Gemini/OpenRouter/DeepSeek) without redesigning canonical routing; a compatibility claim requiring an artifact fails closed (`unsupported-fidelity`) before any upstream call when the selected translation path cannot preserve it (e.g. Chat Completions transport), with no fabrication or reconstruction; and opaque artifact values never appear in route traces, normal diagnostics, logs, or privacy snapshots — only provenance metadata via `describeFidelity()`. | Must | Protocol fidelity contract, continuation, cross-protocol fail-closed, and privacy tests |
| SR-F-030 | Compose profile-scoped Claude views with typed environment/settings ownership for RLY-launched Claude sessions: a deterministic per-profile/per-policy view identity (`<control-plane>/claude/views/<view-id>`, reserved `default` for profile-less launches) replaces the single shared overlay so RLY-only model/default/cache/history state never silently bleeds between RLY profiles while plain non-RLY Claude reads native config only; relevant Claude/gateway/model settings carry explicit ownership categories (RLY-owned, conflicting, safe pass-through, unsupported, user override) with deterministic precedence (child-only RLY gateway contract > RLY-owned persisted projection > explicit RLY/profile settings > native input > client persistence > defaults); an ownership manifest (metadata and hashes only, never credentials or content) distinguishes native-imported / RLY-generated / durable view-owned files/state; deletion/rename reconciliation removes owned imports whose native source disappeared and reclassifies divergent view copies view-owned (RLY never deletes a file it does not own as an import, never additive-only ghost files); composition is concurrency-safe (atomic temp+rename writes, bounded per-view reconcile lock; native config is never rewritten and never restored on child exit); native allowlisted freshness is deterministic with ownership winning over naive mtime; the legacy shared overlay migrates into the `default` view without touching native `~/.claude`, and ambiguous shared persisted state is surfaced rather than silently assigned; diagnostics expose view id/path, composition version, ownership/reconciliation status, and conflicting key categories only — never settings content, prompts, transcripts, skills/agents text, credentials, auth tokens, or raw account identity. | Must | Overlay lifecycle/unit, profile-isolation, black-box sentinel, CLI diagnostics, and privacy tests |
| SR-F-031 | Run update activation as a durable, lock-correct, drain-fenced, migration-safe transaction on top of the #73/#92 lifecycle (SR-F-027): (a) a secret-free transaction journal on the update-state record moves through phases STAGED → DRAINING → SWITCHING → PROBATION → COMMITTING → COMMITTED (or ROLLING_BACK → COMMITTED | RECOVERY_REQUIRED), each phase durably written before the action it fences and carrying the candidate/previous immutable deployment identities plus a bounded rollback-attempt counter, (b) the update lock records the REAL OS process-start identity from the process-attestation subsystem (never acquisition wall-clock time), treats unverifiable owners conservatively as held, and reclaims only locks whose owner identity is proven stale/dead, (c) the new-launch fence (refusal of new launch-session issuance on the old generation) is established BEFORE the serving ref switch, letting existing sessions complete naturally and never replaying or moving in-flight streams/tool loops, (d) the candidate is accepted during PROBATION only after exact runtime identity (serving version equals the staged candidate), management/data protocol compatibility, authenticated readiness, and durable state/schema compatibility are verified, and only then does the transaction commit — a crash before COMMITTED is durable never silently commits the candidate, (e) migration rollback safety uses compatibility classes (`none`/`backward-compatible-expand`/`transactional-replace`/`forward-only`) replacing the binary `migrationForwardOnly` (legacy manifests map true ⇒ forward-only, false ⇒ backward-compatible-expand) and `forward-only` blocks activation before any destructive state change, (f) rollback is bounded to exactly one attempt (rollbackAttempts ≤ 1) that re-establishes `active`/`previous` from durable journal evidence (previous ref written before the active switch so the known-good reference is never lost), restarts at most once, verifies the restored runtime, and terminates in the explicit RECOVERY_REQUIRED state with an actionable `rly doctor` path when it fails — never an infinite restart loop, and (g) crash/reboot recovery makes one deterministic choice from the journal phase: interrupted install → failed with retry guidance, STAGED/DRAINING → resume activation, SWITCHING/PROBATION/COMMITTING → roll back to the durable previous known-good reference, COMMITTED → promote to active, interrupted or duplicate rollback → recovery-required; the `/identity` handshake reports the transaction phase, and `rly status`/`rly doctor` expose allowlisted phase/lock-owner/drain/outcome metadata; the transaction journal never contains credentials, tokens, prompts, responses, or account identity. | Must | Update lifecycle, update state/policy/installer unit, CLI update, diagnostics, crash/recovery transaction, and privacy tests |
| SR-F-032 | Operate a versioned, feature-scoped Compatibility Claim and Evidence model (v2) keyed to the exact execution path: a stable claim key includes client kind, exact client version/baseline, source protocol/revision (#119 vocabulary), adapter/integration surface, access provider, auth mode, endpoint contract, exact physical model, and feature/capability claim (model family is metadata only, never part of the key); features (text, streaming, cancellation, tools incl. parallel/multi-tool continuation, reasoning, reasoning+tools, discovery, session/subagent attribution, config-overlay, and other relied-on features) are claimed independently; Evidence Artifact v2 records carry claim key, layer/kind, fixture/corpus revision, runner version, timestamp, result (`passed`/`failed`/`not-run`; `missing` = record absence), typed failure reason, environment metadata, and safe refs to raw results — never credentials, auth headers, account identity, prompts, real responses, or reasoning text; evidence layers are explicit (A = deterministic fake-matrix conformance, B = installed-client black-box, C = live access-path; required layers declared per adapter with unknown adapters failing closed); `livePassed`/`liveEvidence` booleans are removed from the authoritative classification path so no boolean can grant production trust and a deterministic Layer A pass never implies live/production trust (observation classification can never emit `VERIFIED`; `RLY_LIVE_CANARY`/runner switches only enable execution, never evidence); the current canary matrix is reclassified as Layer A evidence v2; claim/evidence documents are versioned (claim `schemaVersion` 1, evidence `evidenceSchemaVersion` 2) with legacy v1 canary outputs flagged legacy/untrusted for v2 authority decisions (never silently satisfying a stronger v2 claim); claim evidence persists append/audit-friendly (atomic writes, appended records, deduped identical observations, fail-closed malformed reads) with deterministic lookup by exact claim identity + feature; registry `CompatibilityEvidence` may carry a `claimRef` (revision 5) linking reviewed state to its exact claim. | Must | Canary claim/evidence unit, CLI canary, canary privacy, registry, and diagnostics tests |
| SR-F-033 | Run installed-client (Layer B) and live access-path (Layer C) compatibility runners that produce feature-scoped Evidence v2 for the exact execution path: (a) Layer B detects and records the EXACT installed Claude Code / Codex CLI executable path and version (probe output, never inferred; observed ≠ reviewed supported baseline, recorded separately and never auto-promoted), launches the ACTUAL installed binary against a controlled local fixture server serving the client-facing wire surface RLY relies on (Anthropic Messages + `/v1/models` for Claude Code; OpenAI Responses + `/v1/models` for Codex) with child-only environment isolation (never real client config, never real credentials), and exercises the black-box matrix RLY relies on — request/stream framing, cancellation, tool round-trip, multi-tool continuation, parallel tools, reasoning/effort, reasoning+tools, discovery/selection, session/agent/parent attribution, tier alias behavior, subagent concurrency (distinct concurrent attribution contexts), config-overlay behavior, and long sessions — emitting one Evidence Artifact v2 per feature (layer B, kind `installed-client`, runner version, fixture revision, timing, typed/redacted failure reason) keyed to the exact observed client version; a changed client behavior yields a typed exact gate failure keyed to that client version and newly observed versions are tested without becoming supported baselines (version drift surveillance); (b) Layer C executes one selected exact claim path — client + client version + source protocol/revision + adapter/integration + access provider + auth mode + endpoint contract + physical model + feature gate — through the real RLY gateway translation stack to the configured provider endpoint ONLY with explicit opt-in (`RLY_LIVE_CANARY=1`) and an available environment credential, emitting feature-scoped Evidence Artifact v2 (layer C, kind `live-access-path`) where text success never promotes tools/reasoning/discovery claims, the same upstream model through two access providers yields distinct claim keys/evidence (no cross-provider sharing), tool continuation is proven only when the provider returns tool calls (else `not-run`/`provider-did-not-call-tool`), and policy-driven `GET /v1/models` discovery is exercised on the exact-path gateway; (c) missing credentials, skipped runs, environment inability, and unexecuted gates are `not-run` (or absent) — never `passed` and never `VERIFIED`; live runs never silently spend quota; raw runner results persist secret-free under `<control-plane>/canary-runners/` (metadata only) with Evidence v2 `ref`s; and runners are observation-only — they never mutate trusted registry, tier mappings, projection, or effective compatibility state (promotion is #124). | Must | Canary runner unit/E2E (fake + real-binary), CLI canary run-b/run-c, canary privacy, and diagnostics tests |
| SR-F-034 | Operate the Effective Compatibility Registry as the SOLE runtime compatibility authority (#124): (a) explicit Review Decisions tied to exact #122 claim identities AND evidence revisions promote or reject an exact claim/feature — positive trust requires an explicit reviewed decision, a new PASS observation never auto-promotes, and a decision no longer covering the current evidence revision (evidence updated after review) makes the claim untrusted/review-stale (re-review required); (b) a Negative Quarantine Store lets a strong reproducible failure quarantine an EXACT claim/path/feature promptly with narrow scope (never poisoning unrelated providers/features) and without deleting historical evidence, plus explicit audit-friendly lift; (c) a freshness/staleness engine marks evidence stale on configured identity/revision dependency changes — client version/baseline, protocol/adapter revision, provider endpoint/auth mode, physical model fingerprint, fixture/corpus revision, material RLY build change, and evidence age — so a stale positive never remains silently VERIFIED; (d) effective resolution combines evidence + review + quarantine + freshness + policy into ONE answer per exact claim/feature (`trusted`/`stale`/`experimental`/`untrusted`/`quarantined`/`missing`) keeping trust, observed health, freshness, quarantine, and enforcement reason as DISTINCT diagnosable fields — never a single persisted boolean; (e) default enforcement requires effective trusted compatibility for required features — quarantined/known-broken required features FAIL CLOSED with no silent fallback across provider/family/model unless an owning policy explicitly permits it, and the explicit experimental override (exact-pin or `allowExperimental`) is traceable/visible in traces and doctor and can never bypass a hard quarantine except through the separately documented administrative `allowQuarantineBypass` policy; (f) runtime consumers (model selection #68, logical tiers #69, model projection/discovery #72, reasoning/tool eligibility, profile route resolution) consume ECR results — static `model.compatibility.state` is no longer the final production trust authority; (g) legacy static registry compatibility states migrate under an explicit seed/reference policy (`seed-reference-only`) and can never silently satisfy v2 reviewed trust; (h) diagnostics/doctor explain effective results (claim identity, trust decision, evidence layers, health, freshness, quarantine reason, enforcement reason) without secrets or user content; and (i) decision/quarantine records persist secret-free (reviewer/source/reason/timestamp/revision only) under `<control-plane>/compat/` with append/audit-friendly monotonic revisions and fail-closed malformed reads. | Must | ECR unit/freshness/stores/registry/privacy suites, routing-consumer integration, gateway lifecycle, CLI compat, and diagnostics tests |
| SR-F-035 | Produce ONE typed EffectiveModelDecision before account selection for every supported RLY model-routing request (#127): the decision assembles the #125 typed selector intent/provenance, the #68 frozen physical model target, the #69 tier / #72 projection / #71 parent-context provenance, the #70 reasoning translation, the #124 ECR compatibility reference (authority, effective label, enforcement reason, seed state), the #72 provider→pool binding, policy/profile/session/registry/mapping revisions, and #126 profile-view/env/settings ownership state; deterministic precedence (explicit exact selection → RLY logical tier → client-native alias/model → subagent inherit/override → profile/session policy → env/settings ownership → persisted RLY-view state → defaults) with visible, deterministic conflicts (no hidden string/env override silently wins); the physical provider/model/reasoning target is FROZEN before the existing pool/account RouteSelector so account retry/failover can never change it; subagent decisions stay isolated and never mutate the parent model; persisted/resumed client state is validated against the owning profile view, current policy/registry revision, ECR state, and the session-pinned selector namespace — a stale/foreign RLY projection or another profile's persisted model never silently becomes the effective target (fail closed with an actionable reason); Codex model/provider control uses the same decision vocabulary without forcing Claude-specific aliases onto Codex; diagnostics (`rly route-trace`/`rly doctor`) explain the selected model and blocked alternatives/conflicts (conflicting env/settings, stale projection, unsupported alias, unavailable tier, incompatible/expired evidence, profile mismatch) with allowlisted metadata only — never prompts, responses, reasoning text, credentials, auth headers, raw account identity, or full user settings content. | Must | Model-decision unit/lifecycle/privacy suites, routing integration, diagnostics, and regression suites |
| SR-F-036 | Operate a stable RLY-owned runtime bootstrap with exact build attestation and idempotent service-definition reconciliation (#94): (a) install ONE RLY-owned launcher at `<control-plane>/bootstrap/rly-gateway` that is the ONLY execution identity per-user service managers reference — the service definition never points at `dist/cli/init.js`, a direct `runtime/refs/...` deployment path, or the Node installation that invoked `rly init`; (b) the bootstrap resolves ONLY the committed #92 `active` deployment (refusing staged/uncommitted/missing candidates) and executes the real `dist/cli/main.js` dispatcher, exporting `RLY_SERVING_ARTIFACT`; (c) `rly init` establishes the initial committed active deployment from the installed runtime tree (idempotent; a valid committed deployment is never rewritten) and `rly update` keeps the definition on the bootstrap contract; (d) a versioned exact build identity (semantic version, commit revision, build ID, release channel, control/data protocol versions, state schema version, serving artifact digest) is reported identically by `/identity`, `rly --version`/diagnostics, the release-candidate manifest, deployment metadata, and update probation — two artifacts sharing a semantic version are distinguishable by artifact digest and an exact match requires all fields equal; (e) the existing attestation is extended (no parallel identity service): reuse requires the serving build identity digest to match the ownership record (same-semantic-version-different-artifact fails closed as attested-incompatible) and update probation requires the serving artifact digest to equal the candidate's immutable deployment identity whenever the serving runtime identifies its artifact, rolling back otherwise — foreign/unattested listeners continue to fail closed and are never signaled; (f) `rly init` and `rly doctor` detect missing/stale/path-drifted launchd/systemd definitions and repair them idempotently, migrating legacy definitions (dist/cli/init.js, Node binary, direct runtime/refs/... path) to the bootstrap contract without provider/account reconfiguration, never intentionally targeting a staged or deleted deployment, preserving no-root per-user semantics, and keeping unsupported-platform behavior explicit; (g) diagnostics may expose bootstrap path, service-definition revision + reconciliation state, and expected/serving build identity — with no credential, auth, session, or user-content data in service definitions, the bootstrap script, deployment metadata, or build identity. | Must | Bootstrap unit, build-identity unit, reconcile unit, lifecycle attestation, update lifecycle, CLI diagnostics, privacy, and service-manager suites |
| SR-F-037 | Execute streaming transport incrementally with a stream-safe lifecycle for Anthropic Messages and OpenAI Responses (#120): (a) per-stream incremental encoders consume one canonical event at a time and emit only the new wire frames (byte-identical to the batch re-encode-then-slice output on valid text/reasoning/tool fixtures including #119 fidelity events) without recomputing prior output; (b) the routes never retain a full `seen` event history to re-encode it — retained protocol state is bounded by open blocks / the aggregate output projection required for the terminal `response.completed` and continuation persistence, so encoding work is linear in event count with bounded state; (c) downstream writable backpressure pauses upstream iteration (no frame loss/duplication/reordering/unbounded buffering) and the idle clock is disarmed while suspended on backpressure; (d) client abort/disconnect promptly cancels upstream work through the merged AbortSignal with no frames after close, even for upstreams that ignore the signal (stopped at the next terminal check); (e) a connection/setup timeout (first frame) is separate from the healthy stream lifetime, with an explicit idle/progress timeout between upstream events — no generic whole-request timer, so healthy long-lived agent streams with ongoing progress are never killed; (f) iterator, encoder, timers, abort listeners, and the response are cleaned exactly once on completion, error, timeout, disconnect, and teardown; (g) stream diagnostics are metadata only (event/frame counts, backpressure count, duration, terminal kind incl. setup/idle timeout category) and never contain prompts, responses, reasoning text, tool payloads, or credentials. | Must | Incremental encoder contract, stream lifecycle/pump unit, fake-upstream integration, soak, and privacy suites |
| SR-F-038 | Operate a true native OpenAI Responses upstream rail with provider error fidelity and commitment-state retry (#121): (a) at least one supported provider/access path (OpenRouter direct, exact endpoint contract `POST https://openrouter.ai/api/v1/responses`) exercises a first-class Responses request/stream path preserving Responses item identity (`item.id`), continuation fields (`previous_response_id`, response id), tool/reasoning item ordering, and #119 opaque artifacts (reasoning `encrypted_content` via `include: ["reasoning.encrypted_content"]`) through provider invocation — same-protocol Responses requests route to the native rail whenever the adapter declares the exact Responses endpoint contract, and `OpenAI-compatible` alone is never a sufficient compatibility class (every claimed Responses path identifies the exact endpoint/adapter contract); (b) safe structured provider error metadata (status code, code, type, allowlisted bounded message, param, retry-after, rate-limit fields) survives the adapter boundary through `ProviderAdapterError.info` and maps onto protocol-correct client errors on both routes (Responses `{type, code, message, param}`; Anthropic via deterministic `translateProviderError` cross-protocol vocabulary) with `retry-after` surfaced — generic normalization is the fallback, never the only path — and error redaction deliberately excludes credentials, auth headers, secret-bearing bodies, prompts, responses, and reasoning text; (c) execution commitment is tracked separately from client-visible output (`not-sent`/`sent-unacknowledged`/`provider-accepted`/`client-output-started`/`tool-boundary`/`unknown`); adapter/provider rails own the evidence of provider commitment and routing/retry consumes it; (d) rotation/retry happens ONLY when the policy can prove the previous attempt never crossed a provider/client/tool commitment boundary — `canRotate` requires `commitment === "not-sent"`; a deterministic 4xx rejection is rotation-safe, a 5xx or post-send network failure is `unknown` (conservative no-replay), and failure after provider acceptance, during streaming, or at a tool boundary never rotates/replays; account failover never changes the frozen physical model (#127); (e) error/retry observability traces ONLY allowlisted categories (provider/access path, adapter id, model id, attempt number, status class, retryability reason, commitment state, terminal reason, outcome) with a fixed key set — never credentials, prompt/response/reasoning/tool payload content, raw account identity, or unredacted secret-bearing error bodies; (f) deterministic conformance fixtures cover Anthropic and Responses native/supported translation paths (text, streaming, cancellation, tools, parallel/multi-tool continuation, reasoning, opaque continuation artifacts, stop reasons, errors, retry metadata, disconnect ambiguity) recording expected WIRE semantics; (g) chaos tests inject failures at every commitment stage and prove NO duplicate tool execution or unsafe replay, and long-session soak combines #120 incremental transport with retry/error handling without state leakage or unbounded resource growth. | Must | Native-rail adapter contract, conformance corpus, chaos/commitment, soak, route error-fidelity, and privacy suites |
| SR-F-039 | Produce RLY-owned standalone runtime artifacts as the PRIMARY production distribution and verify them before release (#35): (a) self-contained packages per supported target bundle the compiled runtime (`dist`), bundled runtime dependencies (prod `node_modules` with the pnpm virtual-store layout preserved so transitive resolution works, metadata/.bin/test artifacts pruned), the exact pinned Node runtime (`bin/node`; version recorded in build metadata/SBOM inputs), licenses/notices, the release-candidate manifest (`rly.json`), the exact #94 build identity (`rly-build.json`), and artifact metadata (`rly-artifact.json` — target platform, bundled Node version, content-addressed artifact digest, file list); `rly` stays the stable user-facing command and the internal layout is deterministic (`rly`, `bin/node`, `dist`, `node_modules`, `package.json`, `LICENSE`, `docs/third-party-notices.md`, `rly.json`, `rly-build.json`, `rly-artifact.json`); normal execution never depends on the source repo, pnpm workspace, user Node/npm/pnpm, or the invoking CWD (self-locating launcher + bundled Node); (b) the platform matrix explicitly covers or excludes `darwin-arm64`, `darwin-x64`, `linux-x64`, `linux-arm64` — every target is declared `supported` (built AND smoke-tested on a qualified runner) or `experimental` (built deterministically, smoke-testing requires an unprovisioned runner) with a reason, and no target silently reuses another target's artifact or Node bytes; (c) the positive package allowlist ships ONLY compiled runtime code, bundled runtime deps, required assets/templates, licenses/notices, and explicit bootstrap resources — `.git`, dev config, `.env`, local `~/.rly` state, dependency test artifacts, `.bin` shims, pnpm metadata, credentials, reports, private snapshots, and unrelated workspace files are excluded and packaging FAILS on unexpected files, forbidden markers, escaping/absolute symlinks, or secret content in RLY-owned files; (d) artifact production consumes the #94 exact build identity and emits semantic version, commit/source revision, build ID, release channel, target platform, bundled Node version, and artifact digest CONSISTENTLY across `rly-build.json`/`rly.json`/`rly-artifact.json`; the canonical version authority is `RLY_RELEASE_VERSION` → exact git tag → `package.json` (resolving the `0.1.0` vs release-tag ambiguity), a split identity fails the build, and two byte-distinct artifacts cannot share an artifact digest (content-addressed over the exact tree like #92); (e) builds are reproducible from a clean compiled tree with the frozen lockfile and pinned Node version — deterministic sorted file lists, fixed source-date-epoch mtimes, uid/gid 0, no owner names, no absolute developer paths/tokens/home dirs or pnpm store paths embedded, identical inputs => byte-identical tarballs and digests; (f) `.github/workflows/standalone-artifacts.yml` builds and verifies the matrix on release publication or manually, smoke-tests every SUPPORTED CI target (`rly --version` from the unpacked artifact), QUALIFIES the exact artifact bytes and publishes the signed release supply chain (SR-F-040/SR-F-041/SR-F-042), and uploads tarballs + sha256 + manifest + signing/SBOM/provenance/channel assets to the GitHub Release; (g) npm/Homebrew secondary channels, if introduced, must consume the SAME canonical artifact lineage and never build semantically different runtime bytes under the same RLY build identity; (h) installer/updater UX (#129) remains out of scope. | Must | Standalone packaging/verify scripts, hermetic unit suite (allowlist/absence/digest/determinism/matrix/identity/smoke), and CI matrix job |
| SR-F-040 | Publish a canonical release manifest and signed channel metadata as the release supply-chain authority (#128): (a) a canonical release manifest (`rly-release.json`) binds product version, release channel (beta/stable), source commit, build ID, every supported target, artifact filename/size/sha256/content-addressed digest, bundled runtime version, state/protocol compatibility, required signatures/attestations, and workflow/toolchain inputs — consistent with the #94 exact build identity (a manifest whose identity diverges from the packaged `rly-build.json` fails publish and verification); (b) a small signed channel metadata layer (`rly-channel-<channel>.json` + `.sig`, Ed25519; private key ONLY the repository secret `RLY_RELEASE_SIGNING_KEY`, public key committed) maps beta/stable to exact release/build/artifact digests with explicit rollback (monotonic per-channel version counter — lower-than-observed is refused), staleness (`updatedAt` + `staleness.maxAgeDays` — stale is refused), and freeze (explicit marker blocks activation beyond the frozen snapshot) semantics — the updater verifies the signature and never trusts a mutable GitHub `latest` target alone (TUF-style separation of metadata from artifacts); (c) release immutability: published bytes/digests are never silently replaced under the same release identity — republishing the same version with different digests fails and byte replacement of published assets is detected against the signed metadata; (d) beta vs stable gates are machine-readable — beta may publish with documented experimental qualification gaps (`experimental-gaps`), stable requires `qualified` status for every advertised target plus all authenticity/compatibility/privacy/Wave-4-integration gates, and beta evidence never masquerades as stable qualification. | Must | Release manifest/channel/signing/immutability unit suite, publish + verify pipeline integration, release supply-chain gate, RTM/AT rows |
| SR-F-041 | Generate SBOM and provenance from the exact artifact lineage and enforce platform authenticity (#128): (a) an SBOM per production artifact is generated from the ACTUAL packaged bytes (RLY components, bundled pinned Node, third-party `node_modules` packages; deterministic, SPDX-2.3-style) and references the EXACT artifact digest (`artifactRef`) — evidence is a SIBLING release asset (never embedded in the artifact, preserving TUF-style metadata/artifact separation); (b) build provenance/attestation ties every artifact digest (tarball sha256 + content-addressed tree digest) to the exact source revision and the release workflow/toolchain inputs (SLSA-flavored, secret-free); (c) platform authenticity: macOS production artifacts must pass the documented code-signing/notarization/stapling verification gate (`codesign --verify --deep --strict` + `stapler validate` on a provisioned macOS host with the Apple certificate secrets) before stable promotion; Linux authenticity uses the per-artifact Ed25519 signature (`<tarball>.sig` over `sha256:<hex>`) + the release manifest trust chain; a missing required platform signature BLOCKS promotion for that target (machine-readable); (d) privacy/public boundary: manifests, SBOMs, provenance, channel metadata, signatures, and qualification records contain public build metadata only — never credentials, tokens, local state, private source-history content, prompts, responses, or account identity. | Must | SBOM/provenance unit suite, signing key tests, publish + verify pipeline integration, privacy scan, RTM/AT rows |
| SR-F-042 | Qualify the exact artifact bytes as the publication authority (#128): (a) qualification installs and exercises the SAME artifact digest later published — no rebuild between qualification and publication; the matrix covers clean install, `rly --version`/build identity, permissions, platform signing, runtime readiness (doctor ok), update handoff contract (the #73/#92/#94 candidate contract), `rly init`/service registration, and uninstall (self-containment); (b) a gate is `passed` only when it executed against the exact bytes — `skipped`/`not-run` is never passing evidence, gates are host-aware (a target the runner cannot execute is recorded `skipped` with a reason), a target without qualification evidence is NOT stable-qualified, and a missing/failed required signing, SBOM/provenance, compatibility, or platform qualification gate prevents stable promotion (`qualificationBlocksStable`); (c) workflow hardening: release-critical third-party GitHub Actions are pinned to reviewed immutable commit SHAs, release workflows use least-required `GITHUB_TOKEN`/release permissions, and no npm credentials are configured (npm is not the primary channel) — enforced statically by `scripts/check-release-supply-chain.mjs` as part of the release gate; (d) release/supply-chain/installation docs and RTM/AT identify exact-byte qualification as the publication authority. | Must | Qualification matrix unit suite, qualification CLI + stable-block gate, publish + verify pipeline integration, release supply-chain gate, RTM/AT rows |
| SR-F-043 | Operate a verified installer, updater, and uninstaller acquisition lifecycle (#129): (a) verified remote acquisition resolves the selected channel/platform/arch through the #128 signed channel metadata + signed release manifest (Ed25519 verified against the committed release public key), downloads the EXACT immutable artifact from the approved origin, and verifies BEFORE any install/staging mutation — channel rollback (monotonic version vs the durable highest observed), staleness, freeze, exact platform target + channel qualification gate (stable requires `supported` + `qualified`; beta permits documented `experimental-gaps` but never `not-qualified`), artifact sha256/size, the Ed25519 digest statement (`<tarball>.sig` over `sha256:<hex>`), and the unpacked-tree content-addressed digest + identity consistency — rejecting wrong platform, stale/invalid metadata, digest mismatch, missing/invalid signature, or unknown release identity with actionable secret-free errors; (b) a documented minimal fail-closed bootstrap installer (`scripts/install.sh`) performs a clean per-user macOS/Linux install without source checkout/npm/pnpm/user Node or sudo, verifying the artifact digest chain in-shell and the FULL canonical-JSON signature chain in the verified binary before any install mutation, never executing/installing unverified content; (c) the install registers the #94 RLY-owned stable bootstrap as the ONLY service execution identity (never `dist/cli/init.js`), materializes the initial committed `active` deployment from the #35 canonical artifact tree, and uses deterministic per-platform private/user-safe install locations (`~/.rly` control plane, `~/.rly/bootstrap/rly-gateway`, `~/.local/bin/rly` launcher symlink → bootstrap), refusing to overwrite foreign/unowned paths; (d) the first-install vs update boundary is explicit: first install runs/guides `rly init` + `rly config`; update acquisition installs/verifies a candidate and returns a `VerifiedCandidate` to Wave 4 WITHOUT changing the #92 `active` reference or restarting the resident service (INSTALL != ACTIVATE; `--install-only` stops after staging); (e) `rly update` selects `beta`/`stable`/`current` channel policy with explicit auditable channel switches, inspects the current exact build identity, resolves newer qualified artifacts, and reports current/candidate exact build + channel + verification + pending/activation status; (f) `rly uninstall` stops/disables/removes ONLY RLY-owned launchd/systemd definitions and product artifacts while preserving `~/.rly` durable user data by default, and an explicit `--purge --yes` destroys the RLY control plane with unambiguous intent while never touching native Claude/Codex configuration; (g) repair/reinstall verifies ownership/build identity and repairs missing/stale bootstrap + service definitions idempotently without overwriting foreign/unowned paths or killing unknown port owners; (h) the platform matrix matches #35/#128 and unsupported environments fail explicitly; (i) installer/update logs and durable records carry channel/version/build/digest/platform/path/status metadata only — never credentials, tokens, auth headers, email/raw account identity, prompts, responses, or reasoning text; (j) the #128 exact-byte qualification exercises the installer/updater verification on the exact artifact lineage (`verified-install` gate) and the real release signatures are verified post-publish. | Must | Installer acquisition unit/integration/privacy suites, CLI install/uninstall/update tests, bootstrap-installer shell E2E, release supply-chain gate, RTM/AT rows |

## Non-functional requirements

| ID | Requirement | Target or invariant | Verification |
| --- | --- | --- | --- |
| SR-NF-001 | Local network isolation | Data `127.0.0.1:17871`; management `127.0.0.1:17872`; no port auto-increment | Listener/collision tests |
| SR-NF-002 | Secret confidentiality | No raw secret in Git, SQLite, UI/DTO, log, audit, error, fixture, or package | Privacy/secret scans |
| SR-NF-003 | Credential file protection | Store `0700`; files `0600`; correct owner; atomic replace | Mode/owner/crash tests |
| SR-NF-004 | Management security | Separate per-instance secret; exact Origin; CSRF; short sessions; logout/shutdown invalidation | Negative security tests |
| SR-NF-005 | OAuth security | PKCE, exact redirect, single-use expiring state, replay rejection, bounded errors | OAuth negative tests |
| SR-NF-006 | Concurrency correctness | Single-flight refresh; generation CAS; request/pool isolation | Race tests |
| SR-NF-007 | Stream safety | No retry/rotation after externally visible output/tool event | Adversarial stream tests |
| SR-NF-008 | Recoverability | Verified backups/migrations; last valid credentials and metadata recoverable | Crash/restart tests |
| SR-NF-009 | Privacy by default | Prompt, response, tool argument, email, and raw identity excluded from default observability | Adversarial redaction tests |
| SR-NF-010 | Traceability | Every Must BR maps to system, functional, and acceptance IDs | RTM audit |
| SR-NF-011 | Maintainability | Provider behavior isolated behind explicit adapter/credential/protocol contracts | Architecture review |
| SR-NF-012 | Compatibility | Unknown required protocol/provider behavior fails closed and downgrades readiness | Drift fixtures |
| SR-NF-013 | Accessibility | Local UI supports keyboard navigation, visible focus, labels, status text, and responsive layouts | Browser accessibility checks |
| SR-NF-014 | Release integrity | Full verify, privacy, license/provenance, migration/recovery, package and clean-install gates pass; standalone artifact matrix builds from a clean tree with the pinned Node version, passes allowlist/absence/identity/digest verification and the supported-target smoke, and is reproducible (byte-identical tarballs for identical inputs) | Release gate + standalone artifact CI job |

## Data requirements

- SQLite stores non-secret provider, account metadata, profile, pool, membership, health, policy revision, terms revision/acceptance, and metadata-only audit records.
- Credential store contains versioned project-owned secret records and temporary atomic-write artifacts; it is outside Git and SQLite.
- Runtime ownership/lease/session state is separate from durable control-plane metadata.
- Default logs and audit do not contain request bodies or account identity.
- SR-F-021 owns retention/deletion policy. Exact durations and budgets are versioned policy values approved before release.

## External interface requirements

- Data API: protocol-native endpoints authenticated with the transient harness token.
- Management CLI: per-instance bearer read from restrictive runtime state.
- Management browser: launcher fragment bootstrap exchanged for an `HttpOnly`, `SameSite=Strict` cookie; exact Origin and CSRF required.
- Provider interfaces: documented direct API, project-owned OAuth, explicit interoperability, or attested bridge, selected per provider.
- Configuration import/export: metadata and handles only; never raw credentials.
- Per-user service managers: macOS launchd (user LaunchAgent) and Linux `systemd --user` with injectable command runners; definitions contain absolute executable/entrypoint/config paths only and never credentials or account identity.

## Assumptions

- One local owner in V1; authorization roles beyond owner/admin are future scope.
- Loopback is necessary but insufficient for management trust.
- Provider quota quality varies; unknown quota never fabricates availability.
- Filesystem deletion does not guarantee forensic erasure.

## Unresolved questions

- Quantitative performance targets after the first control-plane benchmark.
- Final retention durations and maximum local storage budgets.
- Exact supported OS matrix after the macOS private release.
