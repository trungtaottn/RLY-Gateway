# Requirements Traceability Matrix

## Business-to-acceptance trace

| Business requirement | System requirements | Functional requirements | Use cases / stories | Acceptance |
| --- | --- | --- | --- | --- |
| BR-001 Multiple providers through Claude | SR-F-001, SR-F-003, SR-F-009–012, SR-F-025, SR-F-026, SR-F-033 | FR-008, FR-009, FR-011, FR-021, FR-022, FR-029 | UC-001, UC-005; US-001, US-007 | AT-015–AT-022, AT-033, AT-073–AT-079, AT-091–AT-102, AT-180–AT-190 |
| BR-002 Central administration | SR-F-004/005/009/011/016/017/023 | FR-002, FR-007/008/013, FR-019 | UC-004/006/007; US-004–US-006 | AT-003/004, AT-013–AT-016, AT-025/026, AT-048–AT-052 |
| BR-003 Credential ownership | SR-F-006–008, SR-NF-002/003/005/006/008 | FR-003–FR-006 | UC-002/003/006/008; US-002–US-004/010 | AT-005–AT-014 |
| BR-004 Reuse proven MIT code | SR-F-019, SR-NF-011/014 | FR-015 | UC-009; US-009 | AT-028, AT-030 |
| BR-005 Deterministic safe routing | SR-F-010–014, SR-NF-006/007/010/012, SR-F-025, SR-F-026, SR-F-028, SR-F-029 | FR-009/010/014, FR-021, FR-022, FR-024, FR-025 | UC-005/007; US-005/006 | AT-017–AT-020, AT-027, AT-073–AT-079, AT-091–AT-102, AT-119–AT-128, AT-129–AT-138 |
| BR-006 Claude first, Codex next | SR-F-001–003/015, SR-F-029 | FR-011/012, FR-025 | UC-001/005; US-007/008 | AT-021–AT-024, AT-129–AT-138 |
| BR-007 CLI and local UI | SR-F-009/016/017/023, SR-NF-004/013 | FR-008/013/014, FR-019 | UC-001/004/007; US-001/005/006 | AT-015/016, AT-025–AT-027, AT-031, AT-033, AT-048–AT-052 |
| BR-008 Preserve native recovery/process safety | SR-F-015/018, SR-NF-001/004/008, SR-F-024, SR-F-025, SR-F-026, SR-F-027, SR-F-028, SR-F-029 | FR-001/011/012, FR-020, FR-021, FR-022, FR-023, FR-024, FR-025 | UC-001/008; US-001/010 | AT-001/002, AT-021–AT-026, AT-033, AT-057–AT-079, AT-091–AT-102, AT-103–AT-118, AT-119–AT-128, AT-129–AT-138 |
| BR-009 Privacy | SR-F-005/016/017/021, SR-NF-002–005/008/009/014, SR-F-024, SR-F-025, SR-F-026, SR-F-027, SR-F-028, SR-F-029, SR-F-033 | FR-003–FR-007/013/014/017, FR-020, FR-021, FR-022, FR-023, FR-024, FR-025, FR-029 | UC-002/003/006/007/008; US-002–US-006/010/011 | AT-005–AT-014, AT-025–AT-032, AT-057–AT-079, AT-091–AT-102, AT-103–AT-118, AT-119–AT-128, AT-129–AT-138, AT-180–AT-190 |

| BR-001 Multiple providers through Claude | SR-F-001, SR-F-003, SR-F-009–012, SR-F-025, SR-F-026, SR-F-033, SR-F-034 | FR-008, FR-009, FR-011, FR-021, FR-022, FR-029, FR-030 | UC-001, UC-005; US-001, US-007 | AT-015–AT-022, AT-033, AT-073–AT-083, AT-091–AT-102, AT-180–AT-202 |
| BR-002 Central administration | SR-F-004/005/009/011/016/017/023 | FR-002, FR-007/008/013, FR-019 | UC-004/006/007; US-004–US-006 | AT-003/004, AT-013–AT-016, AT-025/026, AT-048–AT-052 |
| BR-003 Credential ownership | SR-F-006–008, SR-NF-002/003/005/006/008 | FR-003–FR-006 | UC-002/003/006/008; US-002–US-004/010 | AT-005–AT-014 |
| BR-004 Reuse proven MIT code | SR-F-019, SR-NF-011/014 | FR-015 | UC-009; US-009 | AT-028, AT-030 |
| BR-005 Deterministic safe routing | SR-F-010–014, SR-NF-006/007/010/012, SR-F-025, SR-F-026, SR-F-028, SR-F-029, SR-F-034, SR-F-035, SR-F-037 | FR-009/010/014, FR-021, FR-022, FR-024, FR-025, FR-030, FR-031, FR-033 | UC-005/007; US-005/006 | AT-017–AT-020, AT-027, AT-073–AT-083, AT-091–AT-102, AT-119–AT-128, AT-129–AT-138, AT-191–AT-202, AT-203–AT-215, AT-227–AT-236 |
| BR-006 Claude first, Codex next | SR-F-001–003/015, SR-F-029, SR-F-037 | FR-011/012, FR-025, FR-033 | UC-001/005; US-007/008 | AT-021–AT-024, AT-129–AT-138, AT-227–AT-236 |
| BR-007 CLI and local UI | SR-F-009/016/017/023, SR-NF-004/013 | FR-008/013/014, FR-019 | UC-001/004/007; US-001/005/006 | AT-015/016, AT-025–AT-027, AT-031, AT-033, AT-048–AT-052 |
| BR-008 Preserve native recovery/process safety | SR-F-015/018, SR-NF-001/004/008, SR-F-024, SR-F-026, SR-F-028, SR-F-029, SR-F-034, SR-F-035, SR-F-037, SR-F-039 | FR-001/011/012, FR-020, FR-022, FR-024, FR-025, FR-030, FR-031, FR-033, FR-035 | UC-001/008; US-001/010 | AT-001/002, AT-021–AT-026, AT-033, AT-057–AT-072, AT-091–AT-102, AT-119–AT-128, AT-129–AT-138, AT-191–AT-202, AT-203–AT-215, AT-227–AT-236, AT-249–AT-258 |
| BR-009 Privacy | SR-F-005/016/017/021, SR-NF-002–005/008/009/014, SR-F-024, SR-F-025, SR-F-026, SR-F-027, SR-F-028, SR-F-029, SR-F-034, SR-F-035, SR-F-037, SR-F-039 | FR-003–FR-007/013/014/017, FR-020, FR-021, FR-022, FR-023, FR-024, FR-025, FR-030, FR-031, FR-033, FR-035 | UC-002/003/006/007/008; US-002–US-006/010/011 | AT-005–AT-014, AT-025–AT-032, AT-057–AT-064, AT-073–AT-083, AT-091–AT-102, AT-103–AT-118, AT-119–AT-128, AT-129–AT-138, AT-191–AT-202, AT-203–AT-215, AT-227–AT-236, AT-249–AT-258 |
| BR-010 Terms/evidence gates | SR-F-004/005/010, SR-NF-012 | FR-002/007/009/015 | UC-003–UC-005/009; US-004/005/009 | AT-003/004, AT-014, AT-017/018, AT-028 |
| BR-011 Private history/public snapshot | SR-F-019/020, SR-NF-002/010/014 | FR-015/016 | UC-010; US-011/012 | AT-028–AT-030 |
| BR-012 Recoverability | SR-F-008/014/018/021, SR-NF-006/008/014 | FR-001/005/006/010/017 | UC-006/008; US-003/004/010 | AT-002, AT-009–AT-012, AT-019/020, AT-030/032 |

## Business-rule trace

| Rule | Downstream ownership | Acceptance |
| --- | --- | --- |
| BR-R01 Credential existence is not acceptance/readiness | SR-F-005/010; FR-007/009 | AT-014/018 |
| BR-R02 Only authorized owner/admin authenticates/imports | SR-F-006/007/016; FR-003/004/013 | AT-005–AT-008, AT-025/026 |
| BR-R03 One request binds one account/generation | SR-F-012; FR-009 | AT-017/018 |
| BR-R04 No rotation after output/tool event | SR-F-013; FR-010 | AT-019/020 |
| BR-R05 Unverified/incompatible route is unavailable | SR-F-003/004; FR-002/009 | AT-004/018/024 |
| BR-R06 Public snapshot excludes local/private artifacts | SR-F-019/020; FR-015/016 | AT-028–AT-030 |

## Phase 65 executable evidence (persistent runtime service)

| Acceptance | Evidence | Status |
| --- | --- | --- |
| AT-035 / FR-018 / SR-F-022 | `tests/lifecycle/resident-runtime.test.ts` | Verified resident lease prevents zero-lease idle shutdown after launcher release; concurrent launchers reuse one runtime with independent leases; explicit shutdown revokes sessions, closes listeners, and cleans artifacts; idempotent shutdown; authenticated `/shutdown` route; already-running resident is a no-op; foreign listener fails closed without authorization; stale-record recovery; stop refuses launcher-owned instances; bounded launcher drain |
| AT-035 service definitions | `tests/service-manager/definitions.test.ts`, `tests/service-manager/adapters.test.ts` | Verified LaunchAgent plist and systemd user unit content (absolute paths, restart-on-failure, no credentials/identity/env), idempotent register, launchctl/systemctl command sequences with fake runner, already-loaded tolerance, file modes, unsupported-platform actionability |
| AT-035 init/CLI | `tests/unit/cli-init.test.ts`, `tests/unit/cli-gateway.test.ts`, `tests/unit/cli-main.test.ts` | Verified `rly init` registers/starts service, writes `installation.json`, reports readiness, idempotent re-init, unsupported platform skip, readiness failure; `rly gateway start|stop|status` flows; reserved-command parsing |
| AT-035 identity handshake | `tests/lifecycle/gateway-server.test.ts`, `tests/lifecycle/gateway-lifecycle.test.ts` | Verified `/identity` carries `runtimeVersion` + `resident`; `inspectGateway` distinguishes `attested-incompatible` from `occupied-foreign`; `/shutdown` authz and 202-then-close |

## Phase 33 executable evidence (macOS launchd adapter, #33)

| Acceptance | Evidence | Status |
| --- | --- | --- |
| AT-036 / FR-018 / SR-F-022 definitions | `tests/service-manager/definitions.test.ts` | Verified plist renders stable Label, absolute ProgramArguments, RunAtLoad, KeepAlive, explicit bounded ThrottleInterval, WorkingDirectory, StandardOut/ErrPath; no credentials/env/account identity; WorkingDirectory omitted unless provided |
| AT-036 / FR-018 / SR-F-022 adapter | `tests/service-manager/adapters.test.ts` | Verified idempotent register without duplicate labels; changed-definition repair unloads before reloading; unchanged re-registration stays a no-op reload; launchctl v2 bootstrap/kickstart/bootout/print with legacy load/start/unload/list fallback; restart via `kickstart -k`; status parses `state = running` and pid from `print` (and legacy `list`); loaded-but-not-running reports stopped; stop tolerates an unloaded job; root refusal; 0600 plist / 0700 LaunchAgents dir; unregister removes only the RLY plist |
| AT-036 init/CLI | `tests/unit/cli-init.test.ts`, `tests/unit/cli-gateway.test.ts` | Verified `rly init` passes the durable log path (`~/.rly/logs/service.log`) and working directory into the adapter; `rly gateway status` reports service label/load state/pid on macOS separately from runtime readiness; secret-free output |
| AT-036 live smoke | `tests/service-manager/launch-agent-live.test.ts` | macOS-only, opt-in `RLY_LIVE_LAUNCHAGENT_SMOKE=1`; skipped ≠ pass. Proves real launchd bootstrap → running → idempotent start → restart → stop → unregister in a GUI user session when CI/runtime permits |

## Phase 68 executable evidence (capability based model selection, #68)

| Acceptance | Evidence | Status |
| --- | --- | --- |
| AT-044 | `tests/unit/model-selection.test.ts` | Verified deterministic selection from trusted registry evidence only: one eligible candidate; stable document-order tie-break with identical-input determinism; same upstream model id through two access providers stays provider-scoped (cross-provider exact lookup fails closed); aggregator multi-family candidate retrieval with preferred-family filtering; required protocol capabilities evaluated before ranking via existing `missingCapabilities`; reasoning intent eligibility against `ReasoningCapabilityEvidence` (required and reasoning-with-tools); BROKEN always rejected; EXPERIMENTAL rejected by the default normal-user candidate policy and allowed with explicit opt-in; frozen, secret-free decision trace with allowlisted keys only |
| AT-045 | `tests/unit/model-selection.test.ts`, `tests/lifecycle/profile-pool-route.test.ts`, `tests/lifecycle/codex-profile-route.test.ts` | Verified two-stage boundary: `resolveProfileRoute` runs `selectModel` before `RouteSelector`; the selected physical model is frozen in the effective request/route and fed to the account selector; `/v1/route-traces` carries the `modelSelection` decision beside the account decision; existing pool eligibility/affinity/quota/credential-generation behavior and Codex/Cline exact-evidence routes remain unchanged |
| AT-046 | `tests/unit/model-selection.test.ts`, `tests/lifecycle/codex-profile-route.test.ts` | Verified typed failure taxonomy (`unknown-exact-model`, `no-trusted-evidence`, `capability-unsupported`, `reasoning-unsupported`, `compatibility-rejected`, `no-eligible-candidate`) mapped deliberately onto the existing `capability-rejected` profile error contract with the actionable reason attached; no silent substitution or downgrade |
| AT-047 | `tests/unit/model-selection.test.ts`, `tests/lifecycle/profile-pool-route.test.ts` | Verified exact physical model path: exact pins resolve to the exact registry entry without rerouting, validate required capabilities and compatibility, reject BROKEN pins, and treat an explicit EXPERIMENTAL pin as an opt-in for that exact model; unknown pins fail closed |
| SR-F-004 / FR-009 / FR-010 / BR-005 (unchanged) | `tests/routing/*`, `tests/lifecycle/*`, `tests/unit/router.test.ts` | Verified `decideRoute` capability assertion, account eligibility → strategy → immutable `EffectiveRoute`, and pre-output bounded rotation semantics unchanged after the model-selection stage was added |

## Phase 70 executable evidence (provider-neutral reasoning intelligence layer, #70)

| Acceptance | Evidence | Status |
| --- | --- | --- |
| AT-053 | `tests/unit/reasoning-translate.test.ts`, `tests/contract/providers/direct-adapters.test.ts`, `tests/unit/model-registry.test.ts` | Verified deterministic provider-owned translation boundary: six-level discrete, two-level, binary, adaptive, token-budget (reviewed policy only, else fail closed), and unsupported control shapes; exact same-family source effort preserved; intent→nearest-level deterministic mappings with explicit `exact|normalized|downgraded|default` metadata and fallback reasons; `OFF`/`AUTO` delegation; unsupported explicit intents fail closed (`unsupported-reasoning`) unless an explicit best-effort policy downgrades with a recorded reason; secret-free frozen result |
| AT-054 | `tests/contract/anthropic/messages.test.ts`, `tests/fixtures/upstream/claude-code/reasoning-shape.json`, `tests/contract/openai-responses/responses.test.ts` | Verified decoder fidelity: Anthropic `thinking.type` + documented additive `effort` decode into canonical `ReasoningRequest` preserving `sourceMode`/`sourceEffort`; pinned supported-baseline fixture drives the decoder (no assumed field names); unknown additive fields recorded as ignored; Responses `reasoning.effort` preserved instead of collapsed; existing thinking/redacted-thinking/streaming/tool behavior unchanged |
| AT-055 | `tests/unit/reasoning-translate.test.ts`, `tests/lifecycle/profile-pool-route.test.ts`, `tests/integration/direct-provider-runtime.test.ts`, `tests/contract/providers/direct-adapters.test.ts` | Verified routing wiring: `resolveRoute`/`decideRoute`/effective routes carry the translation result; `/v1/route-traces` carries allowlisted requested/canonical/effective/mapping/fallback metadata and no reasoning text, prompts, responses, or credentials; OpenRouter/direct adapter emits the provider-native parameter from the translation result and no longer collapses `enabled`/`adaptive`; binary evidence still emits `reasoning.enabled`, discrete evidence emits native effort |
| AT-056 | `tests/unit/model-selection.test.ts`, `tests/lifecycle/profile-pool-route.test.ts` | Verified #68 gate wiring: explicit non-`OFF`/non-`AUTO` intent plus tool use requires `reasoningWithTools` evidence (`withTools`); unsupported coexistence fails closed with `reasoning-unsupported`; eligibility and model-selection behavior otherwise unchanged |
| SR-F-004 / FR-009 / FR-010 / BR-005 (unchanged) | `tests/routing/*`, `tests/lifecycle/*`, `tests/unit/router.test.ts` | Verified route/account selection, immutable route, and rotation semantics unchanged after the reasoning translation stage was added |

## Phase 69 executable evidence (provider/family-scoped model tier resolver, #69)

| Acceptance | Evidence | Status |
| --- | --- | --- |
| AT-057 | `tests/unit/tier-resolver.test.ts` | Verified `haiku`/`sonnet`/`opus`/`fable` typed separately from physical model ids with stable parsing; `TierResolutionContext` carries access provider + parent model/model family; resolution receives both provider and family context when available |
| AT-058 | `tests/unit/tier-resolver.test.ts` | Verified direct single-family provider resolves every tier to the deterministic #68 winner inside that provider (derived); provider without an eligible same-family target fails closed `tier-unavailable`; no silent cross-provider substitution |
| AT-059 | `tests/unit/tier-resolver.test.ts`, `tests/lifecycle/cline-profile-route.test.ts` | Verified ClinePass aggregator fixtures: Terra-family parent + `fable` → `gpt-5.6-sol` (not Anthropic/DeepSeek); DeepSeek V4 Flash parent + `fable` → `deepseek-v4-pro`; Anthropic parent + `fable` → `claude-fable` (stays in Anthropic family); end-to-end `model: fable` through a ClinePass profile reaches upstream as `gpt-5.6-sol` |
| AT-060 | `tests/unit/tier-resolver.test.ts`, `tests/lifecycle/profile-pool-route.test.ts` | Verified no same-family target + fallback disabled fails closed `tier-unavailable` (HTTP `tier-unavailable` + reason on an all-EXPERIMENTAL family); cross-family fallback only when explicitly enabled and recorded in the trace; cross-provider fallback requires an explicit provider list |
| AT-061 | `tests/unit/tier-resolver.test.ts`, `tests/control-plane/repositories.test.ts`, `tests/unit/profiles.test.ts` | Verified user override pins a tier target (`override-rejected` for unknown/BROKEN targets); reviewed mapping without trusted evidence fails closed `mapping-invalid`; `profile.modelRoles` accepts tier keys as per-profile overrides and rejects unknown keys |
| AT-062 | `tests/unit/tier-resolver.test.ts` | Verified default tier mapping is a frozen revisioned document; resolution records `mappingRevision` + `registryRevision`; identical inputs resolve identically; catalog refresh (#23) never mutates trusted tier mappings (BL-042 wording sharpened) |
| AT-063 | `tests/unit/tier-resolver.test.ts`, `tests/lifecycle/cline-profile-route.test.ts` | Verified tier target passes #68 exact selection (trace `modelSelection.source = exact`) then the existing pool/account selector; tier resolution never selects accounts/credentials and never emits provider-native reasoning fields |
| AT-064 | `tests/unit/tier-resolver.test.ts`, `tests/lifecycle/cline-profile-route.test.ts`, `tests/unit/profiles.test.ts`, `tests/lifecycle/profile-pool-route.test.ts` | Verified secret-free tier traces (requested tier, provider, family, parent, mapping source, selected target, fallback reason; no prompt/credential/identity keys); existing `primary`/`fast`/`reasoning` helper mapping and Codex/Cline/profile-pool routes unchanged; #24/#71 still own the supported Claude Code baseline's native `fable` alias classification |

## Phase 34 executable evidence (Linux systemd user service, #34)

| Acceptance | Evidence | Status |
| --- | --- | --- |
| AT-040 / FR-018 / SR-F-022 definitions | `tests/service-manager/definitions.test.ts` | Verified systemd user unit renders stable Description, absolute ExecStart, `Restart=on-failure` + explicit `RestartSec`, bounded `StartLimitIntervalSec`/`StartLimitBurst`, optional WorkingDirectory and `StandardOutput/StandardError=append:` into the durable RLY log dir, systemd `%` specifier doubling and whitespace quoting; no credentials/env/account identity; optional blocks absent unless provided |
| AT-040 / FR-018 / SR-F-022 adapter | `tests/service-manager/adapters.test.ts` | Verified no-root guard; user-manager bus probe before any mutating op with actionable no-user-manager error (containers/WSL guidance, deliberate no `loginctl enable-linger`); idempotent register with change-only `daemon-reload`; stale-definition repair without duplicate units; `enable --now` start, `restart` for the #73 controlled-restart path, stop tolerating not-loaded; `detail()` parses `systemctl --user show` ActiveState/MainPID/UnitFileState into registered/loaded/running/pid/enabled/activeState; failed unit state surfaced; unreachable manager reports registered-but-unconfirmed instead of loaded; unregister `disable --now` + removes only the RLY unit + `daemon-reload`; 0600 unit / 0700 dir; secret-free unit content |
| AT-040 / FR-018 / SR-F-022 CLI | `tests/unit/cli-gateway.test.ts`, `tests/unit/cli-init.test.ts` | Verified `rly gateway status` reports Linux systemd service label/load state/pid/enabled separately from runtime readiness; init passes durable log/working paths into the adapter |
| AT-040 live smoke | `tests/service-manager/systemd-user-live.test.ts` | Linux-only, opt-in `RLY_LIVE_SYSTEMD_SMOKE=1`; skipped ≠ pass. Proves real `systemd --user` register → enable/start → running → restart → stop → unregister in a logged-in user session when CI/runtime permits |

## Phase 66 executable evidence (`rly config` control plane, #66)

| Acceptance | Evidence | Status |
| --- | --- | --- |
| AT-048 / FR-019 / SR-F-023 | `tests/unit/cli-config.test.ts` | Verified durable config resolution from the `~/.rly` installation record (recorded absolute config path), schema-defaults fallback for a missing recorded file, explicit `--config` and CWD dev fallback, and an actionable uninitialized error; `rly config` runs from an unrelated working directory against a live management listener without any local `gateway.config.toml` |
| AT-049 / FR-019 / SR-F-023 | `tests/unit/cli-config.test.ts`, `tests/lifecycle/config-recovery.test.ts` | Verified resident-runtime ensure/recover: reuse of an attested compatible runtime (resident or launcher-owned), service-manager start + bounded readiness wait when the registered service is down, session-scoped foreground fallback, occupied-foreign and attested-incompatible fail-closed; a real resident runtime stays attested after `rly config` returns (closing the UI does not stop it) |
| AT-050 / FR-019 / SR-F-023 | `tests/unit/cli-config.test.ts`, `tests/lifecycle/config-recovery.test.ts` | Verified user-facing provider/account/pool/profile create/list through the same management endpoints as `rly admin`; a `rly config` provider mutation publishes one policy revision readable by the admin/management surface; multi-account and provider-scoped pool membership remain governed by the control-plane store |
| AT-051 / FR-019 / SR-F-023 | `tests/unit/cli-config.test.ts`, `tests/management/credentials.test.ts` | Verified credential login/import/refresh/revoke flows reuse the credential broker and persist only handle/generation metadata; stale versioned mutations surface `stale-version` explicitly and exit non-zero |
| AT-052 / FR-019 / SR-F-023 | `tests/unit/cli-config.test.ts`, `tests/lifecycle/config-recovery.test.ts`, `tests/privacy/*` | Verified headless bootstrap URL behavior (fragment token, no browser in `--headless`), loopback-only management, and secret-free CLI output (no token/secret/authorization/email/identity keys); the shared management client keeps `rly admin` and `rly config` on one API/policy source of truth |

## Phase 74 executable evidence (RLY Claude configuration overlay, #74)

| Acceptance | Evidence | Status |
| --- | --- | --- |
| AT-065 / FR-020 / SR-F-024 | `tests/lifecycle/claude-overlay.test.ts`, `tests/lifecycle/child-launcher.test.ts`, `tests/e2e/claude-code/overlay.e2e.test.ts` | Verified one durable private namespace `<control-plane>/claude` with `0700` dirs/`0600` files; `launchClaude(configDirectory)` uses the durable overlay and never removes it, while the undefined-`configDirectory` standalone fallback keeps the throwaway temp isolation; the interactive launch contract (`runHarnessCommand`) always prepares and passes the overlay and fails on overlay errors instead of falling back to a sandbox; typed allowlist composition (settings merge, agents/commands/skills refresh, plugin enablement declaration); gated real-Claude E2E (`RLY_CLAUDE_E2E=1`, skipped ≠ pass) |
| AT-066 / FR-020 / SR-F-024 | `tests/lifecycle/claude-overlay.test.ts`, `tests/e2e/claude-code/overlay.e2e.test.ts` | Verified native settings one-way merge: unrelated keys and native `model` preserved in the overlay, gateway-conflict `env` keys stripped, native settings file hash unchanged after launch and simulated `/model` activity |
| AT-067 / FR-020 / SR-F-024 | `tests/lifecycle/claude-overlay.test.ts`, `tests/e2e/claude-code/overlay.e2e.test.ts` | Verified `agents/*.md`, `commands/*.md`, and `skills/**` one-way refresh copies with allowlist only; unknown extensions, `plugins/cache`, and unrelated files never copied; `node_modules`/`.git` excluded from skills recursion; native fixtures byte-identical |
| AT-068 / FR-020 / SR-F-024 | `tests/lifecycle/claude-overlay.test.ts`, `tests/e2e/claude-code/overlay.e2e.test.ts` | Verified `plugins/config.json` carries only `enabledPlugins`/`marketplaces`; `oauthAccounts`/token-like keys and plugin cache/repos never copied; no RLY auth or provider credential material in overlay files |
| AT-069 / FR-020 / SR-F-024 | `tests/lifecycle/claude-overlay.test.ts`, `tests/e2e/claude-code/overlay.e2e.test.ts` | Verified RLY-only projection model (`claude-rly-*`) persisted into the overlay only; native plain-Claude model/hash unchanged; overlay settings contain no `ANTHROPIC_BASE_URL`/auth token/projection leakage that a plain launch could inherit |
| AT-070 / FR-020 / SR-F-024 | `tests/lifecycle/claude-overlay.test.ts`, `tests/e2e/claude-code/overlay.e2e.test.ts` | Verified RLY model/session state survives re-prepare and relaunch when native input is unchanged; native changes re-compose one-way while RLY-only model state wins; durable overlay never deleted on child exit; project-local `.claude` untouched (home-level `~/.claude.json` sentinel byte-identical) |
| AT-071 / FR-020 / SR-F-024 | `tests/lifecycle/claude-overlay.test.ts` | Verified concurrent `prepareClaudeOverlay` calls converge to one consistent overlay without locks or corruption; unchanged native input is not rewritten so sibling `/model` writes survive; no copy-back/restore of native settings anywhere |
| AT-072 / FR-020 / SR-F-024 | `tests/lifecycle/claude-overlay.test.ts`, `tests/unit/cli-diagnostics.test.ts` | Verified private permissions, secret-free `readClaudeOverlayStatus` (directory/source/allowlistVersion/lastComposedAt only), `rly status` overlay summary, and no credential/identity material in overlay files or diagnostics |
| SR-F-015 (unchanged) | `tests/lifecycle/global-config-isolation.test.ts` | Verified the before/after-hash global-isolation contract still holds with real overlay preparation against a temporary HOME (native Claude/Codex config byte-identical) |

## Phase 71 executable evidence (Claude Code subagent model resolution, #71)

| Acceptance | Evidence | Status |
| --- | --- | --- |
| AT-084 | `tests/contract/anthropic/messages.test.ts`, `tests/unit/agent-context.test.ts`, `tests/lifecycle/subagent-profile-route.test.ts`, `tests/e2e/claude-code/subagent-model.e2e.test.ts` | Verified `X-Claude-Code-Session-Id`/`-Agent-Id`/`-Parent-Agent-Id` parse into a typed canonical agent context (case-insensitive, partial attribution, empty/missing → no context) at the Anthropic decoder from real HTTP headers; no prompt/body inspection; gated real-Claude E2E (`RLY_CLAUDE_E2E=1`, skipped ≠ pass) round-trips a real main session and a header-carrying subagent request |
| AT-085 | `tests/unit/agent-context.test.ts`, `tests/lifecycle/subagent-profile-route.test.ts` | Verified `AgentExecutionContextRegistry` records launch-bound execution contexts (provider, frozen model, family, tier, revisions) after successful resolution, validates lease activity on every read, removes entries on `dropLease`/expiry, keeps sessions/agents isolated, and never stores credentials/identity |
| AT-086 | `tests/lifecycle/subagent-profile-route.test.ts`, `tests/unit/tier-resolver.test.ts` | Verified subagent tier resolution order: exact parent-agent context → session main context → profile-default fallback; the resolved parent model/family feeds #69 and the trace records `contextSource`/`parentModelId`/`parentModelFamily`; no family determinable on a multi-family provider fails closed `tier-unavailable` + `family-unknown` cause |
| AT-087 | `tests/lifecycle/subagent-profile-route.test.ts`, `tests/e2e/claude-code/subagent-model.e2e.test.ts` | Verified main Terra session → Kongming `fable` → Sol upstream target; parent/main requests keep resolving to Terra after the subagent completes; a `haiku` reviewer resolves independently via a per-profile tier override without changing Kongming/main mappings; real Claude Code E2E keeps the main session on Terra |
| AT-088 | `tests/lifecycle/subagent-profile-route.test.ts`, `tests/unit/agent-context.test.ts` | Verified parallel subagents with different agent ids/tiers resolve independently under one launch session (Kongming→Sol, reviewer→Terra concurrently) with no context leakage; lease revocation removes all contexts (size → 0) |
| AT-089 | `tests/lifecycle/subagent-profile-route.test.ts` | Verified explicit subagent `effort` (`high`) is preserved into the canonical reasoning request and the translated trace (requested/canonical intent); a tool-using subagent with explicit effort demands reasoning-with-tools evidence and fails closed (`tier-unavailable` + `mapping-invalid` reason + `reasoning-unsupported` cause) |
| AT-090 | `tests/lifecycle/subagent-profile-route.test.ts`, `tests/privacy/*`, `tests/e2e/claude-code/subagent-model.e2e.test.ts` | Verified route traces carry only hashed pseudonyms for session/agent/parent linkage plus parent model/family metadata; serialized traces and E2E output contain no prompts, credentials, raw agent ids, or account identity; no global subagent model override exists; the source `model: fable` agent file stays byte-identical (never rewritten to a physical model) |
| FR-021 / SR-F-025 | `tests/unit/agent-context.test.ts`, `tests/lifecycle/subagent-profile-route.test.ts`, `tests/contract/anthropic/messages.test.ts`, `tests/e2e/claude-code/subagent-model.e2e.test.ts` | Verified the end-to-end subagent pipeline (ingress attribution → execution-context registry → parent context → #69 tier → #68 selection → #70 reasoning → existing account pool) and the isolation/fail-closed/privacy contracts in FR-021/SR-F-025 |
| SR-F-004 / FR-009 / FR-010 / BR-005 (unchanged) | `tests/routing/*`, `tests/lifecycle/*` | Verified existing route/account selection, immutable route, and rotation semantics unchanged after the subagent context stage was added; profile-pool/codex/cline lifecycle suites stay green |

## Phase 72 executable evidence (gateway model discovery and projection, #72)

| Acceptance | Evidence | Status |
| --- | --- | --- |
| AT-073 / FR-021 / SR-F-025 | `tests/lifecycle/gateway-models-route.test.ts`, `tests/unit/model-projection.test.ts` | Verified authenticated `GET /v1/models` on the gateway listener with the Anthropic-compatible discovery shape (`type`/`id`/`display_name`/`created_at`, `has_more`/`first_id`/`last_id`) and `limit`/`before_id`/`after_id` pagination; 401 without launch/gateway credentials; session token serves the pinned universe while the instance bearer serves the policy-derived universe; secret-free discovery bodies |
| AT-074 / FR-021 / SR-F-025 | `tests/lifecycle/child-launcher.test.ts`, `tests/lifecycle/claude-overlay.test.ts`, `tests/lifecycle/gateway-models-route.test.ts` | Verified `CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY=1` is child-only (parent env unchanged); every discoverable id begins with `claude`/`anthropic` (canary) and uses the `claude-rly-` namespace; the discovery flag is stripped from native settings env in the overlay (allowlist v2) so native settings cannot disable RLY discovery and plain launches never inherit it |
| AT-075 / FR-021 / SR-F-025 | `tests/unit/model-projection.test.ts`, `tests/lifecycle/gateway-models-route.test.ts` | Verified explicit reverse mapping (`resolveProjection`): a projection id resolves to one exact registry evidence target + pinned pool; routing never parses id strings; identical inputs resolve identically; unknown/removed ids fail closed |
| AT-076 / FR-021 / SR-F-025 | `tests/unit/model-projection.test.ts`, `tests/lifecycle/gateway-models-route.test.ts` | Verified `GPT-5.6 Sol (Codex)` and `GPT-5.6 Sol (ClinePass)` are distinct projections (distinct ids/labels) and each selected projection routes through the correct provider pool + account (`acct-codex-a` vs `acct-cline-a`) |
| AT-077 / FR-021 / SR-F-025 | `tests/unit/model-projection.test.ts`, `tests/lifecycle/gateway-models-route.test.ts` | Verified universe compilation from the policy: profile pool binding + single-eligible-default-pool providers only; disabled providers, multi-pool providers without an explicit binding, and providers without ready accounts are excluded; discovery lists no unreviewed/proposed/BROKEN entries |
| AT-078 / FR-021 / SR-F-025 | `tests/unit/model-projection.test.ts`, `tests/lifecycle/gateway-models-route.test.ts` | Verified VERIFIED-only default discovery; EXPERIMENTAL appears only with `gateway.modelDiscovery.experimentalModels=true`; BROKEN never appears even with the opt-in |
| AT-079 / FR-021 / SR-F-025 | `tests/lifecycle/gateway-models-route.test.ts` | Verified a session configured with Codex + ClinePass + DeepSeek discovers models from all allowed bindings without manual mapping; Codex/ClinePass/DeepSeek selections each route to their own pool; the ClinePass aggregator exposes multiple model families with correct family/provider identity |
| AT-080 / FR-021 / SR-F-025 | `tests/unit/model-projection.test.ts`, `tests/lifecycle/gateway-models-route.test.ts` | Verified session universe pinning: a policy change after session issue does not change the session's discovery and does not remap an already-issued projection id; a model removed from the registry fails closed instead of substituting |
| AT-081 / FR-021 / SR-F-025 | `tests/lifecycle/gateway-models-route.test.ts`, `tests/unit/model-projection.test.ts` | Verified unknown projection ids and removed/ineligible targets return an actionable typed `model-unavailable` error (or `capability-rejected` with the typed reason) with no silent model/provider substitution |
| AT-082 / FR-021 / SR-F-025 | `tests/lifecycle/gateway-models-route.test.ts` | Verified exact projected selection passes #68 exact selection (trace `source=exact`) and #70 reasoning translation (thinking request → `BALANCED`/`normalized`); route traces carry allowlisted projection id/display metadata + exact model/account decisions and no secrets |
| AT-083 / FR-021 / SR-F-025 | `tests/lifecycle/claude-overlay.test.ts`, `tests/lifecycle/gateway-models-route.test.ts` | Verified RLY-only projection persistence stays inside the overlay (AT-069 base), the discovery flag is child-env only, and concurrent sessions have no global `model` write-back (overlay #74 contract unchanged); discovery/trace bodies secret-free |

## Phase 24 executable evidence (runtime compatibility canary, #24 / BL-043)

| Acceptance | Evidence | Status |
| --- | --- | --- |
| AT-091 / FR-022 / SR-F-026 | `tests/unit/targets.test.ts`, `src/targets/versions.ts`, `src/canary/installed.ts` | Verified exact semantic/version parsing from `--version` output; silent/broken/missing binaries report `unknown`; version never inferred from timestamps/paths; `detectInstalledClients` combines `found` + exact version into separate fields |
| AT-092 / FR-022 / SR-F-026 | `src/canary/client-fixtures.ts`, `tests/fixtures/upstream/claude-code/client-contract-2.1.229.json`, `tests/fixtures/upstream/claude-code/{model-discovery-shape,fable-subagent-shape,effort-signal-shape,streaming-framing}.json`, `tests/fixtures/upstream/codex/client-contract-observed.json`, `tests/unit/canary-matrix.test.ts` | Verified the pinned baseline contract (attribution headers, discovery wire + id-prefix filter + startup cache, tier aliases incl. `fable`, effort additive field, streaming framing, `--no-session-persistence`) and that the embedded contract matches the golden fixture (no embedded drift); fixtures redacted/synthetic |
| AT-093 / FR-022 / SR-F-026 | `tests/unit/canary-matrix.test.ts`, `tests/unit/canary-evidence.test.ts` | Verified text/streaming/cancellation gates pass for a supported access path and capability-gated `not-run` states are never reported as passed |
| AT-094 / FR-022 / SR-F-026 | `tests/unit/canary-matrix.test.ts` | Verified single/multi tool loop aggregation; `tools-parallel` is `not-run` without `parallelTools` evidence (never advertised stronger); a malformed tool continuation fails `tools-single`/`tools-multi` with `tool-result-invalid`/`tool-continuation-invalid` |
| AT-095 / FR-022 / SR-F-026 | `tests/unit/canary-matrix.test.ts`, `tests/unit/canary-evidence.test.ts` | Verified reasoning gate preserves `effort` (`reasoning-effort-clamped` on loss); `reasoning-tools` requires `reasoningWithTools` evidence and is otherwise `not-run`; effort-signal gate fails `effort-signal-lost` when the request field is lost |
| AT-096 / FR-022 / SR-F-026 | `tests/unit/canary-matrix.test.ts`, `tests/unit/canary-projection.test.ts` | Verified the discovery gate pins `GET /v1/models`, the `claude`/`anthropic` id-prefix filter, the `claude-rly-` namespace, and startup cache; a changed filter fails `gateway-model-filter-changed`; canary-derived compatibility gates the #72 projection (VERIFIED default, EXPERIMENTAL opt-in, BROKEN never, independent per provider) |
| AT-097 / FR-022 / SR-F-026 | `tests/unit/canary-matrix.test.ts` | Verified `fable`/tier aliases, session/agent/parent attribution parsing, parallel subagent attribution isolation, and the effort signal reaching the decoder (`sourceEffort`); missing agent header fails `missing-agent-header` |
| AT-098 / FR-022 / SR-F-026 | `tests/unit/canary-evidence.test.ts`, `tests/unit/canary-projection.test.ts` | Verified evidence identity keyed by exact client+provider+adapter+physical model; same upstream model through two providers keeps independent states (no cross-provider reuse); every trusted access path evaluated deterministically (identical inputs → identical evidence) |
| AT-099 / FR-022 / SR-F-026 | `tests/unit/canary-evidence.test.ts`, `tests/unit/cli-canary.test.ts` | Verified VERIFIED requires all required gates + live evidence; fake-only matrix classifies EXPERIMENTAL; a failed required gate is BROKEN; missing/unrun required gates are `unknown` and never reported as passed; `rly canary run` never reports fake evidence as VERIFIED; newly installed unknown versions surface `unknown/not-tested` (`doctor`/`status`) and never replace the tested baseline |
| AT-100 / FR-022 / SR-F-026 | `tests/unit/canary-matrix.test.ts` | Verified deliberately changed fixtures fail exactly the corresponding gate with the typed reason: `missing-agent-header`, `gateway-model-filter-changed`, `tool-result-invalid`, `reasoning-effort-clamped`, `effort-signal-lost` |
| AT-101 / FR-022 / SR-F-026 | `tests/unit/cli-canary.test.ts`, `tests/privacy/canary.test.ts`, `src/canary/artifact.ts`, `src/canary/proposals.ts` | Verified `rly canary run` persists a secret-free artifact under `<control-plane>/canary/`, `rly canary status` reports tested baselines + per-path verdicts, malformed artifacts fail closed on read, proposals never mutate `directProviderRegistry` (trusted state unchanged after proposal), and canary state is consumable by the #72 projection gate without mutation |
| AT-102 / FR-022 / SR-F-026 | `tests/privacy/canary.test.ts`, `tests/unit/cli-canary.test.ts`, `tests/unit/cli-diagnostics.test.ts` | Verified canary artifacts/logs carry only client/provider/model ids, gate names, status, fixture revision, and redacted error categories; no prompts, responses, reasoning text, credentials, authorization headers, email, or account identity; artifacts created with private modes (`0700` dir / `0600` file); live evidence stays opt-in (`RLY_LIVE_CANARY`, skipped ≠ pass) |
| SR-F-004 / FR-009 / FR-010 / BR-005 (unchanged) | `tests/unit/model-registry.test.ts`, `tests/lifecycle/*`, `tests/routing/*` | Verified existing route/account selection, immutable route, registry, projection, overlay, and lifecycle suites stay green after the canary layer was added |

## Phase 73 executable evidence (safe zero-downtime runtime update lifecycle, #73)

| Acceptance | Evidence | Status |
| --- | --- | --- |
| AT-103 / FR-023 / SR-F-027 | `tests/unit/update-state-store.test.ts`, `tests/lifecycle/runtime-update.test.ts` | Verified durable states (`installing`/`pending-activation`/`activating`/`active`/`rollback-required`/`failed`) persisted private (0600) and secret-free; CAS transitions; installation never implies activation; a candidate is active only after the restarted runtime passes identity/readiness verification |
| AT-104 / FR-023 / SR-F-027 | `tests/lifecycle/runtime-update.test.ts`, `tests/lifecycle/gateway-server.test.ts` | Verified `/identity` reports the serving runtime version, durable state/schema version, update state (pending/previous), active launch-session count, and drain flag through the attested handshake — never the package/CLI version alone |
| AT-105 / FR-023 / SR-F-027 | `tests/lifecycle/runtime-update.test.ts` | Verified zero active sessions ⇒ one controlled service-manager restart activates the candidate; update state `active`, serving runtime version flips to the candidate, restart count = 1 |
| AT-106 / FR-023 / SR-F-027 | `tests/lifecycle/runtime-update.test.ts` | Verified one active launch session ⇒ installation completes, the old runtime keeps serving (authorized session-token round-trip), activation waits for the session to end, the session is never killed, and activation completes only after release |
| AT-107 / FR-023 / SR-F-027 | `tests/lifecycle/runtime-update.test.ts` | Verified multiple concurrent sessions keep the update pending until every session ends; each session keeps working on the old runtime |
| AT-108 / FR-023 / SR-F-027 | `tests/lifecycle/runtime-update.test.ts` | Verified an in-flight session-token round-trip succeeds on the old runtime while pending and the same token is not accepted (401) by the new runtime after activation — never moved or replayed |
| AT-109 / FR-023 / SR-F-027 | `tests/lifecycle/runtime-update.test.ts`, `tests/unit/cli-update.test.ts`, `tests/unit/update-policy.test.ts` | Verified the documented launch policy: a compatible pair keeps issuing launches on the old runtime while pending; an incompatible pair refuses only new launches with an actionable `update-pending`/`runtime-version-mismatch` message and never touches existing sessions; `status`/`doctor` show pending state |
| AT-110 / FR-023 / SR-F-027 | `tests/lifecycle/runtime-update.test.ts` | Verified authenticated `POST /drain` sets `draining`, refuses `POST /v1/launch-sessions` with `update-pending`, and rejects unauthenticated drain requests |
| AT-111 / FR-023 / SR-F-027 | `tests/lifecycle/runtime-update.test.ts` | Verified candidate acceptance requires authenticated identity/readiness/state-open verification (serving version equals the pending candidate); a broken candidate triggers exactly one bounded rollback whose success is verified and reported; no restart/rollback loop |
| AT-112 / FR-023 / SR-F-027 | `tests/lifecycle/runtime-update.test.ts`, `tests/unit/update-policy.test.ts` | Verified a forward-only/unrollbackable candidate migration blocks activation before destructive state change with an actionable message; the previous version keeps serving |
| AT-113 / FR-023 / SR-F-027 | `tests/lifecycle/runtime-update.test.ts`, `tests/unit/update-state-store.test.ts` | Verified concurrent `rly update` invocations serialize through the update lock (one completes, the other fails actionably); no double restart, duplicate migration, or state corruption |
| AT-114 / FR-023 / SR-F-027 | `tests/lifecycle/runtime-update.test.ts`, `tests/unit/update-state-store.test.ts` | Verified crash/reboot recovery: stale `installing` → `failed` with retry guidance; `pending-activation` resumes once the candidate verifies; interrupted `activating` rolls back using the preserved previous-version reference; stale update locks are reclaimed by pid/start-identity |
| AT-115 / FR-023 / SR-F-027 | `tests/lifecycle/runtime-update.test.ts` | Verified a foreign/unattested port owner after restart fails closed, is never signaled or killed, and produces a deterministic doctor action without infinite retry |
| AT-116 / FR-023 / SR-F-027 | `tests/unit/cli-diagnostics.test.ts`, `tests/unit/cli-update.test.ts` | Verified `rly status`/`rly doctor` expose allowlisted update metadata (state, current/pending/previous version, active sessions, drain, CLI↔runtime compatibility) with no secret material |
| AT-117 / FR-023 / SR-F-027 | `tests/lifecycle/runtime-update.test.ts`, `tests/unit/update-state-store.test.ts`, `tests/privacy/*` | Verified no-schema updates leave credential stores/account generations untouched; update state, artifacts, and logs contain only versions/timestamps — never credentials, tokens, prompts, responses, or account identity |
| AT-118 / FR-023 / SR-F-027 | `tests/lifecycle/runtime-update.test.ts`, `tests/unit/update-installer.test.ts`, `tests/unit/cli-update.test.ts` | Verified candidate install failure, candidate verification failure, restart failure, rollback success, rollback failure, and the incompatible CLI/runtime handshake each end in a deterministic, actionable state |
| SR-F-004 / FR-009 / FR-010 / BR-005 (unchanged) | `tests/routing/*`, `tests/lifecycle/*` | Verified existing route/account selection, immutable route, and rotation semantics unchanged after the update layer was added |

## Phase 92 executable evidence (immutable deployments and atomic refs, #92)

| Acceptance | Evidence | Status |
| --- | --- | --- |
| AT-139 / FR-023 / SR-F-027 | `tests/unit/update-installer.test.ts`, `tests/lifecycle/runtime-update.test.ts` | Verified `installCandidate` updates only `refs/staged`; `refs/active`/`refs/previous` are untouched by installation; `activateStaged()` is the only primitive switching `active`, and the lifecycle calls it only at the activation transition (never at install); rollback restores `active` and preserves the displaced deployment as `previous` |
| AT-140 / FR-023 / SR-F-027 | `tests/unit/update-installer.test.ts` | Verified two byte-distinct candidates declaring the same semantic version produce distinct SHA-256 artifact ids, coexist under distinct `runtime/versions/<artifactId>` directories, and never alias/overwrite one directory; semantic version is metadata only |
| AT-141 / FR-023 / SR-F-027 | `tests/unit/update-installer.test.ts` | Verified reinstall of the exact same artifact is idempotent: deployment contents byte-identical (no rewrite), single artifact directory, `staged` points at the same artifact id |
| AT-142 / FR-023 / SR-F-027 | `tests/unit/update-installer.test.ts` | Verified `staged`/`active`/`previous` resolve only to validated immutable deployments (private 0700 directory, matching artifact id, valid manifest/entrypoint); refs escaping the store or naming a non-immutable target fail closed with a doctor action |
| AT-143 / FR-023 / SR-F-027 | `tests/unit/update-installer.test.ts` | Verified atomic reference replacement (temp-create + rename + parent fsync): an interrupted ref switch keeps the previous valid reference; rapid-replacement readers observe only old/new valid refs (no gap); stale temp references are cleaned |
| AT-144 / FR-023 / SR-F-027 | `tests/unit/update-installer.test.ts` | Verified an interrupted/failing copy never exposes a partial deployment as staged or active; leftover staging directories are cleaned; a later healthy install succeeds |
| AT-145 / FR-023 / SR-F-027 | `tests/unit/update-installer.test.ts` | Verified legacy `runtime/current`/`previous` + `versions/<semver>` migration: serving deployments renamed to artifact ids (bytes preserved, never deleted), refs established, legacy symlinks removed only after the durable `committed` marker; crash windows between rename and ref creation and between refs and commit marker resume idempotently |
| AT-146 / FR-023 / SR-F-027 | `tests/unit/update-installer.test.ts` | Verified malformed legacy state fails closed with actionable `run rly doctor` errors: dangling/unknown `current`, non-symlink legacy refs, missing deployments during migration — with no legacy bytes deleted and no refs overwritten |
| AT-147 / FR-023 / SR-F-027 | `tests/unit/update-installer.test.ts`, `tests/privacy/*` | Verified deployment metadata contains identifier keys only (artifactId/version/stateVersion/migrationForwardOnly/product/installedAt) with no secret material; private 0600 metadata / 0700 deployment dirs / 0700 refs dir; privacy scan green |
| AT-148 / FR-023 / SR-F-027 | `tests/unit/update-installer.test.ts`, `tests/lifecycle/runtime-update.test.ts`, `tests/unit/update-state-store.test.ts`, `tests/service-manager/*` | Verified concurrent/idempotent staging and the full existing update lifecycle, state-store, service-manager, CLI-update, and privacy regression suites stay green after the immutable store replaced the mutable semver store |
| AT-149 / FR-023 / SR-F-027 | `docs/ARCHITECTURE.md`, `docs/project-decisions.md`, `docs/requirements/functional-requirements.md`, `docs/requirements/system-requirements.md`, `docs/requirements/requirements-traceability-matrix.md`, `docs/requirements/acceptance-test-cases.md` | Verified docs describe content-addressed immutable deployment identity, explicit `staged`/`active`/`previous` reference ownership, atomic reference replacement, legacy migration, and INSTALL != ACTIVATE ownership |

## Phase 93 executable evidence (transactional activation, #93)

| Acceptance | Evidence | Status |
| --- | --- | --- |
| AT-160 / FR-027 / SR-F-031 | `tests/lifecycle/update-transaction.test.ts`, `tests/unit/update-state-store.test.ts` | Verified the update lock records the real OS process-start identity (`identityVerified`); a same-identity live holder is never reclaimed; only a proven stale/dead owner is reclaimed; unverifiable wall-clock locks are conservatively held; `lockStatus()` is secret-free |
| AT-161 / FR-027 / SR-F-031 | `tests/lifecycle/update-transaction.test.ts`, `tests/unit/update-state-store.test.ts` | Verified durable journal phases persist through activation (final record `phase: committed`); recovery from STAGED/DRAINING resumes, SWITCHING/PROBATION/COMMITTING rolls back (never silent commit), COMMITTED promotes, interrupted/duplicate rollback is terminal RECOVERY_REQUIRED |
| AT-162 / FR-027 / SR-F-031 | `tests/lifecycle/update-transaction.test.ts`, `tests/lifecycle/runtime-update.test.ts` | Verified fence-first drain: new launch issuance refused (409 `update-pending`) once draining begins while existing sessions complete (authorized round-trip 200); the old session token is rejected (401) after activation — never replayed |
| AT-163 / FR-027 / SR-F-031 | `tests/lifecycle/update-transaction.test.ts`, `tests/unit/update-policy.test.ts` | Verified `none`/`backward-compatible-expand`/`transactional-replace` pass preflight and activate; `forward-only` (and legacy `migrationForwardOnly: true`) block before destructive activation with active/staged refs untouched and zero restarts; legacy `migrationForwardOnly: false` maps to `backward-compatible-expand` |
| AT-164 / FR-027 / SR-F-031 | `tests/lifecycle/runtime-update.test.ts`, `tests/lifecycle/update-transaction.test.ts` | Verified probation checks exact serving version == staged candidate, management/data protocol compatibility, authenticated `/readyz` (state open), and state/schema compatibility before COMMITTED; a broken candidate fails probation and rolls back |
| AT-165 / FR-027 / SR-F-031 | `tests/lifecycle/runtime-update.test.ts`, `tests/lifecycle/update-transaction.test.ts` | Verified exactly one bounded rollback attempt (journal `rollbackAttempts` ≤ 1) that re-establishes `active`/`previous` from durable evidence (previous written before active; known-good never lost), restarts at most once, verifies the restored runtime, and reports `lastRollbackResult.ok` |
| AT-166 / FR-027 / SR-F-031 | `tests/lifecycle/runtime-update.test.ts`, `tests/lifecycle/update-transaction.test.ts` | Verified rollback failure (foreign listener / restart failure) terminates in explicit `recovery-required` with a `run rly doctor` message; a second `rly update` performs no further restarts and never loops |
| AT-167 / FR-027 / SR-F-031 | `tests/lifecycle/update-transaction.test.ts`, `tests/unit/update-installer.test.ts` | Verified real-ref crash windows before/after fence, ref switch, restart, probation, commit, and rollback: `previous`/`staged`/`active` resolve to validated deployments at every boundary; recovery (`setActiveReferences`) restores refs idempotently and never loses the known-good before COMMITTED; a subsequent update activates normally |
| AT-168 / FR-027 / SR-F-031 | `tests/lifecycle/update-transaction.test.ts`, `tests/unit/cli-update.test.ts`, `tests/unit/cli-diagnostics.test.ts`, `tests/privacy/*` | Verified `rly update`/`status`/`doctor` and `/identity` surface phase, lock owner status, drain state, and last activation/rollback outcome with identifiers only; privacy scans green; no secrets anywhere |
| SR-F-004 / FR-009 / FR-010 / BR-005 (unchanged) | `tests/routing/*`, `tests/lifecycle/*` | Verified existing route/account selection, immutable route, and rotation semantics unchanged after the transactional activation layer was added |

## Maintenance rule

When a requirement changes, update its owning document and this matrix in the same change. When implementation completes, add the executable evidence reference without replacing the stable acceptance ID. If a Must requirement lacks downstream coverage or current evidence, the phase/release cannot be marked accepted.

## Current baseline status

- BR-001/006/008 have partial verified evidence from the completed Claude direct-provider milestone.
- Control-plane foundation evidence exists for schema/migrations, authenticated management, CLI administration, and secret-free DTOs.
- Credential broker and Codex OAuth evidence exists for import, login, refresh CAS, revoke, recovery, and a request-scoped Anthropic route. Request-time pool selection, Claude Code profile integration, and the secret-free local UI are implemented; `rly <profile>` is the canonical Claude Code alias and Codex CLI remains `rly run codex`.
- The model intelligence registry (`src/registry/model-registry.ts`) is the canonical model-data layer and the source of truth for #68-#72: exact access-provider identity, upstream model id/family, capability/limits/reasoning evidence, typed compatibility state, deterministic query helpers, and a discovery→proposal boundary that never mutates reviewed evidence (#23). `ProviderRecord.capabilityEvidence` is typed against the registry schema (was `unknown`). Registry revision 5 (#122) adds an optional `claimRef` to `CompatibilityEvidence` linking a reviewed state to its exact Compatibility Claim key.
- Compatibility Claim and Evidence v2 (#122) replaces the coarse `livePassed` boolean with a versioned, feature-scoped claim model keyed to the exact execution path (client, exact client version/baseline, source protocol/revision, adapter, access provider, auth mode, endpoint contract, physical model, feature). The deterministic canary matrix is reclassified as Layer A evidence; evidence layers A/B/C are explicit and required layers are declared per adapter; `VERIFIED` is unreachable from any observation (promotion is #124); legacy v1 canary outputs are legacy/untrusted for v2 authority; claim docs persist append/audit-friendly under `<control-plane>/claims/` with deterministic lookup by claim identity + feature (FR-028; SR-F-032; AT-169–AT-179).
- The macOS LaunchAgent adapter is delivered by #33 on top of the #65 service-manager contract: one per-user `com.rly.gateway` plist without root, launchctl v2/legacy tolerance, idempotent repair that unloads before reloading, bounded crash-restart, secret-free paths, and service load state/pid reported separately from runtime readiness (AT-036). Linux `systemd --user` specifics remain #34.
- The capability based model selection engine (#68) runs deterministically before account selection: trusted-registry candidates pass hard eligibility (exact evidence, protocol capabilities, reasoning semantics, compatibility state) then rank in stable document order; the selected physical model is frozen into the effective request/route and `RouteSelector` keeps owning account/credential choice within one pool (AT-044–AT-047).
- The provider-neutral reasoning intelligence layer (#70) preserves Claude Code's requested reasoning intent (canonical `OFF`/`ECONOMY`/`BALANCED`/`DEEP`/`MAXIMUM`/`AUTO` plus source mode/effort fidelity) and translates it deterministically per selected model through a provider-owned boundary (`resolveReasoning`) into the exact native control using registry `ReasoningCapabilityEvidence` (discrete effort, binary, adaptive, token-budget, unsupported). Every non-exact mapping is recorded (mapping kind + fallback reason) and explicit unsupported intents fail closed; the OpenRouter adapter no longer collapses `enabled`/`adaptive` into one boolean (AT-053–AT-056).
- The provider/family-scoped model tier resolver (#69) makes portable tier aliases (`haiku`/`sonnet`/`opus`/`fable`) resolve deterministically inside the current execution context (access provider → parent model family → trusted capability evidence) with a frozen reviewed tier mapping, per-profile user overrides, explicit fallback scopes only, and secret-free traces; `model: fable` on a ClinePass profile resolves to the verified same-family target and feeds #68/#70/the account selector (AT-057–AT-064).

- The `rly config` user control plane (#66) is the primary post-install surface on top of the #65 resident runtime: durable `~/.rly` configuration resolution (no CWD `gateway.config.toml` on the normal installed path), resident-runtime ensure/recover, secret-free status, local loopback UI bootstrap through the existing fragment session, and focused provider/account/pool/profile shortcuts over the same management endpoints and policy revision as `rly admin` (AT-048–AT-052).
- The RLY Claude configuration overlay (#74) replaces the throwaway `CLAUDE_CONFIG_DIR` temp directory with a durable RLY-owned namespace under the RLY home: native Claude config stays read/compose-only input through a typed allowlist, RLY-only gateway/model state persists only inside the overlay, RLY session/history state survives launches, concurrent RLY sessions are race-safe, and the no-global-Claude-config-mutation rule is retained (AT-065–AT-072; FR-020; SR-F-024).
- Claude Code subagent model resolution (#71) wires Claude Code agent attribution ingress, a session-scoped execution-context registry, and parent-context tier resolution end to end: `X-Claude-Code-Session-Id`/`-Agent-Id`/`-Parent-Agent-Id` become typed runtime context on the canonical request; a lease-scoped in-memory registry records each agent's resolved provider/model/family/tier; subagent `model: fable` requests resolve inside the parent's execution context through #69 → #68 → #70 → the existing account pool while the parent/main session stays on its model; unknown tiers, missing evidence, reasoning-with-tools gaps, and undeterminable families fail closed with actionable causes; traces carry only hashed agent linkage pseudonyms (AT-084–AT-090; FR-021; SR-F-025).

- The gateway model discovery/projection layer (#72) exposes the configured, trusted RLY model universe to Claude Code through authenticated `GET /v1/models` on the gateway listener with the supported discovery wire contract; projections use the Claude-compatible `claude-rly-*` namespace with an explicit reverse mapping to one exact access-provider/model target + pinned pool; sessions pin their universe (policy/registry revision + bindings) at issue time; `CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY=1` is child-only; exact projected selections pass #68/#70 and route through the pinned pool's account selector, failing closed on unknown/removed/ineligible targets (AT-073–AT-083; FR-021; SR-F-025).

- The macOS LaunchAgent adapter is delivered by #33 on top of the #65 service-manager contract: one per-user `com.rly.gateway` plist without root, launchctl v2/legacy tolerance, idempotent repair that unloads before reloading, bounded crash-restart, secret-free paths, and service load state/pid reported separately from runtime readiness (AT-036). The Linux `systemd --user` adapter is delivered by #34 on the same contract: one per-user `rly-gateway.service` without root, user-manager bus probe with actionable no-linger guidance, change-only `daemon-reload` with idempotent repair, bounded `StartLimit` restart policy, secret-free unit with durable `~/.rly` paths, and unit enabled/active/process state reported separately from runtime readiness (AT-040).
- The safe zero-downtime update lifecycle (#73) is delivered on the #65 identity/version handshake and the #33/#34 service-manager restart, and reworked as a durable transaction by #93: `rly update` separates candidate installation from activation through durable secret-free states (`src/runtime/update/`), runs activation as a journaled transaction (STAGED → DRAINING → SWITCHING → PROBATION → COMMITTING → COMMITTED | ROLLING_BACK | RECOVERY_REQUIRED), establishes the new-launch fence before the serving ref switch, drains launch sessions (not TCP counts) to a safe zero point unless the explicit `--force` path is requested, restarts only attested resident runtimes through the per-user service manager, verifies the new runtime through exact identity/protocol/readiness/state-open checks, rolls back at most once to the previous known-good version on failure (verified, reported, never looped), terminates rollback failure in the explicit `recovery-required` state with an actionable `rly doctor` path, gates migration safety with compatibility classes (none | backward-compatible-expand | transactional-replace | forward-only) blocking forward-only before destructive activation, serializes concurrent updates with an ownership-aware stale-reclaimable lock that records the real OS process-start identity, recovers crash/reboot states deterministically from the durable journal phase (never guessing a candidate committed), refuses new launch-session issuance once draining begins, applies the documented CLI/runtime compatibility launch policy while pending, fails closed on foreign port owners without signaling them, and exposes allowlisted version/update metadata incl. transaction phase and lock-owner status in `status`/`doctor` (FR-023; SR-F-027; FR-027; SR-F-031; AT-103–AT-118; AT-139–AT-149; AT-160–AT-168). Candidate distribution/signing remains #35 (BACKLOG); #73/#93 own the lifecycle once a candidate is obtained.
- Exact evidence mapping is completed phase by phase; no pending row is represented as passing.
- The EffectiveModelDecision control plane (#127) is the FINAL model-control output before account selection: every supported RLY model-routing request (profile, projected, subagent, Codex) assembles ONE typed, secret-free decision (`src/routing/model-decision/`) carrying the #125 selector intent/provenance, the frozen physical provider/model/family target, #69/#71/#72 provenance, the #70 reasoning mapping, the #124 ECR compatibility reference, the provider→pool binding, policy/profile/session/registry/mapping revisions, #126 view/env-settings ownership, visible deterministic conflicts, blocked alternatives, and stable reasons — with no account/credential identity. Deterministic precedence (explicit exact → RLY tier → alias → inherit/override → profile policy → env/settings ownership → persisted view → defaults), freeze-before-account proven across pool failover, subagent isolation (parent never mutated), and stale/foreign persisted-state rejection/explanation are tested (FR-031; SR-F-035; AT-203–AT-215).
- The stable runtime bootstrap, exact build attestation, and service-definition reconciliation (#94) make the installed execution identity strong enough for production: `rly init` installs ONE RLY-owned launcher at `<control-plane>/bootstrap/rly-gateway` (0700 dir / 0755 script) that service managers reference instead of `dist/cli/init.js` or an incidental Node path; the launcher resolves ONLY the committed #92 `active` deployment (refusing staged/uncommitted/missing candidates) and executes the real `dist/cli/main.js` dispatcher, exporting `RLY_SERVING_ARTIFACT`; `rly init` establishes the initial committed deployment from the installed runtime tree idempotently; a versioned exact build identity (semantic version, commit, build ID, release channel, control/data protocol, state schema, serving artifact digest) is reported identically by `/identity`, `rly --version`, diagnostics, the release manifest, deployment metadata, and update probation — same-semantic-version-different-artifact is distinguishable and fails closed in reuse/probation; `rly init`/`rly doctor` detect and idempotently repair missing/stale/path-drifted launchd/systemd definitions, migrating legacy definitions to the bootstrap contract without provider reconfiguration (FR-032; SR-F-036; AT-216–AT-226).
- The streaming transport is incremental and long-session-safe (#120): both routes stream through per-stream incremental encoders (`src/protocols/anthropic/encoder.ts`, `src/protocols/openai-responses/encoder.ts`) that consume one canonical event at a time and emit only its new wire frames (byte-identical to the batch re-encode-then-slice output on text/reasoning/tool fixtures including #119 fidelity events); neither route retains a `seen` history to re-encode — retained protocol state is bounded by open blocks / the aggregate projection needed for the terminal `response.completed` and continuation persistence, so encoding work is linear with bounded state (FR-033; SR-F-037; AT-227–AT-236). The shared pump/lifecycle (`src/routes/stream-pump.ts`, `src/routes/stream-lifecycle.ts`) respects downstream backpressure by suspending at `yield` (idle clock disarmed while suspended, so slow consumers never cause spurious timeouts), propagates client disconnect promptly through the merged AbortSignal with no frames after close (even for upstreams that ignore abort), applies a setup (first-frame) timeout separate from an idle/progress timeout between events — never a generic whole-request timer — and terminates iterator/encoder/timers/abort listeners/response exactly once on completion, error, timeout, disconnect, and teardown. Stream diagnostics are metadata only (event/frame/backpressure counts, duration, terminal kind incl. setup/idle timeout category) and never contain prompts, responses, reasoning text, tool payloads, or credentials.

## Phase 125 executable evidence (typed model intent and selector namespaces, #125)

| Acceptance | Evidence | Status |
| --- | --- | --- |
| AT-119 | `tests/unit/model-intent.test.ts` | Verified the core invariant `fable != rly-tier:fable`: a bare `fable` classifies `CLIENT_NATIVE_ALIAS` (source `client-native-alias-contract`) and an explicit `rly-tier:fable` classifies `RLY_LOGICAL_TIER` (source `rly-tier-namespace`) with exact source-selector provenance preserved; every RLY tier (`haiku`/`sonnet`/`opus`/`fable`) parses from the `rly-tier:` namespace |
| AT-120 | `tests/unit/model-intent.test.ts` | Verified RLY logical tiers are reachable only through the explicit `rly-tier:` namespace; unknown/empty/role/exact-model `rly-tier:` values fail closed `unknown-namespace` and are never reinterpreted as an alias/exact model; projection ids (`claude-rly-*`) classify `EXACT_PROJECTION` even when they embed an alias word; bare aliases classify only through the client-alias contract |
| AT-121 | `tests/lifecycle/model-intent-route.test.ts`, `tests/unit/model-intent.test.ts` | Verified the #69 provider/family tier resolver runs only for an explicitly typed tier intent: bare `fable` and `rly-tier:fable` both resolve contextually to `gpt-5.6-sol` through the resolver, while exact model ids, `default`, and `inherit` resolve through the profile role mapping with no tier resolver invocation; classification precedence is deterministic |
| AT-122 | `tests/lifecycle/model-intent-route.test.ts`, `tests/unit/model-intent.test.ts` | Verified the legacy-compatibility path: bare tier selectors still resolve without rewriting agent/skill files (classified as client-native aliases, traceable), `profile.modelRoles` tier keys keep their #69 override meaning, and persisted exact model ids (e.g. `gpt-5.6-terra`) classify `EXACT_CLIENT_MODEL` and keep the exact #68 path — never silently reinterpreted as tiers |
| AT-123 | `tests/lifecycle/model-intent-route.test.ts` | Verified exact projected selection remains exact: projection selectors are dispatched by `resolveProjectedModelRoute` before profile resolution; an unknown `claude-rly-*` id fails closed `model-unavailable` and is never reinterpreted as a tier/alias/exact model |
| AT-124 | `tests/unit/model-intent.test.ts`, `tests/lifecycle/model-intent-route.test.ts` | Verified client-native alias support is explicit and compatibility-aware: the alias vocabulary is a pinned constant mapped through the deliberate client-alias contract with trace provenance; no user agent/skill file is rewritten to a physical provider model id (#24 still owns baseline classification) |
| AT-125 | `tests/unit/model-intent.test.ts`, `tests/lifecycle/model-intent-route.test.ts` | Verified deterministic precedence among exact projection, explicit RLY tier, client-native alias, inherit, default, and exact model; identical selectors classify identically; `default`/`inherit` resolve through the profile default with no literal role lookup on the raw string |
| AT-126 | `tests/unit/tier-resolver.test.ts`, `tests/lifecycle/cline-profile-route.test.ts`, `tests/lifecycle/model-intent-route.test.ts` | Verified aggregator same-access-provider + same-family default tier affinity is retained after intent migration: Terra-family parent + bare/`rly-tier:` `fable` → `gpt-5.6-sol` (never Anthropic/DeepSeek), with `tierResolution` trace unchanged |
| AT-127 | `tests/lifecycle/model-intent-route.test.ts`, `tests/privacy/*` | Verified route diagnostics expose selector kind/source and resolved target only (`ProfileDecisionTrace.intent`: kind, sourceSelector, source, tier/alias/modelId/role); serialized traces and upstream calls contain no prompts, credentials, account identity, or settings contents |
| AT-128 | `docs/ARCHITECTURE.md`, `docs/protocol-compatibility.md`, `docs/project-decisions.md`, `docs/requirements/*`, `docs/TASKLIST.md` | Verified architecture/model-control docs, protocol compatibility, project decisions, RTM, and the acceptance catalogue explicitly distinguish the client alias namespace from the RLY logical selector namespace (`fable != rly-tier:fable`); FR-024 / SR-F-028 added with stable IDs |

## Phase 119 executable evidence (native protocol rails, fidelity envelope, opaque continuation artifacts, #119)

| Acceptance | Evidence | Status |
| --- | --- | --- |
| AT-129 | `tests/contract/protocol-fidelity.test.ts`, `tests/fixtures/protocol/anthropic-thinking-signature.json` | Verified an Anthropic Messages request with a thinking `signature` decodes with the signature in the fidelity envelope (`anthropic-thinking-signature`, association `1:0`, marked required, `preserved-native` note) while thinking text stays a `translated` semantic projection; the required capability (`reasoning`) is still derived; an envelope is absent when no signature/unknown fields exist |
| AT-130 | `tests/contract/protocol-fidelity.test.ts`, `tests/fixtures/protocol/anthropic-signature-delta-sse.json` | Verified canonical `signature-delta` emits `signature_delta` after `thinking_delta` and before `content_block_stop`, byte-for-byte matching the golden SSE fixture; the aggregator attaches the signature to the aggregate thinking block; a signature delta on a non-thinking block or before its content block fails closed (`invalid_event_order`) |
| AT-131 | `tests/contract/protocol-fidelity.test.ts`, `tests/fixtures/protocol/openai-reasoning-encrypted.json` | Verified Responses reasoning item identity (`id`) survives on the semantic reasoning content and opaque `encrypted_content` rides the fidelity envelope (required); decode → continuation storage → `previous_response_id` request construction → re-encode round-trips both with the exact artifact association; opaque content is never reconstructed from summary text |
| AT-132 | `tests/contract/protocol-fidelity.test.ts` (fidelity diagnostics privacy) | Verified `describeFidelity()` exposes kinds/associations/dispositions/counts only and `JSON.stringify` of the summary contains neither encrypted content values nor item ids; routing decisions consume the semantic projection, never artifact values |
| AT-133 | `tests/contract/protocol-fidelity.test.ts`, `tests/fixtures/protocol/anthropic-unknown-additive.json`, `tests/fixtures/protocol/openai-unknown-additive.json` | Verified unknown top-level additive fields on both protocols record an `ignored` disposition note (field + reason) in the envelope and the existing ignored-fields list; the envelope has no required kinds and no false `preserved-native` notes |
| AT-134 | `tests/contract/protocol-fidelity.test.ts` | Verified a request carrying a required artifact (thinking signature or encrypted content) onto the Chat Completions transport (`OpenRouterAdapter`) fails with `unsupported-fidelity` before any upstream call (`fetch` not invoked); no fabrication/reconstruction; a request without required artifacts routes normally |
| AT-135 | `tests/contract/protocol-fidelity.test.ts` | Verified the envelope records exactly one disposition per wire-significant field/artifact across the supported paths: `preserved-native` (signature, encrypted content), `translated` (thinking/summary text), `ignored` (unknown additive), and `unsupported` (required artifact on an incapable path via `unsupported-fidelity`) |
| AT-136 | `tests/privacy/protocol-fidelity.test.ts`, `tests/contract/protocol-fidelity.test.ts`, `pnpm test:privacy` | Verified route traces/diagnostics/logs/privacy snapshots carry provenance metadata only — never signature or encrypted-content values; the observability redactor treats `signature`/`value` keys as sensitive; the privacy scan (377 files) passes |
| AT-137 | `tests/contract/protocol-fidelity.test.ts`, `tests/fixtures/protocol/openai-reasoning-tool-interleave.json` | Verified multi-turn Responses continuation interleaving reasoning (identity + encrypted content), text, function calls, and outputs keeps each opaque artifact attached to its own reasoning item id (`rs_turn1`, `rs_turn2`) across decode → storage → continued request → re-encode; tool call ids stay distinct; nothing merged/reordered |
| AT-138 | `docs/protocol-compatibility.md`, `docs/ARCHITECTURE.md`, `docs/project-decisions.md`, `docs/requirements/*`, `docs/TASKLIST.md` | Verified protocol compatibility, architecture, project decisions, RTM, and the acceptance catalogue identify the native rail, the semantic projection, and the fidelity envelope as separate authorities; FR-025 / SR-F-029 / AT-129–AT-138 added with non-colliding IDs (checked against merged dev max FR-024 / SR-F-028 / AT-128) |

## Phase 126 executable evidence (profile-scoped Claude views and env/settings ownership, #126)

| Acceptance | Evidence | Status |
| --- | --- | --- |
| AT-150 | `tests/lifecycle/claude-overlay.test.ts` | Verified typed ownership classification (`classifySettingsKey`/`classifySettingsEnvKey`) and the `settingsOwnershipSummary` (counts + conflicting/unsupported/gateway key names only); deterministic precedence in `composeOverlaySettings` (child-only contract > RLY-owned persisted projection > explicit RLY/profile settings > native > client persistence > defaults); unsupported credential-bearing shapes (`oauthAccounts`) are never composed; explicit launch-policy `model` sits above native input but below a persisted `claude-rly-*` projection on re-compose |
| AT-151 | `tests/lifecycle/claude-overlay.test.ts`, `tests/unit/agent-context.test.ts` | Verified `deriveClaudeViewId` is deterministic/collision-safe and never yields the reserved `default` view; two profile views are distinct durable directories; a `claude-rly-*` model persisted into profile A's view never becomes profile B's default; `LaunchSession.viewId` derives from the immutable profile id |
| AT-152 | `tests/lifecycle/claude-overlay.test.ts`, `tests/lifecycle/global-config-isolation.test.ts`, `tests/e2e/claude-code/overlay.e2e.test.ts` | Verified plain-Claude isolation: view settings contain no RLY gateway env/auth/projection ids a plain launch could inherit; native `~/.claude` and home-level `~/.claude.json` sentinels stay byte-identical across prepares and simulated `/model` activity (black-box sentinel); gated real-Claude E2E (`RLY_CLAUDE_E2E=1`, skipped ≠ pass) |
| AT-153 | `tests/lifecycle/claude-overlay.test.ts` | Verified the `.rly-manifest.json` distinguishes native-imported (source-relative path + source/view sha256 + imported key metadata), RLY-generated (settings recomposed after native removal), and view-owned (divergent copies) entries; manifest is metadata/hashes only — never credential values, prompts, or settings content; gateway-contract/unsupported key names are excluded from `settingsSourceKeys` |
| AT-154 | `tests/lifecycle/claude-overlay.test.ts` | Verified deletion/rename reconciliation: deleting a native agent removes its owned import on the next prepare (matching imported hash) while unrelated imports survive; a view copy edited by Claude is reclassified view-owned and kept, never deleted; the native-source-exists guard prevents deleting live imports; RLY never deletes files it does not own as imports |
| AT-155 | `tests/lifecycle/claude-overlay.test.ts` | Verified concurrent prepares (same view ×3, and two profile views concurrently) converge to consistent views with atomic temp+rename writes and the bounded per-view reconcile lock; native settings hash unchanged; no copy-back/restore of native settings anywhere |
| AT-156 | `tests/lifecycle/claude-overlay.test.ts` | Verified a persisted RLY projection model is retained for its owning profile view only across re-prepares; another profile and plain `claude` never inherit it; native model input stays native |
| AT-157 | `tests/lifecycle/claude-overlay.test.ts` | Verified native allowlisted changes re-compose per precedence: newer native mtime triggers recompose while RLY-owned projection state wins over the native `model`; ownership beats naive mtime (a view-owned/RLY-owned value is never overwritten by a newer native timestamp alone); unchanged native input is not rewritten |
| AT-158 | `tests/lifecycle/claude-overlay.test.ts` | Verified deterministic shared-overlay migration into `views/default`: the legacy `<control-plane>/claude` overlay (v2 marker) moves via a crash-safe two-phase sibling rename; native `~/.claude`/`~/.claude.json` byte-identical; ambiguous shared RLY state stays in the unprofiled `default` view; the view container is never re-migrated once profile views exist |
| AT-159 | `tests/lifecycle/claude-overlay.test.ts`, `tests/unit/cli-diagnostics.test.ts`, `tests/lifecycle/global-config-isolation.test.ts`, `pnpm test:privacy` | Verified private view/manifest/marker permissions (`0700`/`0600`); no credential, auth token, settings content, prompt, or account identity persisted into views/manifests; `rly status`/`rly doctor` expose `claudeViews` metadata (view id/path, allowlist version, ownership counts, reconciled deletions/reclassifications, conflicting key categories, last refresh) only; privacy scan passes |

## Phase 122 executable evidence (Compatibility Claim and Evidence v2, #122)

| Acceptance | Evidence | Status |
| --- | --- | --- |
| AT-169 / FR-028 / SR-F-032 | `tests/unit/canary-claim.test.ts`, `src/canary/claim.ts` | Verified stable versioned claim keys from the exact execution path (client, exact client version/baseline, source protocol/revision, adapter, access provider, auth mode, endpoint contract, physical model, feature); model family is metadata only and never part of the key; identical inputs → identical keys; `claimKeyHash` deterministic |
| AT-170 / FR-028 / SR-F-032 | `tests/unit/canary-claim.test.ts`, `tests/unit/canary-evidence.test.ts` | Verified same upstream model through two providers (cline/gpt-5.6-sol vs codex/gpt-5.6-sol) produces distinct claim keys/documents/evidence histories; exact `findEvidence` never returns another provider's claim; deterministic run produces identical claims across runs |
| AT-171 / FR-028 / SR-F-032 | `tests/unit/canary-claim.test.ts`, `tests/unit/canary-matrix.test.ts` | Verified per-feature claim independence on the same path: text vs reasoning vs tools-parallel vs config-overlay are distinct claims; capability-gated features stay `not-run`/`missing` (`no-parallel-tool-evidence`, `no-reasoning-with-tools-evidence`) never passed; config-overlay lookup is `missing` until observed |
| AT-172 / FR-028 / SR-F-032 | `tests/unit/canary-evidence.test.ts`, `src/canary/classify.ts` | Verified `livePassed`/`liveEvidence` removed from the classification input; a full deterministic pass classifies `EXPERIMENTAL` with `production-claim-not-established`; no observation path (any gate combination, any runner switch) emits `VERIFIED` |
| AT-173 / FR-028 / SR-F-032 | `tests/unit/canary-evidence.test.ts`, `tests/unit/cli-canary.test.ts` | Verified `RLY_LIVE_CANARY`/`liveRunnerEnabled` only reports `enabled` and emits no Layer B/C evidence and no VERIFIED verdict (`liveRunner.evidenceEmitted` false); the CLI run persists Layer A only |
| AT-174 / FR-028 / SR-F-032 | `tests/unit/canary-evidence.test.ts`, `tests/unit/canary-claim.test.ts`, `tests/unit/cli-canary.test.ts` | Verified the deterministic matrix emits per-feature Evidence Artifact v2 records (`evidenceSchemaVersion` 2, layer A, kind `deterministic-fake-matrix`, runner `rly-canary-runner/2.0`, fixture revision, typed failure reasons) and appends claim docs under `<control-plane>/claims/`; `rly canary status` reports `claimEvidence` schema/legacy policy |
| AT-175 / FR-028 / SR-F-032 | `tests/unit/canary-claim.test.ts` | Verified claim status derivation: no observations → `missing`; required layer without passing record → `not-run`; any failed observation → `failed`; all required layers (A/B/C per adapter, unknown adapters fail closed) passing → `passed`; `layerStatuses` exposes explicit per-layer presence/result |
| AT-176 / FR-028 / SR-F-032 | `tests/privacy/canary.test.ts`, `tests/unit/canary-claim.test.ts`, `pnpm test:privacy` | Verified claim/evidence docs carry claim keys, layer/kind, gate names, results, fixture revision, runner version, environment, typed failure reasons only — never credentials, auth headers, account identity, prompts, responses, or reasoning text; `0700` claims dir / `0600` files; privacy scan green |
| AT-177 / FR-028 / SR-F-032 | `tests/unit/canary-claim.test.ts`, `tests/unit/model-registry.test.ts` | Verified legacy v1 canary artifacts flagged legacy/untrusted (`legacy-v1-artifact-untrusted-for-v2-claims`), never satisfy a v2 claim (lookup stays `missing`); registry revision bumped 4→5 with optional `claimRef`; pre-v5 rows (incl. migrated legacy rows) carry no claim reference and are untrusted for v2 claim authority |
| AT-178 / FR-028 / SR-F-032 | `tests/unit/canary-claim.test.ts`, `tests/unit/canary-evidence.test.ts` | Verified key determinism, same-model-two-provider separation, feature independence, Layer A/B/C status derivation, Zod schema round-trip + fail-closed reads, append/audit persistence with exact-duplicate dedupe, deterministic lookup by claim identity + feature, and no cross-provider/model/feature reuse |
| AT-179 / FR-028 / SR-F-032 | `docs/protocol-compatibility.md`, `docs/ARCHITECTURE.md`, `docs/project-decisions.md`, `docs/SPEC.md`, `docs/requirements/*`, `docs/TASKLIST.md` | Verified compatibility docs, architecture, project decisions, SPEC §6.10, FRS/SRS, RTM, and the acceptance catalogue define the versioned claim identity, feature scoping, evidence layers A/B/C, removal of `livePassed` trust semantics, and the legacy v1 policy; FR-028 / SR-F-032 / AT-169–AT-179 added with non-colliding IDs (checked against final merged dev max FR-027 / SR-F-031 / AT-168 from #93/#126) |

## Phase 123 executable evidence (installed-client Layer B and live access-path Layer C runners, #123)

| Acceptance | Evidence | Status |
| --- | --- | --- |
| AT-180 / FR-029 / SR-F-033 | `src/canary/installed-runner.ts`, `tests/unit/canary-runners.test.ts`, `tests/e2e/claude-code/blackbox-runner.e2e.test.ts` | Verified the Layer B runner detects the installed Claude Code binary, records the exact probe version and executable path separately from the reviewed baseline (`observedVersion` ≠ `supportedBaseline`, claim keyed to observed), launches the real binary against the controlled local Anthropic Messages + `/v1/models` fixture with child-only env, and emits per-feature Evidence v2 (layer B, `installed-client`, runner version, fixture revision, timing); real-binary sentinel `RLY_CLAUDE_E2E=1 pnpm vitest run tests/e2e/claude-code/blackbox-runner.e2e.test.ts` passes on installed claude 2.1.233 (text/streaming/cancellation/tools-single/model-discovery/session-attribution/long-running-session/config-overlay all passed; ~20 s) |
| AT-181 / FR-029 / SR-F-033 | `src/canary/installed-runner.ts`, `tests/unit/canary-runners.test.ts`, `tests/e2e/codex/blackbox-runner.e2e.test.ts` | Verified the Layer B runner launches the real `codex` binary against the controlled OpenAI Responses fixture and emits typed, version-keyed Evidence v2; deterministic fake-codex matrix passes all gates; real-binary sentinel `RLY_CODEX_E2E=1 pnpm vitest run tests/e2e/codex/blackbox-runner.e2e.test.ts` records typed drift outcomes for codex 0.147.0 (hardcoded OpenAI websocket transport, ignores `OPENAI_BASE_URL`) — never a silent pass, never a supported baseline |
| AT-182 / FR-029 / SR-F-033 | `src/canary/installed-runner.ts`, `tests/unit/canary-runners.test.ts` | Verified the Claude black-box matrix covers request/stream framing, cancellation, tool round-trip, multi-tool continuation, parallel tools, reasoning/effort, reasoning+tools, discovery/selection, session/agent/parent attribution, tier aliases, subagent concurrency, config-overlay, and long sessions (15 gates); capability-dependent gates stay `not-run` with typed reasons when the client sends no such config |
| AT-183 / FR-029 / SR-F-033 | `src/canary/live-runner.ts`, `tests/unit/canary-runners.test.ts` | Verified `rly canary run-c` executes an exact configured access path through the real gateway translation stack (Anthropic Messages surface → direct adapter → provider endpoint) with explicit opt-in and an env credential, emitting per-feature Evidence v2 (layer C, `live-access-path`, timing) — text/streaming/cancellation/tools-single/tools-multi/tools-parallel/reasoning/reasoning-tools/model-discovery/session-attribution/effort-signal each carry their own record; policy-driven `GET /v1/models` discovery exercised on the exact-path gateway with the explicit EXPERIMENTAL opt-in; tool continuation proven only when the provider returns tool calls |
| AT-184 / FR-029 / SR-F-033 | `src/canary/live-runner.ts`, `tests/unit/canary-runners.test.ts` | Verified the same upstream model (deepseek-v4-pro) through openrouter vs deepseek produces distinct claim keys and evidence histories; a provider's evidence never appears in the other's claim lookup |
| AT-185 / FR-029 / SR-F-033 | `src/canary/live-runner.ts`, `src/canary/installed-runner.ts`, `tests/unit/canary-runners.test.ts`, `tests/unit/cli-canary.test.ts` | Verified missing credentials emit `not-run`/`authentication-credentials-unavailable` for every gate with zero upstream calls; a missing/non-executable binary emits `not-run`/`client-not-installed`; `rly canary run-c` refuses without `RLY_LIVE_CANARY=1`; skipped E2E sentinels stay skipped (skipped ≠ pass); no `passed` record exists for any unrun gate |
| AT-186 / FR-029 / SR-F-033 | `src/canary/installed-runner.ts`, `tests/unit/canary-runners.test.ts` | Verified a deliberately drifted fixture (dropped session attribution headers) fails the exact `session-attribution` gate with `missing-agent-header` on the Evidence v2 record keyed to that observed client version (`9.9.9-drift`); the typed taxonomy covers client-contract-drift, missing-agent-header, effort-signal-lost, malformed-continuation, timeout-cancel-failure, and more |
| AT-187 / FR-029 / SR-F-033 | `src/canary/installed-runner.ts`, `tests/unit/canary-runners.test.ts` | Verified Layer B evidence is keyed to the exact observed version and the supported baseline is recorded separately (`observedVersion` 2.1.231 vs baseline 2.1.229); the observed-version claim document cannot reach `passed` (Layer A/C required plus reviewed promotion) — new versions are tested without becoming supported baselines |
| AT-188 / FR-029 / SR-F-033 | `tests/unit/canary-runners.test.ts`, `tests/privacy/*`, `src/canary/runner-store.ts`, `src/canary/claim.ts` | Verified runner raw results and Evidence v2 records carry gate names, results, typed reasons, timing, and wire booleans only — never credentials, auth headers, account identity, prompts, responses, or reasoning text; `assertSecretFree` and the privacy scan pass; runner results persist private (`0700`/`0600`) under `<control-plane>/canary-runners/`; Evidence v2 adds optional additive `timingMs` (schema/round-trip compatible with #122 artifacts) |
| AT-189 / FR-029 / SR-F-033 | `src/canary/installed-runner.ts`, `src/canary/live-runner.ts`, `src/canary/claim.ts` | Verified runner runs are observation-only: they never write the trusted registry, tier mappings, or `/v1/models` projection; failures/timeouts/not-run gates leave trusted compatibility state byte-identical; Layer C uses a throwaway control-plane temp store never touching the real control plane |
| AT-190 / FR-029 / SR-F-033 | `docs/protocol-compatibility.md`, `docs/ARCHITECTURE.md`, `docs/SPEC.md`, `docs/TASKLIST.md`, `.github/workflows/ci.yml` | Verified CI/manual docs separate deterministic Layer A (`rly canary run`), installed-client Layer B (`rly canary run-b`; real-binary sentinels opt-in via `RLY_CLAUDE_E2E=1`/`RLY_CODEX_E2E=1`, skipped ≠ pass), and real-path Layer C (`rly canary run-c`, `RLY_LIVE_CANARY=1` opt-in, credential-gated); FR-029 / SR-F-033 / AT-180–AT-190 added with non-colliding IDs (checked against merged dev max FR-028 / SR-F-032 / AT-179 from #122/#93/#126) |

## Phase 124 executable evidence (Effective Compatibility Registry, #124)

| Acceptance | Evidence | Status |
| --- | --- | --- |
| AT-191 / FR-030 / SR-F-034 | `tests/unit/compat-effective.test.ts`, `tests/unit/compat-registry.test.ts`, `tests/unit/compat-routing.test.ts` | Verified PASS evidence alone never produces reviewed trust (claim with full A/B/C passing + no promote decision resolves `experimental`, trust `none`, enforcement blocked); an explicit promote decision tied to the exact evidence revision resolves `trusted`; decision metadata (reviewer/source/decidedAt/revision) carried in the effective result |
| AT-192 / FR-030 / SR-F-034 | `tests/unit/compat-registry.test.ts`, `tests/unit/compat-stores.test.ts`, `tests/lifecycle/ecr-route.test.ts` | Verified quarantining one exact `(provider, model, feature)` claim resolves it `quarantined` (enforcement blocked, `quarantined-fail-closed`) while the same provider's other features and another provider's same-feature claim stay untouched (quarantine `none`); lift restores evaluation; quarantine never deletes historical claim evidence; gateway lifecycle fails closed with no silent fallback |
| AT-193 / FR-030 / SR-F-034 | `tests/unit/compat-freshness.test.ts`, `tests/unit/compat-effective.test.ts`, `tests/unit/compat-registry.test.ts` | Verified staleness (distinct reason each) on client version/baseline drift, protocol revision drift, adapter/auth-mode/endpoint drift, model fingerprint change, fixture revision drift, material RLY build change, and evidence age; matching dependencies stay fresh; no fabricated staleness without a configured dependency |
| AT-194 / FR-030 / SR-F-034 | `tests/unit/compat-effective.test.ts` | Verified effective results expose trust, health, freshness, quarantine, enforcement as distinct fields + per-layer evidence status (A/B/C); never a single persisted boolean; deterministic label precedence (quarantine → missing → failed → rejected → review-stale → trusted/stale/experimental) |
| AT-195 / FR-030 / SR-F-034 | `src/routing/model-selection/selector.ts`, `src/routing/model-tiers/resolver.ts`, `src/profiles/resolve-route.ts`, `tests/unit/compat-routing.test.ts`, `tests/lifecycle/ecr-route.test.ts` | Verified with an ECR snapshot the selector's compatibility pass is ECR-driven (trace `authority: "ecr"`), enforcement recomputed per request context, and static `model.compatibility.state` is seed/reference only (seed path used solely when no snapshot is supplied for tooling); runtime profile/projection paths always consume the ECR |
| AT-196 / FR-030 / SR-F-034 | `src/routing/model-projection/project.ts`, `src/routes/anthropic-models-route.ts`, `tests/unit/compat-routing.test.ts` | Verified `projectModelUniverse`/models route project only effectively-trusted paths by default; experimental/stale evidence-backed paths appear only with the explicit opt-in; quarantined/failed/missing paths never appear (quarantine excludes even with opt-in); same-model-different-provider projections stay isolated |
| AT-197 / FR-030 / SR-F-034 | `src/routing/model-tiers/resolver.ts`, `src/routing/model-selection/selector.ts`, `tests/unit/compat-routing.test.ts` | Verified tier resolution and reasoning/tool eligibility run their #68 selections through the ECR snapshot; a text-only PASS never implies tool/reasoning/discovery compatibility (each required feature gates independently); a quarantined/blocked required feature fails the tier/selection closed |
| AT-198 / FR-030 / SR-F-034 | `src/compatibility/effective.ts`, `src/routing/model-selection/selector.ts`, `tests/unit/compat-effective.test.ts`, `tests/lifecycle/ecr-route.test.ts` | Verified the exact-pin/`allowExperimental` override is explicit and visible (`experimental-override` + `explicit-experimental-override` in route trace/doctor); override can never elevate `untrusted`/`missing`; quarantine requires the separately documented `allowQuarantineBypass` policy and the bypass is visible (`quarantine-bypass` + `admin-quarantine-bypass`) |
| AT-199 / FR-030 / SR-F-034 | `src/compatibility/policy.ts`, `src/compatibility/registry.ts`, `tests/unit/compat-registry.test.ts`, `tests/unit/compat-routing.test.ts` | Verified legacy static states migrate under the `seed-reference-only` policy: a static `VERIFIED` row without a `claimRef` derives `experimental` at best (enforcement blocked for required features) and can never silently become reviewed v2 trust; `BROKEN` stays a hard negative seed; reviewed claims resolve through the ECR independently |
| AT-200 / FR-030 / SR-F-034 | `src/cli/compat.ts`, `src/cli/diagnostics.ts`, `src/compatibility/registry.ts`, `tests/privacy/compat.test.ts`, `tests/unit/cli-compat.test.ts` | Verified `rly compat explain|status`, doctor `effectiveCompatibility`, and store records explain/record claim identity, decision metadata, layers, health, freshness reason, quarantine reason, enforcement reason — reviewer/source/reason/timestamp/revision only; `assertSecretFree` + privacy scan green (408 files) |
| AT-201 / FR-030 / SR-F-034 | `tests/unit/compat-effective.test.ts`, `tests/unit/compat-freshness.test.ts`, `tests/unit/compat-stores.test.ts`, `tests/unit/compat-registry.test.ts`, `tests/unit/compat-routing.test.ts`, `tests/lifecycle/ecr-route.test.ts` | Verified review promotion; evidence update without promotion (review-stale, re-review required); quarantine precedence over decisions; staleness (build/version/fixture/auth-mode/fingerprint/age); version drift; provider-path isolation; feature isolation; and runtime-consumer integration (selector/tier/projection/resolve-route + gateway lifecycle) |
| AT-202 / FR-030 / SR-F-034 | `docs/ARCHITECTURE.md`, `docs/protocol-compatibility.md`, `docs/project-decisions.md`, `docs/SPEC.md`, `docs/requirements/*`, `docs/TASKLIST.md` | Verified ARCHITECTURE, protocol-compatibility, project-decisions, SPEC, FRS/SRS, RTM, and this catalogue identify the Effective Compatibility Registry as the SOLE runtime compatibility authority; FR-030 / SR-F-034 / AT-191–AT-202 added with non-colliding IDs (checked against merged dev max FR-029 / SR-F-033 / AT-190 from #123) |

## Phase 127 executable evidence (EffectiveModelDecision control plane, #127)

| Acceptance | Evidence | Status |
| --- | --- | --- |
| AT-203 / FR-031 / SR-F-035 | `src/routing/model-decision/types.ts`, `src/profiles/resolve-route.ts`, `tests/lifecycle/model-decision-route.test.ts`, `tests/unit/model-decision.test.ts` | Verified ONE typed `EffectiveModelDecision` is assembled before account selection for profile and projected routes (decision rides `ProfileDecisionTrace.effectiveModelDecision`); no second registry/tier/selector/compatibility authority introduced — the assembler only records the #68/#69/#70/#71/#72/#124/#125/#126 stage outputs |
| AT-204 / FR-031 / SR-F-035 | `tests/unit/model-decision.test.ts`, `tests/lifecycle/model-decision-route.test.ts`, `tests/unit/cli-diagnostics.test.ts` | Verified the decision carries #125 intent/provenance, frozen target (provider/model/family/logicalId/adapter), #124 ECR authority result, #70 reasoning mapping, policy/profile/session/registry/mapping revisions, and #72 pool binding — with no account/credential identity anywhere (unit asserts on the object; CLI route-trace asserts the surface) |
| AT-205 / FR-031 / SR-F-035 | `src/routing/model-decision/assemble.ts`, `tests/unit/model-decision.test.ts`, `tests/lifecycle/model-decision-route.test.ts` | Verified with the ECR wired the decision reports `authority: "ecr"` with effective label + enforcement reason recomputed per request (e.g. `experimental` + `explicit-experimental-override` for an exact pin on an unreviewed path); `seedState` is recorded as seed/reference only |
| AT-206 / FR-031 / SR-F-035 | `src/routing/model-decision/assemble.ts`, `tests/unit/model-decision.test.ts` | Verified the documented precedence order is applied (explicit exact → RLY tier → alias → inherit/override → profile policy → env/settings ownership → persisted view → defaults), the winner is derived per intent kind (incl. `persisted-rly-view` when the selector equals the owning view's persisted projection), and conflicts (persisted-view vs request, launch-policy vs request, subagent vs parent, foreign projection, gateway env keys) are visible, deterministic, and never silently re-route |
| AT-207 / FR-031 / SR-F-035 | `src/routing/model-decision/assemble.ts` (`readPersistedViewModel`), `tests/unit/model-decision.test.ts`, `tests/lifecycle/model-decision-route.test.ts` | Verified persisted state is read ONLY from the owning profile view (derived view id); another profile's persisted `claude-rly-*` model never appears in this profile's decision (not even as routing metadata); non-RLY/missing view state contributes no persisted input |
| AT-208 / FR-031 / SR-F-035 | `src/profiles/resolve-route.ts`, `tests/lifecycle/model-decision-route.test.ts` | Verified freeze-before-account: the decision is assembled before the pool/account `RouteSelector`; with two pool accounts where the first fails pre-output, retry rotates to the second and every attempt's decision carries the identical physical target/reasoning/pool; no pseudonym/credential generation in the decision |
| AT-209 / FR-031 / SR-F-035 | `src/profiles/resolve-route.ts`, `src/routing/model-decision/assemble.ts`, `tests/lifecycle/model-decision-route.test.ts` | Verified subagent isolation: a child `fable` request inherits the parent's frozen family context (provenance `inherit` + `parent-agent`), the child intent wins with a visible `subagent-request-vs-parent-context` conflict when it differs, and the parent/main execution context stays on its model (re-request unchanged) |
| AT-210 / FR-031 / SR-F-035 | `src/profiles/resolve-route.ts`, `src/routing/model-decision/assemble.ts`, `tests/lifecycle/model-decision-route.test.ts`, `tests/unit/model-decision.test.ts` | Verified stale/foreign persisted state is rejected or explained: an unknown/removed projection id fails closed `model-unavailable` at the session-universe boundary (never silently remapped); a selector differing from the owning view's persisted model records `projection-vs-view-state`/`persisted-view-model-vs-request` and resolves through the pinned universe/policy only |
| AT-211 / FR-031 / SR-F-035 | `src/profiles/resolve-route.ts`, `tests/unit/cli-diagnostics.test.ts` | Verified the Codex profile path produces the same decision vocabulary (intent/target/ECR authority/pool binding on the route trace) without Claude-only alias semantics; client-specific gating remains #124 evidence-driven |
| AT-212 / FR-031 / SR-F-035 | `src/routing/model-decision/describe.ts`, `src/cli/diagnostics.ts`, `tests/unit/model-decision.test.ts`, `tests/unit/cli-diagnostics.test.ts` | Verified `rly route-trace` exposes a secret-free `effectiveModelDecision` per trace (selector/intent/precedence/target/provenance/reasoning/compatibility/pool/revisions/reasons/blocked alternatives) and `rly doctor` reports the model-control contract (schema version, precedence order, ECR final authority); blocked alternatives carry typed reasons (capability-unsupported/reasoning-unsupported/compatibility-rejected) |
| AT-213 / FR-031 / SR-F-035 | `tests/privacy/model-decision.test.ts`, `src/control-plane/secret-free.ts`, `tests/lifecycle/model-decision-route.test.ts` | Verified decision + describe pass `assertSecretFree` and contain no prompts, responses, reasoning text, credentials, auth headers, raw account identity, or settings contents (env/settings ownership = counts + key names only); full trace stays secret-free; `pnpm test:privacy` green (414 files) |
| AT-214 / FR-031 / SR-F-035 | existing #68/#69/#70/#71/#72/#124/pool suites | Verified existing model selection, tier resolution, reasoning, subagent, projection/discovery, ECR-consumer, and account-routing regression suites stay green after the decision wiring (unit + lifecycle suites pass; pre-existing env-dependent `project-board-automation` failure also present on origin/dev) |
| AT-215 / FR-031 / SR-F-035 | `docs/ARCHITECTURE.md`, `docs/protocol-compatibility.md`, `docs/project-decisions.md`, `docs/SPEC.md`, `docs/requirements/*`, `docs/TASKLIST.md` | Verified ARCHITECTURE, protocol-compatibility, project-decisions, SPEC, FRS/SRS, RTM, and this catalogue identify `EffectiveModelDecision` as the FINAL model-control output before account selection; FR-031 / SR-F-035 / AT-203–AT-215 added with non-colliding IDs (checked against merged dev max FR-030 / SR-F-034 / AT-202 from #124) |

## Phase 94 executable evidence (stable bootstrap, exact build attestation, service reconciliation, #94)

| Acceptance | Evidence | Status |
| --- | --- | --- |
| AT-216 / FR-032 / SR-F-036 | `tests/service-manager/definitions.test.ts`, `tests/service-manager/adapters.test.ts`, `tests/unit/cli-init.test.ts`, `tests/unit/cli-update.test.ts`, `tests/service-manager/launch-agent-live.test.ts`, `tests/service-manager/systemd-user-live.test.ts` | Verified the stable bootstrap contract: launchd plist / systemd unit ExecStart reference ONLY `<control-plane>/bootstrap/rly-gateway` plus `gateway start --config <abs-config>` (no `dist/cli/init.js`, no Node executable path, no `runtime/refs/...` path); `rly init` registers against the bootstrap and records `bootstrapPath` + `definitionRevision`; `rly update` keeps the definition on the bootstrap; opt-in live smokes register the bootstrap script as the sole executable (skipped ≠ pass) |
| AT-217 / FR-032 / SR-F-036 | `tests/unit/bootstrap.test.ts` | Verified bootstrap resolution: `resolveActiveDeployment` resolves the committed `refs/active` deployment (validated real 0700 dir, matching `.rly-deployment.json` identity, real `dist/cli/main.js`) and REFUSES staged-only, missing, non-immutable, and mismatched-metadata candidates (`BootstrapResolutionError`); the POSIX launcher is self-locating, refuses with exit 78 when no committed active deployment exists, exports `RLY_SERVING_ARTIFACT`, and execs the real dispatcher with the gateway start arguments (real `/bin/sh` + node execution test); `materializeRuntimeTree` dereferences symlinks (pnpm-style) with an allowlist; `ensureInitialActiveDeployment` establishes the initial committed deployment idempotently |
| AT-218 / FR-032 / SR-F-036 | `tests/unit/build-identity.test.ts`, `tests/lifecycle/gateway-server.test.ts` | Verified the versioned exact build identity (semantic version, commit revision, build ID, release channel, control/data protocol versions, state schema version, artifact digest): schema round-trip + fail-closed reads, deterministic digest, `sameSemanticVersionDifferentArtifact` vs `exactIdentityMatch` (a digest-bearing identity never exact-matches one without), dev fallback, generated `rly-build.json` loading, and `/identity` carrying `build` |
| AT-219 / FR-032 / SR-F-036 | `tests/lifecycle/bootstrap-attestation.test.ts`, `tests/unit/cli-main.test.ts`, `tests/unit/cli-diagnostics.test.ts` | Verified the SAME build-identity fields on `/identity.build`, `rly --version`, doctor/status (`update.buildIdentity`/`bootstrap`), deployment metadata, and the candidate manifest; update probation requires the serving artifact digest to equal the candidate's immutable deployment identity when the serving runtime identifies its artifact (exact-match commits; same-semver-different-artifact rolls back) |
| AT-220 / FR-032 / SR-F-036 | `tests/lifecycle/bootstrap-attestation.test.ts`, `tests/lifecycle/gateway-lifecycle.test.ts` | Verified attestation fail-closed: same-artifact reuse works; a same-semantic-version-different-artifact ownership record (fingerprint = digest of another artifact's build) resolves `attested-incompatible` and is never reused with the actionable message; the foreign-listener gate still fails closed and the process is never signaled; the identity challenge/proof contract is unchanged |
| AT-221 / FR-032 / SR-F-036 | `tests/unit/service-definition-reconcile.test.ts`, `tests/unit/cli-init.test.ts`, `tests/unit/cli-diagnostics.test.ts` | Verified idempotent reconciliation: missing/stale/path-drifted definitions are detected (rendered-content comparison) and repaired through the platform adapter without duplication or provider reconfiguration; re-init with a correct definition is a filesystem no-op (`calls.registered` unchanged); `rly status` reports read-only reconciliation state; `rly doctor` repairs and reports `repaired`; legacy `dist/cli/init.js`/Node/`runtime/refs/...` definitions migrate to the bootstrap contract (`migrated: true`); unsupported platforms stay explicit |
| AT-222 / FR-032 / SR-F-036 | `tests/lifecycle/bootstrap-attestation.test.ts`, `tests/lifecycle/update-transaction.test.ts`, `tests/lifecycle/runtime-update.test.ts` | Verified crash-window safety: definition-write/reload, active-ref switch before restart, restart booting the committed candidate, and rollback restoring the known-good ref — at every boundary the bootstrap resolves ONLY the committed `refs/active` deployment and the definition never encodes a deployment path; a deleted/staged target is refused; existing #93 real-ref crash windows stay green |
| AT-223 / FR-032 / SR-F-036 | `tests/unit/cli-init.test.ts`, `tests/unit/service-definition-reconcile.test.ts` | Verified legacy service definitions migrate to the bootstrap contract safely on the supported per-user paths (macOS LaunchAgent / Linux systemd user rendered content), no-root semantics preserved, unsupported platform behavior explicit (`unsupported` status, never false success) |
| AT-224 / FR-032 / SR-F-036 | `tests/privacy/bootstrap.test.ts`, `tests/privacy/*`, `pnpm test:privacy` | Verified service definitions, the bootstrap script, deployment metadata, `/identity.build`, `rly --version`, and doctor/status bootstrap/build-identity output are secret-free (no credentials, auth headers, tokens, account identity, prompts, responses, reasoning text); privacy scan + `assertSecretFree` green |
| AT-225 / FR-032 / SR-F-036 | `tests/service-manager/launch-agent-live.test.ts`, `tests/service-manager/systemd-user-live.test.ts` | Platform smoke tests (opt-in `RLY_LIVE_LAUNCHAGENT_SMOKE=1` / `RLY_LIVE_SYSTEMD_SMOKE=1`) prove service bootstrap → running state → gateway start contract received → restart → stop → unregister with the bootstrap script as the sole executable; skipped ≠ pass |
| AT-226 / FR-032 / SR-F-036 | `docs/ARCHITECTURE.md`, `docs/project-decisions.md`, `docs/SPEC.md`, `docs/requirements/*`, `docs/TASKLIST.md` | Verified ARCHITECTURE, project-decisions, SPEC §6.7, FRS/SRS, RTM, and this catalogue describe the RLY-owned bootstrap contract, the exact build identity and shared comparison, the extended attestation, and idempotent definition reconciliation; FR-032 / SR-F-036 / AT-216–AT-226 added with non-colliding IDs (checked against merged dev max FR-031 / SR-F-035 / AT-215 from #127) |

## Phase 23 executable evidence (propose-only catalog refresh, #23 / BL-042)

| Acceptance | Evidence | Status |
| --- | --- | --- |
| AT-038 | `tests/unit/catalog-proposal.test.ts`, `tests/contract/providers/catalog-discovery.test.ts`, `tests/unit/proposal-store.test.ts`, `tests/unit/cli-admin.test.ts` | Verified deterministic drift engine (`proposeCatalogDrift`) with stable empty proposal on identical inputs; per-kind drift entries (new/removed/family/reasoning/limits/declared capability); cross-provider upstream-id separation; 250-model aggregator snapshot stays deterministic and activates nothing; fail-closed on mixed-provider and duplicate snapshots; OpenRouter API discovery (GET /models) with explicit normalization rules and optional env-ref auth; static reviewed-path source; schema-validated artifact persistence separate from trusted evidence; `rly admin models refresh|proposals` surfaces proposals with `trusted: false` and never mutates `directProviderRegistry` |
| AT-039 | `tests/contract/providers/catalog-discovery.test.ts`, `tests/unit/proposal-store.test.ts`, `tests/unit/catalog-proposal.test.ts` | Verified privacy-redacted upstream errors (bearer tokens / credential pairs / email stripped), secret-free report/artifact walks, and fail-closed reads of malformed artifacts |
| SR-F-004 / FR-002 capability evidence (unchanged) | `tests/control-plane/repositories.test.ts`, `tests/management/mutations.test.ts` | Verified unchanged after the proposal pipeline; refresh never writes control-plane state |
| Existing direct-route fail-closed behavior | `tests/contract/providers/direct-adapters.test.ts`, `tests/unit/model-registry.test.ts` | Verified unchanged: exact `(provider, model)` evidence only; unknown models still get no route |

## Phase 67 executable evidence (model foundation, #67)

| Acceptance | Evidence | Status |
| --- | --- | --- |
| AT-034 | `tests/unit/model-registry.test.ts` | Verified canonical identity (access provider/upstream id/family), same-upstream-id-across-providers separation with fail-closed cross-provider lookup, aggregator multi-family projection, typed compatibility state separate from raw capability support, deterministic provider/family/capability/compatibility queries, registry revision bump (3→4), static legacy document migration, discovery→proposal non-mutation (#23), secret-free evidence |
| SR-F-004 / FR-002 capability evidence | `tests/control-plane/repositories.test.ts`, `tests/management/mutations.test.ts` | Verified typed `ProviderCapabilityEvidence` replaces `unknown`; management boundary validates the registry schema; stored-row parse fails closed |
| NX-004-equivalent (unchanged) | `tests/lifecycle/codex-profile-route.test.ts`, `tests/lifecycle/cline-profile-route.test.ts`, `tests/unit/model-registry.test.ts` | Verified exact `(codex, gpt-5.4)` and `(cline, claude-sonnet-4-5)` evidence still resolve after the schema change; missing/cross-provider evidence still fails closed |
| Existing Codex OAuth / ClinePass exact evidence | `tests/contract/providers/direct-adapters.test.ts`, lifecycle suites above | Verified unchanged behavior after the schema change |

## Phase 11 executable evidence

| Acceptance | Evidence | Status |
| --- | --- | --- |
| AT-023 | `tests/contract/openai-responses/responses.test.ts`, `tests/e2e/codex/fake-upstream.e2e.test.ts` | Verified Responses text/function/reasoning/usage/continuation and Codex fake launcher E2E |
| AT-024 | `tests/contract/openai-responses/responses.test.ts`, `tests/integration/fake-upstream/openai-responses-route.test.ts` | Verified unknown required items/include fail closed; client abort binds the upstream signal |
| AT-032 | `tests/storage/retention.test.ts` | Verified versioned policy, expired class deletion, and interrupted-cleanup resume |
| AT-030 | `scripts/check-license.mjs`, `scripts/package-release.mjs`, `scripts/clean-install-smoke.mjs`, `tests/control-plane/migrations.test.ts` | Verified license/provenance inventory, private-path-free package, clean-install doctor, and existing migration restore |
| AT-001/002 Codex | `tests/lifecycle/child-launcher.test.ts`, `tests/lifecycle/global-config-isolation.test.ts` | Verified transient `CODEX_HOME` and byte-identical `~/.codex` |

## Phase 06 executable evidence

| Acceptance | Evidence | Status |
| --- | --- | --- |
| AT-003, AT-004 | `tests/management/mutations.test.ts`, `tests/control-plane/repositories.test.ts` | Verified for versioned provider mutations and reject-closed stale/unauthorized cases |
| AT-013 | `tests/management/mutations.test.ts` | Verified pause/resume-style account update; DTO is pseudonym/readiness only |
| AT-014 | `tests/control-plane/repositories.test.ts` | Verified explicit terms acknowledgement against the required revision |
| AT-015, AT-016 | `tests/management/mutations.test.ts`, `tests/control-plane/repositories.test.ts` | Verified atomic profile/pool create and duplicate/cross-provider/stale rejection |
| AT-025, AT-026 | `tests/management/auth.test.ts`, `tests/management/mutations.test.ts` | Verified separate bearer, single-use fragment exchange, cookie flags, CSRF/Origin/replay/logout/stale-version fail-closed |
| AT-001, AT-002 management listener | `tests/lifecycle/management-lifecycle.test.ts`, `tests/lifecycle/gateway-lifecycle.test.ts` | Verified loopback management bind, foreign fail-closed, teardown, no port increment |
| SR-NF-002 / AT-027 subset | `tests/privacy/control-plane.test.ts`, `tests/control-plane/schema.test.ts` | Verified SQLite/DTO/audit have no secret or raw-identity columns |
| SR-NF-008 migration recovery | `tests/control-plane/migrations.test.ts` | Verified failed and interrupted migrations restore the prior schema |

## Phase 07 executable evidence

| Acceptance | Evidence | Status |
| --- | --- | --- |
| AT-005, AT-006 | `tests/credentials/import.test.ts` | Verified explicit Codex import leaves the source byte-identical; malformed/oversized/changed sources fail without a usable project record |
| AT-007, AT-008 | `tests/credentials/oauth.test.ts` | Verified PKCE/S256 exact loopback login; state mismatch, replay after close, cancel, callback collision, and bounded token bodies fail closed |
| AT-009, AT-010 | `tests/credentials/refresh.test.ts`, `tests/credentials/store.test.ts` | Verified single-flight refresh commits one generation; stale commit cannot overwrite a newer record; crashed credential locks are reclaimed and live locks are not stolen |
| AT-011 | `tests/credentials/revoke.test.ts`, `tests/management/credentials.test.ts` | Verified revoke removes usable active/temp/backup project records and excludes the account |
| AT-012 | `tests/credentials/store.test.ts` | Verified corrupt active restores the last valid backup, or marks the handle unready |
| AT-013 subset | `tests/management/credentials.test.ts`, `tests/management/mutations.test.ts` | Verified pause/select/revoke and secret-free readiness on account DTOs |
| AT-014 subset | `src/credentials/service.ts` readiness | Terms-unaccepted accounts are unready for manual selection |
| Provider-scoped identity | `tests/credentials/identity.test.ts`, `tests/control-plane/repositories.test.ts`, `tests/control-plane/migrations.test.ts` | Verified `(provider, pseudonym)` uniqueness, cross-provider reuse, credential/provider mismatch fail-closed, and v2 migration keeps valid accounts |
| AT-027 subset | `tests/privacy/credentials.test.ts` | Verified DTO/audit/policy JSON contain no access or refresh material |
| Codex OAuth adapter | `tests/contract/providers/codex-oauth/adapter.test.ts`, `tests/credentials/route.test.ts` | Verified request-scoped secret use and one selected account bound into the Anthropic route |
| Live Codex smoke | `tests/contract/providers/codex-oauth/live-smoke.test.ts` | Skipped unless `RLY_LIVE_CODEX_OAUTH=1`; not passing evidence |

## Phase 08 executable evidence

| Acceptance | Evidence | Status |
| --- | --- | --- |
| AT-014 request-time | `tests/routing/eligibility.test.ts` | Verified unaccepted and stale terms acknowledgement are ineligible; current required revision enables selection |
| AT-017 | `tests/routing/strategies.test.ts`, `tests/routing/effective-route.test.ts`, `tests/credentials/route.test.ts`, `tests/credentials/refresh.test.ts` | Verified deterministic manual/round-robin/fill-first selection binds one credential generation after refresh and before invoke |
| AT-018 | `tests/routing/eligibility.test.ts` | Verified paused, expired, unready, cooling, incompatible, and unaccepted candidates are excluded before strategy; exhausted without cooldown is a recovery probe |
| AT-019 | `tests/routing/retry.test.ts`, `tests/routing/outcomes.test.ts` | Verified pre-output auth/quota/transient failure records a transactional outcome; success restores healthy; probe failure extends cooldown |
| AT-020 | `tests/routing/retry.test.ts` | Verified failure after text or tool event does not invoke a second account |
| AT-027 subset | `tests/privacy/routing.test.ts` | Verified decision traces and outcome audit contain no secret or identity fields |
| SR-NF-006 / SR-NF-008 | `tests/routing/race.test.ts`, `tests/routing/restart.test.ts`, `tests/routing/outcomes.test.ts` | Verified selector isolation, durable pause/cooldown across restart, and interrupted health writes roll back |

## Phase 09 executable evidence

| Acceptance | Evidence | Status |
| --- | --- | --- |
| AT-021 subset | `tests/lifecycle/profile-pool-route.test.ts`, `tests/e2e/claude-code/pool-profile.e2e.test.ts` | Verified text, helper mapping, and tools through a profile pool; real Claude E2E remains opt-in (`RLY_CLAUDE_E2E=1`) |
| AT-022 subset | `tests/lifecycle/profile-pool-route.test.ts`, `tests/unit/profiles.test.ts` | Verified concurrent child tokens and lease-drop invalidation; account is not bound at activation. Client abort remains covered by the Anthropic route bind test, not the profile inject suite |
| AT-027 subset | `tests/privacy/profiles.test.ts`, `tests/lifecycle/profile-pool-route.test.ts` | Verified route-trace and activation DTOs contain no credential, identity, prompt, response, or tool argument |
| Profile launch UX | `tests/unit/cli-main.test.ts`, `src/cli/main.ts` | Verified `--profile` is mutually exclusive with `--route` and uses a child token |
| Live Codex pool smoke | `tests/contract/providers/codex-oauth/live-smoke.test.ts` | Skipped unless `RLY_LIVE_CODEX_OAUTH=1` and a project-owned handle is provided; not passing evidence |

## Phase 10 executable evidence

| Acceptance | Evidence | Status |
| --- | --- | --- |
| AT-025, AT-026 | `tests/ui/session.test.ts`, `tests/management/auth.test.ts` | Verified fragment exchange, CSRF rotate/resume, Origin/CSRF fail-closed, security headers |
| AT-027 subset | `tests/ui/page.test.ts`, `tests/ui/session.test.ts`, `tests/privacy/control-plane.test.ts` | Verified UI/HTML/DTO/traces contain no secrets, no browser storage, no file picker |
| AT-031 subset | `tests/ui/page.test.ts`, `tests/browser/at-031.spec.ts` | HTML/CSS plus Chromium: keyboard skip/save/logout, 375/1024 nav, labeled controls, textual status/alert, secret-free DOM. Remaining views (accounts/pools/profiles/traces/confirm) are not this issue. |
| Provider contracts | `tests/contract/providers/catalog.test.ts`, `tests/contract/providers/gemini-oauth.test.ts`, `tests/contract/providers/expansion.test.ts`, `tests/contract/providers/isolation.test.ts` | Verified Gemini/Claude OAuth, Antigravity bridge identity, Cline explicit lock/restore, OpenCode Go/Alibaba isolation |
| Live provider smoke | none in this phase | Opt-in live gates remain skipped; not passing evidence |

## Phase 2 executable evidence

| Acceptance | Evidence | Status |
| --- | --- | --- |
| AT-033 | `tests/unit/cli-main.test.ts`, `tests/lifecycle/global-config-isolation.test.ts` | Verified `rly <profile>` launches Claude Code; reserved commands stay reserved; unknown profile fails closed; `rly run codex` unchanged; global Claude/Codex config byte-identical |

## Phase 3 executable evidence

| Acceptance | Evidence | Status |
| --- | --- | --- |
| NX-001 / FR-008 | `README.md`, `CONTRIBUTING.md` | Operator recipe: `codex` OAuth provider, login or explicit import, pool, Claude profile named `codex`, `rly codex`. `rly run codex` remains Codex CLI |
| AT-021 subset | `tests/lifecycle/codex-profile-route.test.ts`, `tests/e2e/claude-code/codex-oauth.e2e.test.ts` | Verified helper mapping, streaming, and tools through a Codex OAuth profile; Claude Code fake E2E is opt-in (`RLY_CLAUDE_E2E=1`) and skipped ≠ pass |
| AT-022 subset | `tests/e2e/claude-code/codex-oauth.e2e.test.ts` | Verified cancellation aborts the Codex fake upstream; no global Claude/Codex config mutation |
| AT-017 / AT-019 subset | `tests/lifecycle/codex-profile-route.test.ts` | Verified quota rotation and sticky session through existing pool machinery on a Codex profile |
| NX-004 | `tests/unit/model-registry.test.ts`, `tests/lifecycle/codex-profile-route.test.ts` | Verified exact `(codex, gpt-5.4)` evidence; OpenRouter ids and unknown Codex models fail closed; images fail closed |
| AT-027 subset / NX-005 | `tests/unit/cli-diagnostics.test.ts` | Verified `quota` and `route-trace` print only pseudonym, quota class, and decision reason for a Codex profile |
| NX-003 live | `tests/e2e/claude-code/codex-oauth-live.e2e.test.ts`, `tests/contract/providers/codex-oauth/live-smoke.test.ts` | Skipped unless `RLY_LIVE_CODEX_OAUTH=1` (and a project-owned handle / `claude` binary); not passing evidence |

## Phase 4 executable evidence

| Acceptance | Evidence | Status |
| --- | --- | --- |
| NX-012 / FR-003 / FR-008 / AT-005 subset | `README.md`, `CONTRIBUTING.md`, `tests/credentials/import.test.ts`, `tests/management/credentials.test.ts` | Verified operator recipe: `cline` provider with explicit endpoint, preview+import with `providerId`, pool, Claude profile named `clinepass`, `rly clinepass`. Preview without `providerId` is rejected. Import leaves the Cline source byte-identical and writes no lock/backup files |
| AT-021 subset | `tests/lifecycle/cline-profile-route.test.ts`, `tests/e2e/claude-code/cline-interop.e2e.test.ts` | Verified helper mapping, streaming, and tools through a ClinePass profile; Claude Code fake E2E is opt-in (`RLY_CLAUDE_E2E=1`) and skipped ≠ pass |
| AT-022 subset / NX-013 isolation | `tests/lifecycle/cline-profile-route.test.ts`, `tests/contract/providers/isolation.test.ts`, `tests/e2e/claude-code/cline-interop.e2e.test.ts` | Verified Cline failure does not mutate Codex credential files; Cline source remains unchanged |
| NX-004-equivalent | `tests/unit/model-registry.test.ts`, `tests/lifecycle/cline-profile-route.test.ts` | Verified exact `(cline, claude-sonnet-4-5)` evidence; Codex/OpenRouter ids and unknown Cline models fail closed; images fail closed |
| NX-014 live | `tests/e2e/claude-code/cline-interop-live.e2e.test.ts` | Skipped unless `RLY_LIVE_CLINEPASS=1`, `RLY_LIVE_CLINE_HANDLE`, `RLY_LIVE_CLINE_ENDPOINT`, and a `claude` binary; not passing evidence |
| NX-015 superseded | `src/credentials/broker.ts`, `src/providers/interop/cline.ts`, `tests/credentials/import.test.ts` | Default import remains one-time read-only. `lockClineInterop` / `backupClineSource` / `restoreClineSource` are not called from import or launch |

## Phase 120 executable evidence (incremental streaming transport, #120)

| Acceptance | Evidence | Status |
| --- | --- | --- |
| AT-227 / FR-033 / SR-F-037 | `src/routes/anthropic-messages-route.ts`, `src/routes/openai-responses-route.ts`, `src/routes/stream-pump.ts`, `tests/contract/incremental-encoders.test.ts` | Verified neither route retains `seen` to re-encode all previous events: both stream through the single-pass pump + per-stream incremental encoders; the Anthropic encoder retains only open-block state and the Responses encoder only the aggregate projection; no `seen -> encode(all) -> slice` path remains (source inspection + instrumented tests) |
| AT-228 / FR-033 / SR-F-037 | `tests/contract/incremental-encoders.test.ts` | Verified instrumented N-event tests: one push per event, total frames equal the batch encoder, `retainedStateSize()` bounded by open blocks / aggregate projection for N = 1, 10, 1000+; `finish()` fails closed on open content exactly like the batch `final = true` encoder |
| AT-229 / FR-033 / SR-F-037 | `tests/contract/incremental-encoders.test.ts`, `tests/integration/fake-upstream/incremental-streaming.test.ts`, existing #119 fidelity suites | Verified incremental output is byte-identical to `encodeAnthropicSse`/`encodeResponsesSse` on text, tool, reasoning + signature, reasoning + tool interleave, and redacted-reasoning streams; `signature_delta` ordering preserved; route-level streamed body equals batch output byte-for-byte; #119 golden fixtures stay green |
| AT-230 / FR-033 / SR-F-037 | `tests/integration/fake-upstream/incremental-streaming.test.ts` (real-socket slow consumer) | Verified a slow real HTTP client reading a multi-megabyte stream with a deliberate pause longer than the idle window receives the byte-identical body with no spurious idle timeout (idle clock disarmed during backpressure suspension); streamed response metadata reports `backpressureCount > 0` and `terminalKind: completed`; frames complete, in order, no duplication |
| AT-231 / FR-033 / SR-F-037 | `tests/integration/fake-upstream/incremental-streaming.test.ts`, `tests/lifecycle/stream-pump.test.ts`, `tests/lifecycle/stream-lifecycle.test.ts` | Verified client disconnect mid-stream aborts upstream work promptly through the merged AbortSignal (observed upstream abort), no error/partial frames after close, and one-time cleanup of pump/encoder/timers/abort listeners; an upstream that ignores abort is stopped at the next terminal check |
| AT-232 / FR-033 / SR-F-037 | `tests/integration/fake-upstream/incremental-streaming.test.ts`, `tests/lifecycle/stream-lifecycle.test.ts` | Verified setup (first-frame) and idle (progress) timeout policies: paced healthy streams complete even with a small idle window; a never-starting upstream emits exactly one in-band setup-timeout error frame; a mid-stream stall emits exactly one idle-timeout error frame (Anthropic `api_error`, Responses `timeout_error`); no generic whole-request timer exists |
| AT-233 / FR-033 / SR-F-037 | `tests/lifecycle/stream-lifecycle.test.ts`, `tests/lifecycle/stream-pump.test.ts` | Verified exactly-once termination on every terminal path: normal completion (onComplete/onFinished once, `completed`), encoder error (one error frame, `error`), timeout (one in-band frame, `timeout` + category), disconnect (no frames, `cancelled`), teardown (`cancelled`, not completed); `finish()` idempotent, clears timers, removes client abort listener once; clean end overrides spurious close-after-end cancellation |
| AT-234 / FR-033 / SR-F-037 | `tests/integration/fake-upstream/incremental-streaming.test.ts` (soak), `tests/contract/incremental-encoders.test.ts` | Verified 3000+ event streams route to byte-identical bodies within a bounded wall-clock window; O(N) encoder work with bounded retained state; no route retains event history |
| AT-235 / FR-033 / SR-F-037 | `tests/lifecycle/stream-lifecycle.test.ts`, `tests/privacy/*` | Verified stream metrics surface only `eventCount`, `frameCount`, `backpressureCount`, `durationMs`, `terminalKind`, `timeoutCategory` — no prompt/response/reasoning/tool/signature/credential values; metrics key set fixed and content-free |
| AT-236 / FR-033 / SR-F-037 | `docs/ARCHITECTURE.md`, `docs/protocol-compatibility.md`, `docs/SPEC.md`, `docs/requirements/*`, `docs/TASKLIST.md` | Verified ARCHITECTURE, protocol-compatibility, SPEC, FRS/SRS, RTM, and this catalogue describe the incremental encoder API, O(N) route execution with bounded state, backpressure suspension, cancellation propagation, setup/idle/progress timeout policy (no whole-request timer), exactly-once cleanup, and metadata-only diagnostics; FR-033 / SR-F-037 / AT-227–AT-236 added with non-colliding IDs (checked against merged dev max FR-032 / SR-F-036 / AT-226 from #94) |

## Phase 121 executable evidence (native Responses rail, error fidelity, commitment retry, #121)

| Acceptance | Evidence | Status |
| --- | --- | --- |
| AT-237 / FR-034 / SR-F-038 | `src/providers/direct/openai-responses-adapter.ts`, `src/providers/direct/openai-chat-adapter.ts`, `src/providers/direct/openrouter-adapter.ts`, `tests/contract/providers/openai-responses-adapter.test.ts`, `tests/contract/conformance-corpus.test.ts`, `tests/contract/protocol-fidelity.test.ts` | Verified native `POST {endpoint}/responses` rail preserving item id (`content-started.itemId` → client wire), `previous_response_id`/response-id continuation, tool/reasoning ordering, and `encrypted_content` via `include`; provider-returned artifacts flow through `fidelity-artifacts` events into the continuation store and re-attach on continuation |
| AT-238 / FR-034 / SR-F-038 | `src/providers/direct/openai-chat-adapter.ts` (same-protocol dispatch), `tests/contract/providers/openai-responses-adapter.test.ts`, `tests/contract/conformance-corpus.test.ts` | Verified Responses requests to a native-contract adapter call `/responses` (never `/chat/completions`); cross-protocol providers (DeepSeek) still fail closed `unsupported-fidelity` with zero upstream calls; outbound item shape and client-wire item ids pinned |
| AT-239 / FR-034 / SR-F-038 | `src/providers/provider-error.ts`, `src/routes/provider-error-mapping.ts`, `src/routes/openai-responses-route.ts`, `src/routes/anthropic-messages-route.ts`, `tests/contract/conformance-corpus.test.ts`, `tests/privacy/error-retry.test.ts` | Verified allowlisted status/code/type/message/param/retry-after/rate-limit survive the adapter boundary; both routes emit protocol-correct client errors and `retry-after`; deterministic cross-protocol `translateProviderError`; 429 fixture pinned |
| AT-240 / FR-034 / SR-F-038 / SR-NF-009 | `src/providers/provider-error.ts`, `src/observability/retry-trace.ts`, `tests/privacy/error-retry.test.ts` | Verified secret-bearing provider bodies, route error payloads, retry traces, and `retryReasonOf` never expose credentials/auth headers/raw bodies/prompts/reasoning text; `ProviderErrorInfo` and `RetryTrace` key sets fixed and allowlisted; privacy scan passes |
| AT-241 / FR-034 / SR-F-038 / SR-NF-007 | `src/providers/commitment.ts`, `src/routing/pools/retry.ts`, `src/routing/pools/execute.ts`, `tests/routing/retry.test.ts`, `tests/integration/chaos-retry-commitment.test.ts` | Verified `canRotate` consumes explicit commitment (`not-sent` only); pool advances commitment on response-started/output/tool events; failure after provider acceptance with no output byte never rotates |
| AT-242 / FR-034 / SR-F-038 / SR-NF-007 | `src/routing/pools/execute.ts`, `src/protocols/anthropic/fake-upstream.ts`, `src/providers/provider-error.ts`, `tests/integration/chaos-retry-commitment.test.ts` | Verified post-send network failure (unknown) never replayed: no pool rotation, `collectWithSafeRetry` no retry; connection-level failures (not-sent) rotate within budget |
| AT-243 / FR-034 / SR-F-038 / SR-NF-007 | `tests/integration/chaos-retry-commitment.test.ts` | Verified chaos at tool boundary/streaming/provider-acceptance invokes exactly one account and executes the tool side effect exactly once — no duplicate tool execution, no failover after commitment |
| AT-244 / FR-034 / SR-F-038 | `tests/fixtures/protocol/responses-native-rail-*.json`, `tests/contract/conformance-corpus.test.ts` | Verified WIRE-semantics corpus: native Responses stream (reasoning+text+tool interleave, item ids, artifacts), 429 error (retry-after/rate-limit/commitment), disconnect ambiguity (provider-accepted, no replay), continuation outbound body, Anthropic stop/error frames |
| AT-245 / FR-034 / SR-F-038 / SR-NF-007 | `tests/integration/chaos-retry-commitment.test.ts` | Verified failures at each commitment stage (before-send rotates; after-send/acceptance/streaming/tool never replay) with a counting tool executor proving no duplicate side effects |
| AT-246 / FR-034 / SR-F-038 | `tests/integration/fake-upstream/responses-native-soak.test.ts` | Verified 3000+ event native streams route byte-identically twice with bounded encoder retained state and no cross-request leakage; not-sent retry in the pool yields one byte-identical completed response; continuation storage bounded |
| AT-247 / FR-034 / SR-F-038 | `docs/protocol-compatibility.md`, `docs/ARCHITECTURE.md`, `docs/SPEC.md` | Verified exact native Responses endpoint contract documented separately from Chat-Completions translation; `OpenAI-compatible` never a sufficient class |
| AT-248 / FR-034 / SR-F-038 | `docs/protocol-compatibility.md`, `docs/requirements/*`, `tests/contract/conformance-corpus.test.ts`, `src/canary/claim.ts` | Verified corpus/contracts record exact protocol + adapter + access path per scenario (aligned with the #122 claim-key vocabulary) so Wave 2 can key compatibility claims to exact contracts; FR-034 / SR-F-038 / AT-237–AT-248 added with non-colliding IDs (checked against merged dev max FR-033 / SR-F-037 / AT-236 from #120) |

## Phase 35 executable evidence (standalone runtime artifacts, #35)

| Acceptance | Evidence | Status |
| --- | --- | --- |
| AT-249 / FR-035 / SR-F-039 | `scripts/standalone/build-standalone.mjs`, `scripts/standalone/verify-artifact.mjs`, `scripts/standalone/pack.mjs` (launcher), `tests/unit/standalone-artifact.test.ts` | Verified the self-locating `rly` launcher executes the BUNDLED pinned Node (`bin/node`) against `dist/cli/main.js`; clean-artifact smoke `rly --version` passes from the unpacked artifact and from a foreign working directory with no user node/npm/pnpm; real end-to-end build+smoke on linux-x64 (`node scripts/standalone/build-standalone.mjs --target linux-x64 --local-node` → smoke `passed`) |
| AT-250 / FR-035 / SR-F-039 | `scripts/standalone/pack.mjs` (`TARGET_MATRIX`, `targetStatus`, `hostTarget`), `tests/unit/standalone-artifact.test.ts` | Verified `darwin-arm64`, `darwin-x64`, `linux-x64`, `linux-arm64` are all explicit with status+reason; `linux-x64` is `supported`, the other three `experimental` (built deterministically, smoke requires an unprovisioned runner); every target maps to its own Node dist tarball (distinct names) and records its own `targetPlatform` — no silent byte reuse |
| AT-251 / FR-035 / SR-F-039 / SR-NF-002 | `scripts/standalone/pack.mjs` (`checkAllowlist`, `checkForSecretContent`, `copyEntryDeref`), `scripts/standalone/build-standalone.mjs`, `tests/unit/standalone-artifact.test.ts` | Verified the positive top-level allowlist + forbidden markers + secret-content scan fail packaging on `.env`, `.git`, `plans/`, `tests/`, `coverage/`, `reports/`, snapshots, sqlite, keys, pnpm metadata, `.bin` shims (absolute store paths), escaping/absolute symlinks; a clean artifact passes; dependency test artifacts are pruned from bundled deps |
| AT-252 / FR-035 / SR-F-039 | `scripts/standalone/node-version.json`, `scripts/standalone/build-standalone.mjs` (`downloadNode` sha256 + `--version` verification), `rly-artifact.json` (`bundledNodeVersion`/`bundledNodeVersionSource`) | Verified the bundled Node version is pinned, recorded in artifact metadata from the ACTUAL bundled binary, sha256-verified against nodejs.org SHASUMS256, and the extracted binary's `--version` must equal the pin; `rly-artifact.json` is the SBOM input for #128 |
| AT-253 / FR-035 / SR-F-039 / FR-032 / SR-F-036 | `scripts/write-build-identity.mjs` (`RLY_RELEASE_VERSION`), `scripts/standalone/pack.mjs` (`resolveReleaseVersion`, `buildRlyManifest`, `buildArtifactMetadata`), `tests/unit/standalone-artifact.test.ts` | Verified `rly-build.json` == `rly.json` == `rly-artifact.json` identity fields and `dist/rly-build.json` matches the artifact-root copy; canonical version authority `RLY_RELEASE_VERSION` → exact git tag → `package.json` resolves the 0.1.0-vs-tag ambiguity; a diverging identity version fails the build; `rly --version` from the artifact reports the same identity |
| AT-254 / FR-035 / SR-F-039 / FR-032 | `scripts/standalone/pack.mjs` (`treeDigest`), `tests/unit/standalone-artifact.test.ts` | Verified the artifact digest is content-addressed over the exact tree (sorted paths + file digests + relative symlink targets), recorded in `rly-artifact.json`, recomputed on verification (mismatch fails); byte-distinct trees with the same semantic version get different digests and tarball sha256; identical inputs produce byte-identical tarballs (two CLI builds matched `3bf335…`) |
| AT-255 / FR-035 / SR-F-039 / FR-032 | `scripts/standalone/pack.mjs` (`TOP_LEVEL_ALLOWLIST`, launcher), `tests/unit/standalone-artifact.test.ts` | Verified the deterministic layout `rly`, `bin/node`, `dist/`, `node_modules/`, `package.json`, `LICENSE`, `docs/third-party-notices.md`, `rly.json`, `rly-build.json`, `rly-artifact.json`; launcher is self-locating (no source repo/pnpm workspace/CWD dependence); layout consumable by the #94 initial-deployment materialization and the #129 installer |
| AT-256 / FR-035 / SR-F-039 | `.github/workflows/standalone-artifacts.yml`, `scripts/standalone/build-standalone.mjs` (smoke), `scripts/standalone/verify-artifact.mjs` | Verified the CI matrix builds + verifies every target on release publication/manual runs and smoke-tests every SUPPORTED CI target (linux-x64 `rly --version` from the unpacked artifact, identity matches metadata); experimental targets are built + verified and their unprovisioned smoke is explicitly `skipped`, never reported as passed |
| AT-257 / FR-035 / SR-F-039 | `docs/SPEC.md`, `docs/ARCHITECTURE.md`, `docs/project-decisions.md`, `docs/release-governance.md` | Verified packaging/release docs state npm/Homebrew secondary channels must consume the SAME canonical artifact lineage (tarball + sha256 + content-addressed digest + identity) and never build semantically different bytes under the same build identity; standalone artifacts are PRIMARY, external Node is a development concern only |
| AT-258 / FR-035 / SR-F-039 | `docs/ARCHITECTURE.md`, `docs/SPEC.md`, `docs/project-decisions.md`, `docs/requirements/*`, `docs/release-governance.md`, `README.md`, `CONTRIBUTING.md` | Verified docs/architecture identify standalone RLY-owned artifacts as the PRIMARY production distribution; FR-035 / SR-F-039 / AT-249–AT-258 added with non-colliding IDs (checked against merged dev max FR-034 / SR-F-038 / AT-248 from #121) |
