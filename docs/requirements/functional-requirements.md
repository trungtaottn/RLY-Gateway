# Functional Requirements Specification

## Functional domains

### FR-001 — Initialize and attest local runtime

- Traces: BR-008, SR-F-018, SR-NF-001.
- Preconditions: configured deterministic ports are available or owned by a compatible attested instance.
- Behavior: inspect listeners and ownership; start one loopback instance or acquire a compatible lease; create separate data and management secrets; launch the requested harness with transient settings.
- Failure: foreign/mismatched listener returns an actionable error; no signal, reuse, or alternate port.
- Acceptance: AT-001, AT-002.

### FR-002 — Administer provider definitions

- Traces: BR-002, BR-010, SR-F-004.
- Preconditions: authenticated management principal.
- Behavior: create/update/disable provider definition, integration mode, endpoint policy, capability evidence, required terms revision, and provenance reference using optimistic versioning.
- Failure: stale version, invalid mode, missing evidence, or unauthorized mutation fails without partial state.
- Acceptance: AT-003, AT-004.

### FR-003 — Import an existing credential

- Traces: BR-003, SR-F-006, SR-NF-002/003.
- Preconditions: explicit source path/type, provider importer, and owner confirmation.
- Main flow: read bounded source; validate schema; derive non-secret account metadata; write a new project record atomically; preserve source hashes/metadata for immutability proof; return a handle only.
- Failure: schema drift, permission failure, collision, secret validation failure, or source change aborts import and removes temporary project files.
- Acceptance: AT-005, AT-006.

### FR-004 — Complete OAuth login

- Traces: BR-003, SR-F-007, SR-NF-005.
- Preconditions: provider OAuth adapter and free exact loopback callback endpoint.
- Main flow: generate PKCE and single-use expiring state; open authorization; validate exact callback/state; exchange bounded response; atomically persist generation one; expose readiness without identity or token.
- Failure: state mismatch/replay/expiry, callback collision, cancellation, invalid grant, malformed response, or oversized error leaves no usable partial credential.
- Acceptance: AT-007, AT-008.

### FR-005 — Refresh a credential safely

- Traces: BR-003, SR-F-007/008, SR-NF-006.
- Preconditions: refreshable current generation and refresh requirement.
- Behavior: acquire per-credential single-flight and an ownership-aware lock (lock id, credential handle, owner pid, process start identity, timestamp); never steal a live lock; reclaim a lock only when pid/start identity proves the owner is gone; refresh; validate response; commit only if stored generation still matches; atomically replace and retire transient backup.
- Failure: stale refresh cannot overwrite a newer generation; invalid grant marks account authentication-unready without exposing upstream body.
- Acceptance: AT-009, AT-010.

### FR-006 — Logout, revoke, and recover credentials

- Traces: BR-003, BR-012, SR-F-007/008.
- Behavior: invoke upstream revoke where supported; invalidate local account; remove active/temp/backup usable records; fsync directory; recover only the last schema-valid, non-revoked record after interrupted writes.
- Constraint: do not claim forensic erasure of deleted filesystem blocks.
- Acceptance: AT-011, AT-012.

### FR-007 — Manage accounts and terms

- Traces: BR-002, BR-010, SR-F-005/010.
- Behavior: list pseudonymous accounts keyed by `(provider, pseudonym)` so the same pseudonym may exist on another provider; show readiness/quota class/pause/cooldown/terms state; pause/resume; acknowledge a specific provider terms revision; invalidate acceptance when required revision changes.
- Failure: credential/provider mismatch fails closed; UI/CLI never returns raw identity or secret.
- Acceptance: AT-013, AT-014.

### FR-008 — Configure pools and profiles

- Traces: BR-001/002/007, SR-F-009/011/015.
- Behavior: create pool membership, strategy, bounded affinity/retry; create harness profile with provider/pool and model roles; validate referenced capabilities and ownership. The profile name is the canonical Claude Code alias: `rly <profile>` launches Claude Code with that profile; `rly run claude --profile <name>` remains compatibility; `rly run codex` launches Codex CLI and is not a profile alias. Reserved CLI commands are never treated as profile names.
- Failure: invalid references, duplicate memberships, unsupported strategy, or stale version fails atomically. Unknown profile names fail closed and are not remapped to Codex CLI.
- Acceptance: AT-015, AT-016, AT-033.

### FR-009 — Select an account per request

- Traces: BR-005/010, SR-F-010/012/013.
- Main flow: decode request; derive capabilities; load one policy revision; refresh the selected credential to a final generation; filter eligibility including terms; apply deterministic strategy; bind account pseudonym and that credential generation; create immutable EffectiveRoute; invoke using the frozen generation without a further refresh.
- Failure: no eligible account returns a structured, secret-free error; no silent provider/model substitution.
- Acceptance: AT-017, AT-018.

### FR-010 — Handle provider outcome and bounded rotation

- Traces: BR-005/012, SR-F-013/014.
- Behavior: classify pre-output authentication/quota/transient failures; transactionally update health/cooldown; rotate only within configured budget and only before first output/tool event; seal route after output begins. Quota failure marks `exhausted` and starts cooldown; after cooldown the account is a recovery probe; success restores `healthy`; probe failure extends cooldown. Authentication, quota, and transient stay classified separately; invalid-grant may mark authentication-unready without treating that as quota death.
- Failure: post-output failure propagates without another account invocation.
- Acceptance: AT-019, AT-020.

### FR-011 — Preserve Claude Code protocol behavior

- Traces: BR-001/006, SR-F-001/003/015.
- Behavior: preserve text, image, tool, thinking, usage, stop, stream, cancellation, helper-role mapping, and concurrency within declared capability.
- Failure: unsupported required behavior rejects before upstream invocation.
- Acceptance: AT-021, AT-022.

### FR-012 — Preserve Codex Responses behavior

- Traces: BR-006, SR-F-002/003/015.
- Behavior: preserve Responses item/event, function argument, reasoning, usage, error, cancellation, and continuation semantics through the shared control plane.
- Failure: unknown required lifecycle semantics mark compatibility unready.
- Acceptance: AT-023, AT-024.

### FR-013 — Operate management CLI and UI

- Traces: BR-002/007/009, SR-F-016/017.
- Behavior: CLI authenticates with separate instance bearer; browser exchanges a single-use fragment for a bounded session; all mutations require exact Origin, CSRF, current version, and secret-free DTOs; logout/shutdown invalidates session.
- Accessibility behavior: UI supports keyboard-only operation, visible focus, programmatic labels, textual status/error output, and supported responsive viewports.
- Acceptance: AT-025, AT-026, AT-031.

### FR-014 — Diagnose without leaking data

- Traces: BR-005/009, SR-F-014/016, SR-NF-009.
- Behavior: expose request ID, provider/model/profile, policy revision, pseudonymous account, capability/readiness, decision reason, timing/status, and coarse quota class.
- Prohibition: no prompt, response, tool argument, email, account identity, authorization header, or raw credential.
- Acceptance: AT-027.

### FR-015 — Reuse upstream source with provenance

- Traces: BR-004, SR-F-019.
- Behavior: require exact revision/artifact hash, source/destination paths, license, copyright notice, adaptation classification, and verification owner before substantial copy.
- Failure: unknown CLIProxy Plus or other component license blocks copying that component.
- Acceptance: AT-028.

### FR-016 — Bootstrap a clean public baseline and promote releases

- Traces: BR-011, SR-F-020.
- Preconditions: approved `dev` snapshot passes release/privacy/provenance gates.
- Behavior: for the one-time baseline, create a release branch from orphan `main`; copy the approved tracked snapshot without `.git` history/local artifacts; create one commit; open one PR to `main`. After that baseline, promote `dev` to `main` only through a reviewed PR that preserves ancestry.
- Prohibition: never merge/rebase historical private `dev` into `main` or push private branches as part of the first public baseline.
- Acceptance: AT-029, AT-030.

### FR-017 — Enforce retention and deletion

- Traces: BR-009/012, SR-F-021, SR-NF-008/009/014.
- Behavior: apply a versioned policy to logs, metadata audit, response continuation state, temporary/backup credentials, revoked records, database/migration backups, and cleanup evidence; refuse release when a class lacks an owner, duration/budget, or deletion verification.
- Failure: interrupted cleanup remains recoverable and resumes idempotently; expired material cannot re-enter active state.
- Constraint: deletion evidence proves application-level reachability/removal, not forensic media erasure.
- Acceptance: AT-032.

### FR-018 — Bootstrap and operate the persistent per-user runtime service

- Traces: BR-008, SR-F-022, SR-NF-001/002.
- Preconditions: `rly init` is invoked with a valid configuration; deterministic ports are available or owned by a compatible attested resident instance.
- Behavior: settle the durable `~/.rly` home; validate the control-plane store; register the per-user service idempotently (macOS LaunchAgent or Linux `systemd --user`, no root); start the resident runtime; wait for an attested compatible resident instance; expose `rly gateway start|stop|status`; report version/resident state.
- Resident ownership: the service holds a service-owned lease renewed by its own process, so the zero-lease idle shutdown never fires while the service is intentional; child launch/session leases stay independent and revocable.
- Failure: foreign or unattested listener fails closed without signaling the owner; a launcher-owned instance is never reused or killed by the service; stale records are recovered with startup-lock/process-identity rules.
- Shutdown: explicit service stop revokes launch sessions and closes boundedly (revoke, bounded close, broker/control-plane close, artifact cleanup).
- macOS LaunchAgent specifics (#33): one stable per-user `com.rly.gateway` label and plist in the current user's GUI launchd domain, never root; absolute executable/state paths; `RunAtLoad` plus bounded `KeepAlive`/`ThrottleInterval` crash restart; service stdout/stderr into the durable RLY log directory; idempotent repair that unloads stale definitions before reloading; launchctl v2 (`bootstrap`/`kickstart`/`bootout`/`print`) with legacy `load`/`start`/`unload`/`list` tolerance; registration/load state reported separately from runtime `/identity` readiness.
- Linux `systemd --user` specifics (#34): one stable per-user unit `rly-gateway.service` under `~/.config/systemd/user` (mode `0600`, directory `0700`), never root; absolute executable/entrypoint/config paths plus durable `~/.rly` `WorkingDirectory`; `Restart=on-failure` with explicit `RestartSec` and a bounded `StartLimitIntervalSec`/`StartLimitBurst` policy so repeated broken startups become a diagnosable `failed` state instead of an uncontrolled tight loop; service stdout/stderr appended into the durable RLY log directory (`~/.rly/logs/service.log`), journal default otherwise; a reachable user systemd manager (user D-Bus) is probed before any mutating operation and a session without one (containers/minimal distros/WSL) fails actionably with explicit guidance that RLY never auto-enables `loginctl enable-linger`; `daemon-reload` runs only when the definition changed; idempotent repair never duplicates the unit; `systemctl --user` enable/start/stop/restart/disable command construction stays centralized in the adapter; unit enabled/active/process state reported separately from runtime `/identity` readiness.
- Prohibition: no credential, token, or account identity in service definitions, logs, or diagnostics.
- Acceptance: AT-035, AT-036, AT-040.

### FR-019 — Operate the `rly config` user control plane

- Traces: BR-002, BR-007, SR-F-023, SR-NF-002/004.
- Preconditions: `rly init` has completed (durable `~/.rly` installation record) or an explicit `--config` path is provided; an attested compatible runtime is reachable or recoverable.
- Behavior: resolve durable configuration from the installation record so `rly config` works from any working directory without a local `gateway.config.toml`; ensure/recover the resident runtime before control-plane operations (reuse an attested launcher-owned instance as-is, start the registered per-user service when it is down, fall back to a session-scoped foreground runtime for uninitialized/dev checkouts, and fail closed on foreign or incompatible listeners); provide a secret-free status summary and a local loopback UI bootstrap through the existing single-use fragment session contract; create/list providers, accounts, pools, and profiles through the same management endpoints and DTOs as `rly admin` so both surfaces observe exactly one policy revision; perform credential login/import/refresh/revoke through the credential broker persisting only handle/generation metadata.
- Failure: an uninitialized home, a missing recorded config, a foreign or incompatible runtime, a stale versioned mutation, an invalid pool/profile, or a credential failure returns an actionable error with no partial state and no secret, token, or account identity in output.
- Constraint: closing the config UI never stops the resident runtime; `--headless` prints the bootstrap URL without opening a browser.
- Acceptance: AT-048, AT-049, AT-050, AT-051, AT-052.

### FR-020 — Compose the RLY Claude configuration overlay

- Traces: BR-008, BR-009, SR-F-015, SR-F-024, SR-NF-002.
- Preconditions: a Claude launch is requested (`rly <profile>` or `rly run claude`) and the durable RLY control-plane directory is resolvable.
- Behavior: before launching Claude, prepare the durable RLY Claude overlay under `<control-plane>/claude` (`0700` directories, `0600` files, atomic writes) and point the child `CLAUDE_CONFIG_DIR` at it; compose the native user Claude config root (parent `CLAUDE_CONFIG_DIR` or `~/.claude`) as read-only input through a typed allowlist: `settings.json` one-way merge (strip `env` keys that conflict with the child-only gateway contract; keep unrelated settings and the native `model` as user input; preserve a previously persisted RLY-only projection model as RLY-owned state); user `agents/*.md`, `commands/*.md`, and `skills/**` one-way refresh copies; `plugins/config.json` with only the enablement declaration (`enabledPlugins`/`marketplaces`) and credential-bearing keys dropped. Never copy unknown files, plugin cache/repos, history, or runtime state; never read or write `~/.claude.json`; never touch project-local `.claude`.
- Refresh policy: an allowlisted file is composed when missing or when the native file is newer; unchanged native input leaves the overlay untouched so sibling sessions' `/model` writes survive; native deletions are not propagated (RLY state is additive); malformed native JSON surfaces are skipped, never rewritten or failed over.
- Model isolation: an RLY-only projection model id persisted by Claude `/model` (Enter/direct) lives only in the overlay settings; RLY gateway `ANTHROPIC_BASE_URL`/`ANTHROPIC_AUTH_TOKEN` are child-env only and never persisted in overlay settings; a subsequent plain `claude` launch reads native state only.
- Concurrency: RLY's own writes are atomic and deterministic from native input, so concurrent RLY launches converge without locks; RLY never copies back or restores native settings on exit.
- Diagnostics: `rly status` reports overlay directory/source/allowlist version/composition timestamp only; no settings, agent, plugin, session, or credential content.
- Failure: an unresolvable control-plane home or unreadable overlay write returns an actionable error and the launch does not proceed with a throwaway sandbox.
- Acceptance: AT-065, AT-066, AT-067, AT-068, AT-069, AT-070, AT-071, AT-072.

### FR-021 — Resolve Claude Code subagent model requests

- Traces: BR-001, BR-005, BR-008, BR-009, SR-F-025, SR-NF-002.
- Preconditions: a launch session with an activated Claude profile is bound to a live lease; the request carries the launch child token.
- Behavior: capture Claude Code agent attribution headers (`X-Claude-Code-Session-Id`, `X-Claude-Code-Agent-Id`, `X-Claude-Code-Parent-Agent-Id`) at the Anthropic ingress as typed runtime context on the canonical request, without inspecting prompt content; maintain a session-scoped in-memory execution-context registry bound to the launch session/lease (access provider, frozen physical model id, model family, effective tier, mapping/registry revisions) that disappears on lease revocation or runtime restart; resolve a subagent request's tier in its parent's execution context (exact parent-agent match, then the session's main context, then the launch session's profile-default model when unambiguous); pass the parent's resolved model/family into #69 tier resolution, then #68 capability selection and #70 reasoning translation, then the existing account pool for the frozen target.
- Isolation: a subagent's resolution never mutates the parent/main session's model, profile mapping, or global Claude settings; concurrent subagents with different agent ids resolve independently without context leakage.
- Reasoning: explicit subagent `effort` (documented `low`/`medium`/`high`/`xhigh`/`max`) is preserved through the canonical reasoning request and #70 translation; a tool-using subagent with an explicit reasoning intent requires reasoning-with-tools evidence and fails closed otherwise.
- Failure: unknown tier, missing evidence, unsatisfied capability, reasoning-with-tools gap, or an undeterminable parent/session family produce actionable typed errors (`tier-unavailable`, `capability-rejected`, plus the underlying cause); there is no silent fallback to the parent model, no global subagent model override, and no prompt-derived routing.
- Trace/privacy: route traces may carry allowlisted pseudonyms (hashes) of Claude session/agent/parent ids plus the parent model/family that scoped tier resolution; never prompts, credentials, or durable user identity.
- Acceptance: AT-084, AT-085, AT-086, AT-087, AT-088, AT-089, AT-090.

### FR-021 — Expose the trusted RLY model universe through Claude gateway discovery

- Traces: BR-001, BR-005, BR-009, SR-F-025, SR-NF-002.
- Preconditions: the gateway listener is up and authenticated; at least one enabled control-plane provider has an eligible pool; the model registry has trusted evidence.
- Behavior: expose an authenticated `GET /v1/models` on the gateway listener (not the management listener) matching the supported Claude Code discovery wire contract: `{ data: [{ type: "model", id, display_name, created_at }], has_more, first_id, last_id }` with `limit`/`before_id`/`after_id` pagination. Serve the launch session's pinned model universe when the request carries a launch-session child token, or the universe derived from the current control-plane policy for the instance bearer. Project only trusted registry models from enabled configured provider→pool bindings: `VERIFIED` compatibility by default, `EXPERIMENTAL` only with the explicit `gateway.modelDiscovery.experimentalModels` config opt-in, `BROKEN`/unreviewed/proposed targets never. Every discoverable id uses the Claude-compatible RLY projection namespace (`claude-rly-...`); the same upstream model through two access providers gets two distinct ids/display labels because auth/pool/endpoint/terms differ. Maintain an explicit reverse mapping (projection id → exact access-provider/model target + pinned pool); routing never parses id strings. An exact projected model routes through #68/#70 validation and the pinned provider pool's account selector; an unknown, removed, BROKEN, or ineligible projection target fails closed with a typed error and never substitutes another model or provider. Pin the session's universe (policy revision/hash, registry revision, bindings, experimental policy) at launch-session issue time so a policy/registry change never silently remaps an already-issued projection id mid-session.
- Child launch: RLY-launched Claude sessions receive child-only `CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY=1` (the parent/global environment is unchanged) and the overlay strips that key from native settings env so RLY sessions cannot be silently disabled; a plain `claude` launch never inherits RLY discovery env, auth, or projection ids.
- Diagnostics: `route-trace` shows projection id/display name as allowlisted routing metadata followed by the exact access-provider/model and account decisions; discovery responses and traces never contain credentials, authorization headers, account identity, prompts, or responses.
- Failure: an unauthenticated request is rejected; an unknown/ineligible projection id, a missing provider/pool, or no policy returns an actionable typed error without partial state or secrets.
- Acceptance: AT-073, AT-074, AT-075, AT-076, AT-077, AT-078, AT-079, AT-080, AT-081, AT-082, AT-083.

### FR-022 — Runtime compatibility evidence canary

- Traces: BR-001, BR-005, BR-008, BR-009, SR-F-026, SR-NF-002.
- Preconditions: a checked-out RLY tree with a config (or a control-plane directory for artifacts); no live credentials required for the deterministic fake matrix.
- Behavior: report the exact installed Claude Code / Codex CLI version metadata separately from binary `found` state (version parsed from the client's own `--version` output only; never inferred from timestamps, package directories, or paths; unknown when unparseable). Pin the supported client baseline's wire contract with redacted fixtures: Claude Code session/agent/parent attribution headers, gateway `GET /v1/models` request/auth/response selection incl. the discovered-id prefix rule (`claude`/`anthropic`) and startup cache behavior, `fable`/`haiku`/`sonnet`/`opus` alias semantics, subagent/session `effort` additive field, streaming framing, and `--no-session-persistence`. Run a deterministic fake gate matrix (text, streaming, cancellation, single/multi/parallel tool loops, reasoning, reasoning+tools, model discovery, session attribution, subagent routing, parallel subagents, effort signal, long-running session) for each exact access path and classify it `VERIFIED` (all required gates passed + live evidence where the provider class requires it), `EXPERIMENTAL` (partial or fake-only), `BROKEN` (a required contract fails), or `unknown` (required gates unrun). A newly installed unknown client version surfaces a visible `unknown/not-tested` status and never silently replaces the tested baseline; a deliberately changed fixture fails the exact gate with a typed reason (e.g. `missing-agent-header`, `gateway-model-filter-changed`, `tool-result-invalid`, `reasoning-effort-clamped`).
- Evidence identity: each result is keyed by exact client kind/version + access provider + adapter/integration mode + physical model (and family when known); the same upstream model through two access providers never shares evidence.
- Artifacts: `rly canary run` persists secret-free machine-readable evidence under `<control-plane>/canary/` and `rly canary status` reports tested baselines + latest per-path verdicts; artifacts carry client/provider/model ids, gate names, status, fixture revision, and redacted error categories only — never prompts, model responses, reasoning text, credential material, authorization headers, email, or account identity. Proposals from canary evidence never mutate the trusted registry or tier mappings; promotion stays an explicit reviewed control-plane action (#23/#67).
- Gating: the #72 projection gate consumes canary-derived compatibility state (VERIFIED default, EXPERIMENTAL opt-in, BROKEN/unreviewed never); live provider runs remain opt-in (`RLY_LIVE_CANARY=1`-style gates, skipped ≠ pass) and are never reported as passed when unrun.
- Acceptance: AT-091, AT-092, AT-093, AT-094, AT-095, AT-096, AT-097, AT-098, AT-099, AT-100, AT-101, AT-102.

### FR-023 — Safe zero-downtime runtime update lifecycle

- Traces: BR-008, BR-009, SR-F-027, SR-NF-001/002.
- Preconditions: a per-user installation exists (`rly init`); the candidate runtime artifact is obtained and verified (distribution/signing is #35; #73 owns the lifecycle once a candidate is available).
- Behavior: `rly update` treats candidate installation and resident-runtime activation as separate durable states (`idle`/`installing`/`pending-activation`/`activating`/`active`/`rollback-required`/`failed`) recorded secret-free under `<control-plane>/update-state.json`; installed deployments are immutable and content-addressed under `<control-plane>/runtime/versions/<artifactId>` (SHA-256 over the exact candidate bytes/build tree; semantic version is metadata only, never the storage key — byte-distinct candidates with the same semantic version never share a directory and reinstalling the identical artifact is an idempotent no-write); explicit `staged`/`active`/`previous` references under `runtime/refs/` point only at validated immutable deployments, installation updates only `staged` and never changes the serving `active` reference, and reference replacement is atomic (temp-create + rename + parent fsync, never `rm + symlink`) so readers observe a valid old or new reference; the serving runtime's `/identity` reports its actual runtime version, durable state/schema version, update state, active launch-session count, and drain flag through the attested handshake (never the package version on disk alone); active Claude Code sessions continue on the old process until they end naturally (launch-session count, not TCP connections), unless the user explicitly requests the destructive `--force` path; activation restarts the runtime only through the per-user service manager (#33/#34 `restart()`) and only for attested resident instances (launcher-owned instances are never updated/restarted); once drain begins the old runtime refuses new launch-session issuance (`update-pending`); new launches while activation is pending follow the documented compatibility policy (a compatible CLI/runtime pair may continue on the old runtime; an incompatible pair refuses only new launches with an actionable `update-pending`/`runtime-version-mismatch` message); the activated candidate is accepted only after authenticated identity/readiness/state-open verification; a health/identity failure triggers one bounded rollback to the previous known-good version with verified rollback and a deterministic message; a forward-only/unrollbackable candidate migration blocks activation before any destructive state change; concurrent `rly update` invocations serialize through an ownership-aware update lock (stale locks reclaimed by pid/start-identity); crash/reboot states recover deterministically (pending install → failed with retry guidance, pending activation → resume, interrupted activation → rollback, rollback reference preserved); legacy `runtime/current`/`previous` + `versions/<semver>` layouts migrate in place without deleting a serving known-good runtime before the new ref state is durable (bytes renamed, never removed; durable `migrating`/`committed` marker resumes a crashed migration idempotently; malformed legacy state fails closed with a doctor recovery path); a foreign/unattested port owner after restart fails closed and is never signaled; `rly status`/`rly doctor` expose allowlisted version/update metadata (state, current/pending/previous version, active sessions, drain, CLI↔runtime compatibility).
- Prohibition: no credential, token, prompt, response, or account identity in update state, logs, diagnostics, deployment manifests, or refs; never signal a process from port occupancy alone; never loop restart/rollback indefinitely; no silent forward-only migration.
- Acceptance: AT-103, AT-104, AT-105, AT-106, AT-107, AT-108, AT-109, AT-110, AT-111, AT-112, AT-113, AT-114, AT-115, AT-116, AT-117, AT-118, AT-139, AT-140, AT-141, AT-142, AT-143, AT-144, AT-145, AT-146, AT-147, AT-148, AT-149.

### FR-024 — Classify model selectors into typed model intents (#125)

- Traces: BR-005, BR-008, BR-009, SR-F-028, SR-NF-002.
- Preconditions: a request carries a model selector string; routing has not yet started.
- Behavior: classify the incoming model selector into exactly one typed `ModelIntent` (`EXACT_PROJECTION`, `RLY_LOGICAL_TIER`, `CLIENT_NATIVE_ALIAS`, `EXACT_CLIENT_MODEL`, `INHERIT`, `DEFAULT`) before any routing, preserving the exact source selector and the namespace/rule that produced the classification. Give RLY logical tiers an explicit namespace (`rly-tier:haiku|sonnet|opus|fable`) so bare client-native alias strings (`haiku`/`sonnet`/`opus`/`fable`) are never RLY policy selectors by string equality (core invariant `fable != rly-tier:fable`); a selector claiming the RLY namespace with an unknown value fails closed (`unknown-namespace`) and is never silently reinterpreted. Invoke the #69 provider/family tier resolver only for an explicitly typed tier intent (`RLY_LOGICAL_TIER`, or a client-native alias mapped through the explicit traceable client-alias contract); exact projected selection (#72) remains exact and is dispatched before profile resolution; persisted exact model ids and `profile.modelRoles` tier keys keep their meaning and are never reinterpreted (exact ids keep the #68 exact path). Apply deterministic precedence among exact projection, explicit RLY tier, client alias, profile override, inherit, and default, and distinguish the typed failure taxonomy (unknown namespace, unsupported client alias, unknown exact model, invalid projection, tier unavailable, conflicting selector sources) on the existing profile error contract.
- Diagnostics: route traces expose selector kind/source and the resolved logical target only — never prompts, credentials, account identity, or settings contents.
- Acceptance: AT-119, AT-120, AT-121, AT-122, AT-123, AT-124, AT-125, AT-126, AT-127, AT-128.

### FR-025 — Preserve native protocol rails, fidelity envelope, and opaque continuation artifacts (#119)

- Traces: BR-005, BR-006, BR-008, BR-009, SR-F-029, SR-NF-002, SR-NF-009, SR-NF-012.
- Preconditions: a request is decoded from Anthropic Messages or OpenAI Responses traffic; the wire payload carries wire-significant state not modeled semantically (thinking signatures, reasoning item identity with opaque `encrypted_content`, unknown additive fields); routing has not yet started.
- Native protocol rails: same-protocol traffic keeps the native encoder/decoder wire shapes as the source of wire truth; RLY patches only RLY-owned controls (selected model, auth, endpoint). The semantic core (`CanonicalRequest`/`CanonicalEvent`) stays the routing, capability, tool, reasoning-intent, and cross-protocol translation projection and is never required to absorb every provider-specific opaque field.
- Fidelity envelope: a versioned envelope (`src/core/fidelity.ts`, `FIDELITY_ENVELOPE_VERSION` 1) carries source protocol/revision, typed opaque continuation artifacts (kind, stable association, value), translation provenance notes (`preserved-native`/`translated`/`ignored`/`unsupported`), and the artifact kinds that a compatibility claim requires. Adapters/protocol codecs may preserve opaque artifacts; routing policy inspects only explicitly modeled safe metadata (kind, association, disposition), never artifact values.
- Anthropic fidelity: decode preserves thinking `signature` into the envelope (`anthropic-thinking-signature`) and marks it required when present; the canonical event stream gains a `signature-delta` event emitted in valid order relative to `thinking_delta`/`content_block_stop`; the aggregator attaches the signature to the aggregate thinking block. A signature delta targeting a non-thinking block or arriving before its content block fails closed (`invalid_event_order`).
- OpenAI Responses fidelity: decode preserves reasoning item identity (semantic, non-secret) and opaque `encrypted_content` (`openai-reasoning-encrypted-content`) into the envelope, required when present; continuation storage retains the exact artifact-to-item association across turns; a later `previous_response_id` request merges prior artifacts into the continued request; re-encode attaches each item's exact encrypted content to the matching reasoning item. Opaque content is never reconstructed from summary text.
- Unknown additive fields: recorded as `ignored` provenance notes with the reason "not required for continuation", never silently discarded as required state and never misinterpreted.
- Fail-closed policy: a compatibility claim requiring an artifact cannot pass when the selected translation path cannot preserve it. The Chat Completions transport (OpenRouter/DeepSeek adapters) cannot represent signatures or encrypted content, so a request carrying a required artifact on that path fails with `unsupported-fidelity` before any upstream call — nothing is fabricated, decrypted, or silently dropped.
- Extension points: `OpaqueArtifactKind` is a typed union, so future Gemini thought signatures, OpenRouter reasoning details, DeepSeek reasoning continuation, and other provider-owned opaque artifacts extend the fidelity envelope without redesigning canonical routing.
- Privacy: opaque artifact values are runtime/protocol state, never diagnostics. They are never logged, never placed in route traces, never included in diagnostic bundles, and are redacted by the observability redactor; `describeFidelity()` is the only diagnostic surface and exposes provenance metadata (kinds, dispositions, field names, counts) only. Continuation persistence applies existing private-file/storage rules.
- Acceptance: AT-129, AT-130, AT-131, AT-132, AT-133, AT-134, AT-135, AT-136, AT-137, AT-138.

### FR-026 — Profile-scoped Claude views, typed env/settings ownership, and overlay ownership reconciliation (#126)

- Traces: BR-005, BR-008, BR-009, SR-F-030, SR-NF-002.
- Preconditions: an RLY-launched Claude session is about to be prepared; native user Claude config exists or not; a profile may or may not be selected.
- View identity: prepare a durable per-profile/per-policy Claude view at `<control-plane>/claude/views/<view-id>` (`0700` dirs, `0600` atomic files). `deriveClaudeViewId(profileId)` derives a deterministic, collision-safe id from the immutable control-plane profile id (renames keep the durable view; two profiles never share one); profile-less launches use the reserved `default` view. A profile's RLY-only model/default/discovery/cache/history state must never silently become another profile's state. Plain non-RLY Claude reads native config only.
- Typed ownership: classify relevant Claude/gateway/model settings (`classifySettingsKey`/`classifySettingsEnvKey`) into RLY-owned (gateway-contract env keys; persisted `claude-rly-*` projection model), conflicting (native `model` vs persisted/view state), safe pass-through (unrelated settings/env), unsupported (credential-bearing shapes such as `oauthAccounts` — never composed), and explicit user override (launch-policy `model`/`env`). Precedence is deterministic (high → low): child-only RLY gateway contract env (never inherited/persisted), RLY-owned persisted projection model, explicit RLY/profile settings, user native settings/env (gateway-conflict env stripped), client persistence in the view, defaults. A conflicting native gateway/auth/model setting never overrides RLY's scoped launch contract silently.
- Ownership manifest: write `.rly-manifest.json` per view recording each composed/persisted surface as native-imported (source-relative path + sha256 at import/refresh), RLY-generated (e.g. settings recomposed after native settings removal), or view-owned (durable state; divergent copies reclassified). Metadata and hashes only: never credentials, settings content, prompts, transcripts, or account identity; gateway-contract/unsupported key names are not recorded.
- Deletion/rename reconciliation: on each prepare, an imported view file whose native source disappeared is deleted only when the manifest says native-imported AND the view copy still matches the imported hash; a divergent view copy is reclassified view-owned and kept. RLY never deletes a file it does not own as an import; no additive-only ghost files.
- Concurrency: individual writes are atomic (temp + rename, `0600`/`0700`); the manifest read-modify-write and reconciliation are serialized per view with a bounded reconcile lock (skipped when busy — refresh stays atomic and convergent, reconciliation best-effort); native settings are never rewritten and never “restored” on child exit.
- Native freshness and migration: allowlisted native changes compose per documented precedence (ownership wins over naive newer-mtime); the legacy shared `<control-plane>/claude` overlay migrates into `views/default` deterministically (two-phase sibling move, crash-safe) without touching native `~/.claude`; ambiguous shared persisted state stays in the unprofiled `default` view and is surfaced, never silently assigned to a profile.
- Diagnostics/privacy: `rly status`/`rly doctor` expose per-view id/path, composition version, ownership/reconciliation status (native-imported/rly-generated/view-owned counts, reconciled deletions, reclassifications), conflicting key categories, and last refresh metadata — never settings content, prompts, transcripts, skills/agents text, credentials, auth tokens, or account identity.
- Acceptance: AT-150, AT-151, AT-152, AT-153, AT-154, AT-155, AT-156, AT-157, AT-158, AT-159.

### FR-027 — Transactional update activation, migration classes, and crash-safe recovery (#93)

- Traces: BR-008, BR-009, SR-F-031, SR-NF-001/002; builds on FR-023/SR-F-027 (the #73/#92 lifecycle primitives: immutable store, staged/active/previous refs, INSTALL != ACTIVATE).
- Preconditions: a candidate is installed and staged under `refs/staged` (FR-023); the displaced known-good deployment is preserved under `refs/previous`; the update ownership lock is held or acquirable.
- Behavior: activation is a DURABLE TRANSACTION journaled secret-free on the update-state record — STAGED → DRAINING → SWITCHING → PROBATION → COMMITTING → COMMITTED (or ROLLING_BACK → COMMITTED | RECOVERY_REQUIRED), each phase written durably BEFORE the action it fences and carrying the candidate/previous immutable deployment identities plus a bounded rollback-attempt counter; the update lock records the REAL OS process-start identity from the process-attestation subsystem (never acquisition wall-clock time), treats unverifiable owners conservatively as held, and reclaims only locks whose owner identity is proven stale/dead; the new-launch fence (old generation refuses new launch-session issuance) is established BEFORE the serving ref switch, letting existing sessions complete naturally on the old process (launch-session count, not TCP) and never replaying or moving in-flight streams/tool loops, unless the user explicitly requests the destructive `--force` path (skips only the drain wait); the drain gate FAILS CLOSED (#J7): the fence must actually ENGAGE and the session count must be VERIFIED as zero before the switch — an unattested/inaccessible fence or an unreadable `/identity` session count is UNKNOWN (never treated as zero), activation stays `pending-activation` in the DRAINING phase with an actionable message, and `--force` is the only bypass; the ref switch moves `refs/active` to the staged candidate with `refs/previous` written before `active` so the known-good reference is never lost; probation accepts the candidate only after exact runtime identity (serving version equals the staged candidate), management/data protocol compatibility, authenticated readiness, and durable state/schema compatibility are verified — only then does the transaction COMMIT, and a crash before COMMITTED is durable never silently commits the candidate; migration rollback safety uses compatibility classes (`none`, `backward-compatible-expand`, `transactional-replace`, `forward-only`) replacing the binary `migrationForwardOnly` signal (rollback-safe classes pass preflight; `forward-only` blocks activation before any destructive state change; legacy manifests map true ⇒ forward-only, false ⇒ backward-compatible-expand); a candidate failure triggers exactly one bounded rollback attempt (rollbackAttempts ≤ 1) that re-establishes `active`/`previous` from the durable journal evidence, restarts at most once, and verifies the restored runtime; rollback failure terminates in the explicit `recovery-required` state with an actionable `rly doctor` path — never an infinite restart loop and never auto-retried; crash/reboot recovery makes ONE deterministic choice from the durable journal phase — interrupted install → `failed` with retry guidance, STAGED/DRAINING → resume activation, SWITCHING/PROBATION/COMMITTING → roll back to the durable previous known-good reference (never guessing a candidate committed), COMMITTED → promote to active, interrupted or duplicate rollback → `recovery-required`; the `/identity` handshake reports the transaction phase alongside update state; `rly status`/`rly doctor` expose allowlisted transaction phase, lock-owner status, drain state, and last activation/rollback outcome (identifiers only).
- Prohibition: no credential, token, prompt, response, or account identity in the transaction journal, update state, logs, diagnostics, deployment manifests, or refs; never signal a process from port occupancy alone; never loop restart/rollback indefinitely; no silent forward-only migration; never guess that a candidate committed before the COMMITTED phase is durable.
- Acceptance: AT-160, AT-161, AT-162, AT-163, AT-164, AT-165, AT-166, AT-167, AT-168.

### FR-028 — Versioned, feature-scoped Compatibility Claim and Evidence v2 (#122)

- Traces: BR-001, BR-005, BR-008, BR-009, SR-F-032, SR-NF-002, SR-NF-009.
- Preconditions: a canary/test observation exists for an exact access path; the pinned supported client contract is known.
- Claim identity: a stable, versioned Compatibility Claim key must include the exact execution path — client kind, exact client version/baseline, source protocol/revision (#119 vocabulary), adapter/integration surface, access provider, auth mode, endpoint contract, exact physical model, and the feature/capability claim. Model family is classification metadata only and must never replace exact provider/model/access identity (it is never part of the key). The same upstream model through two access providers, or two features on the same path, must produce distinct claim keys and independent evidence histories; there is no cross-provider/model/feature claim reuse.
- Feature scope: text, streaming, cancellation, tools (single/multi/parallel continuation), reasoning, reasoning+tools, model discovery, session/subagent attribution, config-overlay behavior, and other relied-on features are claimed independently — passing text never implies tool/reasoning/discovery compatibility.
- Evidence Artifact v2: each record carries the claim key, evidence layer/kind, fixture/corpus revision, runner/tool version, timestamp, result (`passed`/`failed`/`not-run`; `missing` is record absence), typed failure reason, environment/platform metadata needed for interpretation, and safe references to raw machine-readable results. Evidence records NEVER contain credentials, auth headers, account identity, prompts, real responses, or reasoning text.
- Evidence layers: Layer A = deterministic protocol/adapter conformance with fake/synthetic fixtures; Layer B = exact installed-client black-box behavior (#123, runner not built here); Layer C = exact real access-path live verification (#123, runner not built here). A claim carries explicit per-layer records; layer presence/result is never collapsed into one boolean. `requiredLayersForAdapter` is the single place adapter layer requirements are declared; unknown adapters fail closed (A/B/C required).
- No boolean trust: `livePassed`/`liveEvidence` are removed from the authoritative classification path; classification can never emit `VERIFIED` from an observation (a full deterministic pass is `EXPERIMENTAL` with `production-claim-not-established` until Layer B/C evidence plus reviewed promotion, owned by #124, exist). A CLI/environment switch (`RLY_LIVE_CANARY`/`liveRunnerEnabled`) may enable a runner hook (#123) but can never stand in for an evidence artifact.
- Reclassification: the current deterministic canary gate matrix is reclassified as Layer A evidence; `rly canary run` emits secret-free Layer A Evidence v2 per feature and appends feature-scoped claim documents under `<control-plane>/claims/`.
- Schema/migration: claim documents carry `schemaVersion` 1 and evidence records `evidenceSchemaVersion` 2; legacy v1 canary outputs are readable but flagged legacy/untrusted (`legacy-v1-artifact-untrusted-for-v2-claims`) and can never silently satisfy a stronger v2 claim — a claim lookup returns `missing` until real v2 observation. Registry `CompatibilityEvidence` carries an optional `claimRef` (revision 5) linking a reviewed state to its exact claim.
- Persistence/query: claim evidence persists append/audit-friendly (atomic writes, records appended, never silently rewritten; identical observations deduped) and supports deterministic lookup by exact claim identity + feature (`ClaimEvidenceStore.findEvidence`). Malformed claim documents fail closed on read.
- Acceptance: AT-169, AT-170, AT-171, AT-172, AT-173, AT-174, AT-175, AT-176, AT-177, AT-178, AT-179.

### FR-029 — Installed-client (Layer B) and live access-path (Layer C) compatibility runners (#123)

- Traces: BR-001, BR-005, BR-008, BR-009, SR-F-033, SR-NF-002, SR-NF-009; builds on FR-028/SR-F-032 (the #122 Claim + Evidence v2 model) and #97 (Wave 2 authority).
- Preconditions: the #122 claim/evidence model is merged; a supported client contract baseline is pinned (#24); Layer C additionally requires explicit opt-in (`RLY_LIVE_CANARY=1`) and an available credential for the exact path.
- Layer B — installed-client black-box (`rly canary run-b`, `src/canary/installed-runner.ts`): detect and record the EXACT installed Claude Code / Codex CLI executable path and version (probe output, never inferred); launch the real binary against a controlled local fixture server serving the client-facing wire surface RLY relies on (Anthropic Messages + `/v1/models`; OpenAI Responses + `/v1/models`) with child-only environment isolation (never real client config, never real credentials); run a per-feature black-box matrix — request/stream framing, cancellation, tool round-trip, multi-tool continuation, parallel tools, reasoning/effort, reasoning+tools, `/v1/models` discovery/selection, session/agent/parent attribution, tier alias behavior, subagent concurrency (distinct concurrent attribution contexts), config-overlay behavior, and long sessions; emit one Evidence Artifact v2 per feature with layer B / kind `installed-client`, runner version, fixture corpus revision, timing, and typed/redacted failure reason. Evidence is keyed to the EXACT observed client version; the reviewed supported baseline is recorded separately and never implied by a black-box pass (drift surveillance: new versions are tested WITHOUT becoming supported baselines; observed ≠ baseline). A missing/non-executable binary reports every gate `not-run` (`client-not-installed`), never PASS.
- Layer C — exact access-path live runner (`rly canary run-c`, `src/canary/live-runner.ts`): execute ONE selected exact claim path — client + client version + source protocol/revision + adapter/integration + access provider + auth mode + endpoint contract + physical model + feature gate — through the real RLY gateway translation stack to the configured provider endpoint, with explicit opt-in and an environment credential (never stored/logged); emit feature-scoped Evidence Artifact v2 (layer C / kind `live-access-path`); the same upstream model through two access providers produces distinct claim keys and independent evidence (no cross-provider sharing); text success can never promote tools/reasoning/discovery claims because each feature carries its own request and record; tool continuation is proven only when the provider actually returns tool calls (otherwise `not-run`/`provider-did-not-call-tool`); `GET /v1/models` discovery is exercised against the exact-path gateway's policy-driven model universe (with the explicit EXPERIMENTAL opt-in for registry targets).
- Never-PASS invariants: missing credentials (`authentication-credentials-unavailable`), skipped runs, environment inability, and unexecuted gates are `not-run` (or absent) — never `passed`, never `VERIFIED`; Layer C refuses to run without `RLY_LIVE_CANARY=1` and never spends quota during normal test execution.
- Typed failure taxonomy: `client-contract-drift` (incl. `missing-agent-header`, `effort-signal-lost`, client-did-not-send-* config), `protocol-translation-failure`, `authentication-failure`, `provider-unavailable`, `provider-rate-limit`, `unsupported-feature`, `malformed-continuation`, `timeout-cancel-failure`, `environment-inability`, `client-not-installed`, `provider-did-not-call-tool`, `not-applicable-to-access-path`.
- Runners are observation-only: they never mutate the trusted registry, tier mappings, `/v1/models` projection, or effective compatibility state; promotion/quarantine/enforcement is owned by #124. Raw runner results persist secret-free under `<control-plane>/canary-runners/` (metadata only: gate, result, typed reason, timing, wire booleans) and Evidence v2 records carry a safe `ref` to them; claim documents append via `ClaimEvidenceStore`.
- Acceptance: AT-180, AT-181, AT-182, AT-183, AT-184, AT-185, AT-186, AT-187, AT-188, AT-189, AT-190.

### FR-030 — Effective Compatibility Registry: review decisions, quarantine, freshness, and enforcement (#124)

- Traces: BR-001, BR-005, BR-008, BR-009, SR-F-034, SR-NF-002, SR-NF-009; builds on FR-028/SR-F-032 (#122 Claim + Evidence v2) and FR-029/SR-F-033 (#123 Layer B/C runners), under #97 (Wave 2 authority). The Effective Compatibility Registry is the SOLE runtime compatibility authority — the model registry keeps owning model identity/capability evidence and must not be duplicated.
- Preconditions: #122 claim/evidence documents exist for an exact execution path (or not — `missing` is a valid fail-closed state); explicit reviewed control-plane operations are authorized.
- Review Decision Store: an explicit promotion/rejection decision is tied to the exact #122 Compatibility Claim identity AND the exact evidence revision (a deterministic digest of the claim's observation history). Positive trust requires an explicit reviewed decision; a new PASS observation never auto-promotes. A decision no longer covering the current evidence revision (evidence updated after review) marks the claim `untrusted`/review-stale — re-review is required. Decisions persist secret-free (reviewer/source/reason/timestamp/revision metadata only) under `<control-plane>/compat/reviews/`, append/audit-friendly with monotonic revisions, and malformed documents fail closed.
- Negative Quarantine Store: a strong reproducible failure can quarantine an EXACT claim/path/feature promptly per documented policy. Scope is inherently narrow (keyed to the exact claim) so one provider/model/feature failure never poisons unrelated paths or features; quarantine never deletes historical trusted evidence. Records persist secret-free under `<control-plane>/compat/quarantines/` with monotonic revisions and explicit lift (audit-friendly).
- Freshness/staleness engine: freshness is defined per evidence kind/claim class and marks evidence STALE when a configured identity/revision dependency changes — client version/baseline drift, protocol/adapter revision drift, provider endpoint/auth-mode drift, physical model fingerprint change, fixture/corpus revision drift, material RLY build change, and evidence age. A stale positive can never remain silently VERIFIED.
- Effective Compatibility Registry resolution: evidence + review decision + quarantine + freshness + policy resolve into ONE effective answer per exact claim/feature (`trusted` / `stale` / `experimental` / `untrusted` / `quarantined` / `missing`) with trust state, observed health, freshness, quarantine, and enforcement reason kept as DISTINCT diagnosable fields — never a single persisted boolean. Precedence: active quarantine → missing evidence → failed evidence → reject decision → promote decision not covering current evidence → promote covering + stale → promote covering + fresh + passed → no decision (PASS = `experimental` at most).
- Default enforcement: normal user execution/exposure requires effective trusted compatibility for the exact required features; a quarantined or otherwise untrusted REQUIRED feature fails closed with the typed reason — no silent fallback across provider/family/model unless an owning policy explicitly permits it. The explicit experimental override (exact-pin opt-in or `allowExperimental`) is traceable and visible in route traces/doctor and can elevate `experimental`/`stale` but NEVER `untrusted`/`missing`/`quarantined`; a hard quarantine is bypassable only through the separately documented administrative policy (`allowQuarantineBypass`), which is visible in the enforcement field.
- Runtime integration: model selection (#68), logical tier resolution (#69), model projection/discovery (#72), reasoning/tool eligibility, and `src/profiles/resolve-route.ts` consume ECR results; static `model.compatibility.state` is no longer the final production trust authority. The selector/tier/projection accept an ECR snapshot (logicalId → per-feature effective answers) and recompute enforcement per request context; the gateway server and `resolve-route` build the snapshot from the claim/review/quarantine stores + the pinned runtime policy.
- Migration/audit: legacy static registry compatibility states become seed/reference data (`seed-reference-only` policy): a static `VERIFIED` row without a `claimRef` can never silently satisfy reviewed v2 trust (it derives `experimental` at best); `BROKEN` remains a hard negative seed. Auditability is preserved when trust changes (monotonic decision revisions, append-only records) and when evidence expires (freshness reasons recorded).
- Diagnostics: doctor/status/`rly compat explain` explain WHY a target is trusted, stale, quarantined, experimental, or blocked — claim identity, latest trust decision metadata, evidence layers, health, freshness reason, quarantine reason, enforcement reason — all secret-free (never credentials, account identity, prompts, responses, or reasoning text).
- Acceptance: AT-191, AT-192, AT-193, AT-194, AT-195, AT-196, AT-197, AT-198, AT-199, AT-200, AT-201, AT-202.

### FR-031 — Produce the EffectiveModelDecision model-control plane (#127)

- Traces: BR-005, BR-008, BR-009, SR-F-035, SR-NF-002, SR-NF-009; builds on FR-024/SR-F-028 (#125 typed selector intent), FR-026/SR-F-030 (#126 profile-scoped views + typed env/settings ownership), FR-021/SR-F-025 (#71 subagent context, #72 projection), #68 model selection, #69 tier resolver, #70 reasoning translation, and FR-030/SR-F-034 (#124 Effective Compatibility Registry as the final compatibility authority). EffectiveModelDecision is the FINAL model-control output before account selection; it does NOT create a second model registry, tier resolver, account selector, or compatibility authority.
- Decision contract: for every supported RLY model-routing request, ONE typed `EffectiveModelDecision` (`src/routing/model-decision/`) is produced BEFORE the pool/account `RouteSelector` runs. It carries (a) the #125 typed selector intent + provenance (kind/source/exact selector), (b) the deterministic precedence bookkeeping — the full documented order (explicit exact selection → RLY logical tier → client-native alias/model → subagent inherit/override → profile/session policy → env/settings ownership → persisted RLY-view state → defaults) plus the winning source and how it resolved, (c) visible, deterministic conflicts (no hidden string/env override silently wins), (d) the FROZEN physical access provider/model/family/logicalId/adapter target, (e) provenance of the target (#72 projection reverse mapping, #69 tier mapping, #125 client-alias mapping, #71 parent context, #68 exact-model, profile role, #126 persisted-view state, env ownership), (f) the #70 effective reasoning intent → native control mapping, (g) the #124 ECR compatibility reference (authority `ecr`/`seed`, effective label, enforcement reason, seed state, per-feature answers), (h) the #72 provider→pool binding + session-pinned universe revision, (i) policy/profile/session/registry/mapping revisions, (j) stable safe decision reasons, and (k) blocked alternatives with typed failure reasons. Account/credential identity is NEVER part of the decision — account selection happens downstream in the pool.
- Precedence and conflicts: precedence is deterministic and documented; the request's explicit selection always wins; conflicts (persisted-view model vs request, launch-policy pin vs request, subagent explicit selection vs frozen parent context, foreign/stale projection vs owning view, gateway-contract env keys present) are recorded with allowlisted detail so nothing wins silently. A stale/foreign RLY projection or another profile's persisted model never silently becomes the effective target: the owning profile view is the only persisted-state input and a projection id resolves only through the session's pinned universe reverse mapping (fail closed otherwise).
- Freeze-before-account: the physical access-provider/model target and reasoning policy are frozen in the decision BEFORE the existing pool/account `RouteSelector`; account retry/failover inside the pool can never change the physical model or selector meaning.
- Subagent/parent affinity: the decision preserves #71 session/agent/parent affinity — explicit child intent and reasoning stay isolated from parent state and the parent model is never mutated by a subagent decision.
- Codex boundary: Codex model/provider control uses the same intent/decision vocabulary without forcing Claude-specific aliases/config semantics onto Codex; client-specific behavior stays gated by exact compatibility evidence (#124).
- Diagnostics/privacy: `rly route-trace` carries the secret-free decision; `rly doctor` reports the model-control contract; diagnostics explain the selected model and blocked alternatives/conflicts with actionable reasons (conflicting env/settings, stale projection, unsupported alias, unavailable tier, incompatible/expired evidence, profile mismatch). Decision output is allowlisted metadata only — never prompts, responses, reasoning text, credentials, auth headers, raw account identity, or full user settings content.
- Acceptance: AT-203, AT-204, AT-205, AT-206, AT-207, AT-208, AT-209, AT-210, AT-211, AT-212, AT-213, AT-214, AT-215.

### FR-032 — Stable runtime bootstrap, exact build attestation, and service-definition reconciliation (#94)

- Traces: BR-008, BR-009, SR-F-036, SR-NF-001/002/008/014; builds on FR-023/SR-F-027 (#73/#92 immutable deployments + transactional activation #93).
- Preconditions: a per-user installation exists (`rly init`); a committed `active` deployment exists under the #92 immutable store (established by init from the installed runtime tree, or by `rly update`).
- Stable bootstrap contract: `rly init` installs ONE RLY-owned executable launcher at `<control-plane>/bootstrap/rly-gateway` (`0700` dir, `0755` owner-executable) and registers the per-user service (macOS LaunchAgent / Linux `systemd --user`) against it — the service definition never references `dist/cli/init.js`, a direct `runtime/refs/...` deployment path, or the Node installation that happened to invoke `rly init`. The launcher is self-locating, resolves ONLY the committed `active` reference from the #92 store (refusing staged/uncommitted/missing candidates with an actionable message and exit 78), validates the immutable deployment (private 0700 current-user directory, matching `.rly-deployment.json` artifact identity, real `dist/cli/main.js` dispatcher), resolves Node (RLY_NODE → PATH → recorded re-validated fallback) without ever encoding it in the definition, exports `RLY_SERVING_ARTIFACT`, and execs the real dispatcher (`dist/cli/main.js gateway start --config <config>`) — `dist/cli/init.js` is a module, not an entrypoint, and is never executed.
- Initial deployment: `rly init` establishes the initial committed `active` deployment from the installed runtime tree (package.json + dist + node_modules + LICENSE + rly.json, PRESERVING pnpm's relative in-tree symlink layout so the deployed runtime resolves transitive dependencies exactly like the source tree; unsafe absolute/escaping links are refused and the tree stays self-contained and digest-deterministic — see FR-040); re-init/doctor never re-copies or rewrites a valid committed deployment (idempotent, INSTALL != ACTIVATE preserved).
- Exact build identity: one versioned identity object (`src/runtime/build-identity.ts`, `identitySchemaVersion` 1) binds semantic version, commit/source revision, build ID, release channel (`dev`/`beta`/`stable`), control/data protocol versions, durable state/schema version, and the serving artifact digest (#92 content-addressed deployment identity when serving from the store). `/identity` carries `build`; `rly --version` and doctor/status report the CLI build identity; the release-candidate manifest (`rly.json`) and deployment metadata declare the same fields; the ownership record's executable fingerprint is the digest of the exact serving build identity. Two artifacts sharing a semantic version are distinguishable by artifact digest, and an exact match requires all fields equal (`exactIdentityMatch`; `sameSemanticVersionDifferentArtifact`).
- Attestation integration: the existing runtime attestation is extended (no parallel identity service) — reuse (`reusableRecord`) requires the serving build identity digest to match the ownership record, so a same-semantic-version-different-artifact record/process fails closed as `attested-incompatible`; foreign/unattested listeners keep failing closed and are never signaled. Update probation (`verifyCandidateRuntime`) requires, when the serving runtime identifies its artifact, that the exact artifact digest equals the candidate's immutable deployment identity plus matching semantic version and control protocol — a same-semantic-version-different-artifact candidate fails probation and rolls back; a runtime that does not identify an artifact (legacy/dev) falls back to the #73 version/protocol/readiness gate.
- Service-definition reconciliation: `rly init` and `rly doctor` detect missing, stale, or path-drifted launchd/systemd definitions and repair them idempotently through the platform adapter (rendered-content comparison + atomic rewrite + change-only daemon reload/bootstrap; legacy definitions pointing at `dist/cli/init.js`, a Node binary, or a direct `runtime/refs/...` path migrate to the bootstrap contract without provider/account reconfiguration). `rly status` reports reconciliation state read-only. A service definition never intentionally targets a staged or deleted deployment because it references only the stable bootstrap, which resolves the committed `active` ref at boot.
- Distribution independence: the bootstrap contract is a stable interface that standalone artifacts, installer scripts, Homebrew, or later channels can satisfy (a real built tree at the same layout, or a candidate produced by #35) without changing runtime lifecycle semantics; Wave 4 remains the authority for resolving the committed runtime.
- Platform/migration: existing launchd/systemd definitions migrate to the bootstrap contract on re-init/doctor; no-root per-user service semantics are preserved (never run as root); unsupported platforms stay explicit (never falsely report service registration success).
- Diagnostics/privacy: `rly status`/`rly doctor`/`rly --version` may expose the bootstrap path, service-definition revision + reconciliation state, expected (active-deployment metadata) vs serving (attested `/identity`) build identity, and the same-semantic-version-different-artifact signal; no credential, auth, session, or user-content data ever enters service definitions, the bootstrap script, deployment metadata, or build identity.
- Failure: bootstrap resolution failure (missing/staged/invalid active deployment), reconcile failure, or probation mismatch produce actionable errors (`run rly init`, `run rly update`, `run rly doctor`); no loop, no silent fallback to an uncommitted target, no signal to foreign listeners.
- Acceptance: AT-216, AT-217, AT-218, AT-219, AT-220, AT-221, AT-222, AT-223, AT-224, AT-225, AT-226.

### FR-033 — Incremental streaming transport with stream-safe lifecycle (#120)

- Traces: BR-005, BR-006, BR-008, BR-009, SR-F-037, SR-NF-002, SR-NF-007, SR-NF-009; builds on FR-025/SR-F-029 (#119 fidelity events) and owns Wave 1 streaming transport execution for Anthropic Messages and OpenAI Responses. Provider retry/error semantics (#121) and the native Responses upstream adapter remain out of scope.
- Incremental encoder API: each protocol encoder exposes a per-stream incremental encoder (`createAnthropicIncrementalEncoder`, `createResponsesIncrementalEncoder`) that consumes ONE canonical event at a time and returns ONLY the new wire frames that event produces. Protocol ordering and #119 fidelity events (thinking `signature_delta`, reasoning/tool interleave) are preserved without recomputing prior output; the batch encoders remain the re-encode-then-slice reference and stay byte-identical on golden fixtures.
- O(N) route execution: neither streaming route retains a full `seen` event history to re-encode it; the Anthropic encoder retains only open-block state, and the Responses encoder retains only the aggregate projection (items + per-block text/reasoning/tool-JSON accumulators + open set) required to emit the final `response.completed` payload and persist continuation. Total encoding work is linear in events; retained state is bounded by open blocks / output size, never by event count.
- Backpressure: downstream writable backpressure pauses upstream iteration (the pump suspends at `yield`, so the upstream is not pulled and buffering stays bounded by the readable high-water mark); no frame loss, duplication, reordering, or unbounded buffering. The idle/progress clock is disarmed while suspended on downstream backpressure so a slow consumer never causes a spurious timeout.
- Cancellation/disconnect propagation: client abort/disconnect promptly cancels upstream work through the merged AbortSignal (client disconnect + policy timeouts); no frames are emitted after cancellation/close, and an upstream that ignores abort is stopped at the next pump terminal check.
- Stream timeout policy: a connection/setup timeout (first frame within `setupTimeoutMs`) is separate from the healthy stream lifetime; after the first frame an idle/progress timeout (`idleTimeoutMs`) measures gaps between upstream events while the downstream is writable. There is NO generic whole-request timer, so healthy long-lived agent streams with ongoing deltas are never killed. Timeout emits exactly one in-band structured error frame while the client is still connected.
- Bounded cleanup: iterator (upstream `return()`), encoder `finish()`, timers, client-abort listeners, and the response are cleaned exactly once on completion, error, timeout, disconnect, and teardown; the client abort binding is released when the stream terminates (the handler returns before the stream ends).
- Terminal-event invariant (#J6): a provider stream that ends without a terminal `response-completed` is a TRUNCATED response, never success. The pool executor requires the terminal event before recording a success outcome — natural exhaustion without it records a transient failure (cooldown) and fails closed with a typed error; rotation/replay is allowed only before provider acceptance or client output, exactly like a provider-emitted `response-failed`. Adapters may additionally enforce the invariant; the core no longer depends on them.
- Observability/privacy: stream metrics are metadata only — event count, frame count, backpressure count, duration, terminal kind (completed/error/cancelled/timeout with setup/idle category). Prompts, responses, reasoning text, tool payloads, and credentials are never recorded in diagnostics.
- Acceptance: AT-227, AT-228, AT-229, AT-230, AT-231, AT-232, AT-233, AT-234, AT-235, AT-236.

### FR-034 — Native Responses upstream rail, provider error fidelity, commitment-state retry (#121)

- Traces: BR-005, BR-006, BR-008, BR-009, SR-F-038, SR-NF-002, SR-NF-007, SR-NF-009; builds on FR-025/SR-F-029 (#119 fidelity envelope), FR-033/SR-F-037 (#120 incremental transport), FR-030/SR-F-034 (#124 ECR), FR-031/SR-F-035 (#127 frozen model decision), and #68/#69/#70 routing. Owns the Wave 1 transport-completion gate: a true native Responses upstream rail, provider-native error fidelity, commitment-aware retry/failover, and the conformance/soak/chaos corpus.
- True OpenAI Responses upstream rail: at least one supported provider/access path (OpenRouter direct, exact endpoint contract `POST https://openrouter.ai/api/v1/responses`) wires a first-class Responses request/stream path — NOT Chat-Completions-like semantics. Same-protocol Responses requests route to the native rail whenever the adapter declares the exact Responses endpoint contract (`OpenAiChatAdapter.supportsResponsesNativeRail`); the rail preserves Responses item identity (`item.id`), continuation fields (`previous_response_id`, response id), tool/reasoning item ordering, and #119 opaque artifacts (reasoning `encrypted_content` via `include: ["reasoning.encrypted_content"]`) through provider invocation. Providers without the exact contract keep the cross-protocol translation path and still fail closed on required artifacts (`unsupported-fidelity`). `OpenAI-compatible` is never a sufficient compatibility class: every claimed Responses path identifies the exact endpoint/adapter contract.
- Provider-native error fidelity: `ProviderAdapterError` carries safe structured `ProviderErrorInfo` (status code, code, type, allowlisted bounded message, param, retry-after, rate-limit fields) extracted by `parseProviderError` from a bounded provider body — only allowlisted fields, never the raw body. Both routes map it onto protocol-correct client errors (Responses `{type, code, message, param}`; Anthropic via `translateProviderError` cross-protocol vocabulary) and surface `retry-after`. Generic normalization is the fallback, never the only path. Error redaction is deliberate: credentials, auth headers, secret-bearing bodies, prompts, responses, and reasoning text are excluded by construction and proven by privacy tests.
- Commitment state model: `src/providers/commitment.ts` tracks execution commitment separately from client-visible output with states `not-sent`, `sent-unacknowledged`, `provider-accepted`, `client-output-started`, `tool-boundary`, `unknown`. Adapter/provider rails own evidence of provider commitment (HTTP 2xx/response-started ⇒ `provider-accepted`; tool-call events ⇒ `tool-boundary`; output deltas ⇒ `client-output-started`); routing/retry consumes it.
- Retry/failover policy: rotation/retry happens ONLY when the selected policy can prove the previous attempt did not cross a provider/client/tool commitment boundary — `canRotate` requires `commitment === "not-sent"` in addition to the existing pre-output/budget/outcome rules. A deterministic 4xx rejection is `not-sent` (safe to rotate); a 5xx or network failure after send is `unknown` (conservative no-replay); failure after provider acceptance, during streaming, or at a tool boundary never rotates and never replays. Account failover never changes the frozen physical model (#127). `collectWithSafeRetry` retries only provably-not-sent transport failures.
- Error/retry observability: `retryTrace` exposes ONLY allowlisted categories (provider/access path, adapter id, model id, attempt number, status class, retryability reason, commitment state, terminal reason, outcome) with a fixed key set; `retryReasonOf` classifies from safe metadata only. Never logs credentials, prompt/response/reasoning/tool payload content, raw account identity, or unredacted secret-bearing error bodies.
- Conformance corpus: deterministic fixtures for the Anthropic supported translation path and the OpenAI Responses native rail covering text, streaming, cancellation, tools, parallel/multi-tool continuation, reasoning, opaque continuation artifacts, stop reasons, errors, retry metadata, and disconnect ambiguity — recording expected WIRE semantics (frames), not just canonical event equivalence.
- Chaos/soak verification: failures injected before send, after send/before response, after provider acceptance, during streaming, around tool boundaries, and after client output begins; tests prove NO duplicate tool execution or unsafe replay across retry/failover. Long-session soak combines #120 incremental transport with retry/error handling with bounded encoder state and no state leakage between requests.
- Acceptance: AT-237, AT-238, AT-239, AT-240, AT-241, AT-242, AT-243, AT-244, AT-245, AT-246, AT-247, AT-248.

### FR-035 — Standalone runtime artifacts as the primary production distribution (#35)

- Traces: BR-008, BR-009, SR-F-039, SR-NF-002, SR-NF-014; builds on FR-032/SR-F-036 (#94 stable bootstrap + exact build identity) and FR-023/SR-F-027 (#73/#92 immutable deployments + transactional activation #93). Standalone RLY-owned runtime artifacts are the PRIMARY production distribution: they install and run WITHOUT a user-provisioned Node/npm/pnpm toolchain or a source checkout. External Node is a development concern only; npm/Homebrew may later wrap or point to the SAME canonical artifact lineage and must never build semantically different runtime bytes under the same RLY build identity.
- Standalone artifact contract: a self-contained package per supported target bundles the compiled runtime (`dist`), bundled runtime dependencies (`node_modules` prod install), the exact pinned Node runtime (`bin/node`), licenses/notices, the release-candidate manifest (`rly.json`), the exact #94 build identity (`rly-build.json`), and artifact metadata (`rly-artifact.json` incl. target platform, bundled Node version, artifact digest, file list). `rly` remains the stable user-facing command; the internal runtime layout is RLY-owned and deterministic (`rly`, `bin/node`, `dist`, `node_modules`, `package.json`, `LICENSE`, `docs/third-party-notices.md`, `rly.json`, `rly-build.json`, `rly-artifact.json`). Normal execution never depends on the source repository, the pnpm workspace, or the invoking working directory (the self-locating `rly` launcher resolves its own tree and runs the bundled Node).
- Platform matrix: first-class artifacts are produced for `darwin-arm64`, `darwin-x64`, `linux-x64`, and `linux-arm64` when the target is buildable/testable; every target is EXPLICITLY declared `supported` (built AND smoke-tested on a qualified runner) or `experimental` (built deterministically; smoke-testing requires an unprovisioned runner) with a reason — a target never silently reuses another target's artifact or Node bytes (each target maps to its own Node distribution tarball).
- Positive package allowlist: the artifact ships ONLY compiled runtime code, bundled runtime dependencies, required static assets/templates, licenses/notices, and explicit bootstrap resources; `.git`, developer config, `.env`, local `~/.rly` state, dependency test artifacts, `.bin` shims and pnpm metadata (which may carry absolute store paths), credentials, reports, private snapshots, and unrelated workspace files are excluded; packaging FAILS on any unexpected file or forbidden marker, and RLY-owned files are content-scanned for secrets (same rules as the privacy scan).
- Canonical build/version identity: artifact production consumes the #94 exact build identity contract (`dist/rly-build.json` generated by `scripts/write-build-identity.mjs`) and emits semantic version, commit/source revision, build ID, release channel, target platform, bundled Node version, and the artifact digest CONSISTENTLY across `rly-build.json`, `rly.json`, and `rly-artifact.json`; the version authority is `RLY_RELEASE_VERSION` (release builds) → exact git tag → `package.json`, resolving the `0.1.0` vs release-tag ambiguity — a build whose `dist/rly-build.json` version diverges from the resolved release version FAILS. Two artifacts with different bytes cannot claim the same artifact digest (content-addressed over the exact tree, same rules as the #92 immutable store).
- Reproducible/clean build: artifacts are built from a clean compiled tree with the frozen lockfile and pinned Node version (`scripts/standalone/node-version.json`); deterministic sorted file lists, fixed source-date-epoch mtimes, uid/gid 0, no owner names, no absolute developer paths/tokens/home dirs, and no pnpm store paths are embedded; identical inputs produce byte-identical tarballs and digests.
- CI/distribution: `.github/workflows/standalone-artifacts.yml` builds the full matrix on release publication (the release tag is the canonical version input) or manually, verifies every artifact (allowlist/absence/identity/digest), smoke-tests the supported CI target (`linux-x64`, `rly --version` from the unpacked artifact), QUALIFIES the exact artifact bytes and publishes the signed release supply chain (see FR-036/FR-037/FR-038), and uploads the tarballs + sha256 + manifest + signing/SBOM/provenance/channel assets; artifacts are attached to the GitHub Release. Installer/updater UX (#129) remains out of scope.
- Failure: any allowlist violation, secret content, identity split, digest mismatch, node version mismatch, or smoke failure aborts packaging/verification with an actionable error; skipped or unprovisioned target verification is never reported as passed.
- Acceptance: AT-249, AT-250, AT-251, AT-252, AT-253, AT-254, AT-255, AT-256, AT-257, AT-258.

### FR-036 — Publish a canonical release manifest and signed channel metadata (#128)

- Traces: BR-008, BR-009, SR-F-040, SR-NF-002, SR-NF-014; builds on FR-035/SR-F-039 (#35 standalone artifacts) and FR-032/SR-F-036 (#94 exact build identity). The release supply chain turns the #35 artifact lineage into authenticated, traceable, qualification-tested release bytes.
- Canonical release manifest (`rly-release.json`, `scripts/release/manifest.mjs`): binds product version, release channel (beta/stable), source commit, build ID, every supported target, artifact filename/size/sha256/content-addressed digest, bundled runtime version, state/protocol compatibility, required signatures/attestations, and workflow/toolchain inputs — consistent with the #94 exact build identity; a manifest whose identity diverges from the packaged `rly-build.json` fails publish and verification.
- Signed channel metadata (`rly-channel-<channel>.json` + `.sig`, `scripts/release/channel.mjs`): a small signed layer (Ed25519; private key ONLY the repository secret `RLY_RELEASE_SIGNING_KEY`, public key committed at `scripts/release/signing-public-key.pem`) maps beta/stable to exact release/build/artifact digests — TUF-style separation of metadata from artifacts. Explicit semantics: rollback protection (monotonic per-channel `version`; lower-than-observed is refused), staleness (`updatedAt` + `staleness.maxAgeDays`; stale is refused), freeze (explicit `freeze.frozen` blocks activation beyond the frozen snapshot). The updater verifies the signature and never trusts a mutable GitHub `latest` redirect alone.
- Release immutability (`scripts/release/immutability.mjs`): published bytes/digests are never silently replaced under the same release identity — republishing the same version with different digests fails and byte replacement of published assets is detected against the signed metadata.
- Beta vs stable gates are machine-readable: beta may publish with documented experimental qualification gaps (`experimental-gaps`); stable requires `qualified` status for every advertised target plus all authenticity/compatibility/privacy/Wave-4-integration gates — beta evidence never masquerades as stable qualification.
- Acceptance: AT-259, AT-262, AT-265, AT-266, AT-268, AT-269, AT-271.

### FR-037 — Generate SBOM and provenance from the exact artifact lineage and enforce platform authenticity (#128)

- Traces: BR-008, BR-009, SR-F-041, SR-NF-002, SR-NF-014; builds on FR-036/SR-F-040 (#128 release supply chain) and FR-035/SR-F-039 (#35 artifacts).
- SBOM per production artifact (`rly-<target>.sbom.json`, `scripts/release/sbom.mjs`): generated from the ACTUAL packaged bytes (RLY components, bundled pinned Node, third-party `node_modules` packages; deterministic, SPDX-2.3-style) and references the EXACT artifact digest via `artifactRef` — evidence is attached/referenced to the exact digest and is a SIBLING release asset (never embedded, preserving the TUF-style metadata/artifact separation).
- Build provenance/attestation (`rly-provenance.json`, `scripts/release/provenance.mjs`): ties every artifact digest (tarball sha256 + content-addressed tree digest) to the exact source revision and the release workflow/toolchain inputs (SLSA-flavored, secret-free).
- Platform authenticity: macOS production artifacts must pass the documented code-signing/notarization/stapling verification gate (`codesign --verify --deep --strict` + `stapler validate` on a provisioned macOS host with the Apple certificate secrets) before stable promotion; Linux authenticity uses the per-artifact Ed25519 signature (`<tarball>.sig` over `sha256:<hex>`) + the release manifest trust chain. A missing required platform signature BLOCKS promotion for that target (machine-readable via the qualification gate).
- Privacy/public boundary: manifests, SBOMs, provenance, channel metadata, signatures, and qualification records contain public build metadata only — never credentials, tokens, local state, private source-history content, prompts, responses, or account identity.
- Acceptance: AT-260, AT-261, AT-265, AT-268, AT-271.

### FR-038 — Qualify the exact artifact bytes as the publication authority (#128)

- Traces: BR-008, BR-009, SR-F-042, SR-NF-002, SR-NF-014; builds on FR-036/SR-F-040 and FR-037/SR-F-041 (#128) and the #94/#92/#73 runtime lifecycle contracts.
- Exact-byte qualification (`rly-qualification.json`, `scripts/release/qualify.mjs`, `scripts/release/qualification.mjs`): qualification installs and exercises the SAME artifact digest later published — no rebuild between qualification and publication. The matrix covers clean install, `rly --version`/build identity, permissions, platform signing, runtime readiness (doctor ok), update handoff contract (the #73/#92/#94 candidate contract), `rly init`/service registration, and uninstall (self-containment).
- Gate semantics: a gate is `passed` only when it executed against the exact bytes; `skipped`/`not-run` is never passing evidence; gates are host-aware (a target the runner cannot execute is recorded `skipped` with a reason). A target without qualification evidence is NOT stable-qualified, and a missing/failed required signing, SBOM/provenance, compatibility, or platform qualification gate prevents stable promotion (`qualificationBlocksStable`).
- Workflow hardening: release-critical third-party GitHub Actions are pinned to reviewed immutable commit SHAs and release workflows use least-required `GITHUB_TOKEN`/release permissions with no npm credentials (`scripts/check-release-supply-chain.mjs` is part of the release gate `pnpm test:release`).
- Acceptance: AT-263, AT-264, AT-265, AT-267, AT-270, AT-271.

### FR-039 — Verified installer, updater, and uninstaller acquisition lifecycle (#129)

- Traces: BR-008, BR-009, SR-F-043, SR-NF-002, SR-NF-014; builds on FR-038/SR-F-042 (#128 signed release supply chain), FR-035/SR-F-039 (#35 standalone artifacts), FR-032/SR-F-036 (#94 stable bootstrap), FR-023/SR-F-027 + FR-027/SR-F-031 (#73/#92/#93 immutable deployments + transactional activation). The end-user acquisition lifecycle: verified remote download, bootstrap install, first-install vs update boundary, channel policy, uninstall, and repair. It does NOT own runtime drain/activate/rollback (Wave 4 #92/#93).
- Verified remote acquisition: `rly install`/`rly update --channel beta|stable|current` resolve the selected channel/platform/arch through the #128 signed channel metadata (`rly-channel-<channel>.json` + `.sig`, Ed25519 verified against the committed release public key) and the signed canonical release manifest (`rly-release.json` + `.sig`); download the EXACT immutable artifact from GitHub Releases/approved origin; and verify, BEFORE any install/staging mutation: channel rollback (monotonic version vs the durable highest observed), staleness (`updatedAt` + `staleness.maxAgeDays`), freeze (explicit marker), exact platform target + channel qualification gate (stable requires `supported` + `qualified`; beta permits documented `experimental-gaps` but never `not-qualified`), artifact sha256/size, the Ed25519 digest statement (`<tarball>.sig` over `sha256:<hex>`), and the unpacked-tree content-addressed digest equals the manifest `artifactDigest` with `rly-build.json`/`rly.json`/`rly-artifact.json` identity consistency. Wrong platform, stale/invalid metadata, digest mismatch, missing/invalid signature, or unknown release identity FAILS before any mutation with an actionable secret-free error (`src/installer/*`).
- Bootstrap installer: `scripts/install.sh` is a minimal, audited, fail-closed POSIX-sh bootstrap for clean per-user macOS/Linux installs that requires no source checkout, npm, pnpm, or user Node and never requires sudo. It downloads the signed channel metadata + release manifest + exact artifact into a private staging dir, verifies the tarball sha256 against the published `.sha256` AND the digest embedded in both signed metadata documents, verifies the artifact Ed25519 digest statement with the committed release public key (OpenSSL 3 `pkeyutl -rawin`, auto-detected via PATH/Homebrew kegs/`RLY_OPENSSL_BIN`; unsupported/absent openssl fails closed with an actionable message), then unpacks and runs `rly install --artifact <tarball> --metadata-dir <staging>` which re-verifies the FULL signature chain (canonical-JSON Ed25519 signatures + tree digest + identity) before any install mutation. Nothing executes or installs unverified content; `RLY_RELEASE_PUBLIC_KEY_FILE` documents the operator/mirror trust-anchor override (default is the committed RLY release key).
- Stable local ownership/layout: the install registers the #94 RLY-owned bootstrap (`<control-plane>/bootstrap/rly-gateway`) as the ONLY service execution identity (never `dist/cli/init.js`, never an incidental Node path, never a direct `runtime/refs/...` path), materializes the initial committed `active` deployment from the #35 canonical artifact tree, installs the deterministic per-platform layout (`~/.rly` control plane, `~/.rly/bootstrap/rly-gateway`, user launcher symlink `~/.local/bin/rly` → bootstrap, no sudo), and refuses to overwrite a foreign/unowned path (the launcher symlink fails closed on a foreign occupant).
- First install vs update boundary: first install runs/guides `rly init` (per-user service registration + initial committed deployment) and then `rly config`; update acquisition (`rly update --channel …`) installs and verifies a candidate and returns a `VerifiedCandidate` to Wave 4 — it NEVER changes the #92 `active` reference and NEVER restarts the resident service (INSTALL != ACTIVATE). `--install-only` stops after staging for the local candidate path too; a later plain `rly update` resumes activation.
- Updater/channel policy: `rly update` selects `beta`/`stable`/`current` channel policy (explicit switch is auditable in `<control-plane>/installer/acquisition-log.json`; `current` keeps the installed build identity's channel, `dev`/unknown → `stable`), inspects the current exact build identity, resolves a newer qualified artifact, reports current exact build, candidate exact build/channel, verification state, and pending/activation status; downgrade/rollback respects signed metadata (monotonic version) and Wave 4 state compatibility.
- Uninstall lifecycle: `rly uninstall` stops/disables/removes ONLY RLY-owned launchd/systemd definitions (through the existing adapters), removes ONLY RLY-owned product artifacts (`bootstrap/`, `runtime/`, `installer/`, installation + update state), removes the user launcher symlink ONLY when it is RLY-owned, and PRESERVES `~/.rly` config/accounts/credential/control-plane/overlay/log state by default. `rly uninstall --purge --yes` is an explicit destructive removal of the whole RLY control plane that requires unambiguous intent (`--purge` alone refuses) and never removes unrelated native Claude/Codex configuration.
- Repair/reinstall: re-running `rly install`/`rly init` on an existing installation verifies ownership/build identity (installation record + committed active deployment) and repairs missing/stale bootstrap + service definitions idempotently (a valid committed deployment is never rewritten); a foreign/unowned path or process is never overwritten; a differing verified version is reported as an update handoff (`rly update` owns staging/activation).
- Platform behavior: the supported matrix matches #35/#128 (darwin-arm64, darwin-x64, linux-x64, linux-arm64); unsupported OS/arch/service-manager environments fail explicitly with actionable messages, never partial success.
- Privacy/security: installer/update logs and durable records (`acquisition-log.json`, `observed-channels.json`, `VerifiedCandidate`) carry channel/version/build/digest/platform/path/status metadata only — never credentials, management/launch tokens, auth headers, email/raw account identity, prompts, responses, or reasoning text; downloads are private (`0600`/`0700`) and nothing is committed to git.
- Qualification integration: the #128 exact-byte qualification matrix gains a `verified-install` gate that exercises the installer/updater verification machinery against the EXACT artifact bytes (positive verified + tampered-refused) on the same packaged lineage; the real release signatures are verified post-publish by `verify-release.mjs`.
- Acceptance: AT-272, AT-273, AT-274, AT-275, AT-276, AT-277, AT-278, AT-279, AT-280, AT-281, AT-282, AT-283, AT-284, AT-285.

### FR-040 — Materialize runtime deployments preserving the pnpm symlink layout (#144)

- Traces: BR-008, BR-009, SR-F-044, SR-NF-002/014; builds on FR-032/SR-F-036 (#94 stable bootstrap) and FR-023/SR-F-027 (#73/#92 immutable deployments).
- Preconditions: an installed runtime tree (or a verified #35 artifact tree) is being materialized into the #92 immutable store as an initial or staged deployment.
- Behavior: `materializeRuntimeTree` (and the #92 candidate-installer copy path) PRESERVES pnpm's relative in-tree `node_modules` symlink structure — `node_modules/<pkg>` and `node_modules/.pnpm/<pkg>@<ver>/node_modules/<dep>` links are copied AS LINKS with their original relative targets, so Node's transitive resolution (e.g. fastify→avvio) works on the deployed tree. Dereferencing these links collapses the virtual-store sibling layout and breaks real dependency graphs (`Cannot find module '<dep>'`); dev-only trees without transitive deps mask the bug. Only safe relative in-tree links are kept: absolute or escaping symlinks are refused (deployments stay self-contained). pnpm metadata files (which may carry build-machine store paths), `.bin` shims, and dependency test artifacts are never shipped (mirror of the #35 pack rules). The allowlisted top-level entries (package.json, dist, node_modules, LICENSE, rly.json) and the absence semantics are unchanged.
- Digest identity: the immutable artifact digest hashes relative in-tree symlinks deterministically as `link:<target>` (the same rule as the #35 artifact digest) so symlinked candidates and deployments keep deterministic content-addressed identity; special files still fail closed.
- Failure: an absolute/escaping symlink, a special file, or a non-allowlisted top-level entry fails materialization/install with an actionable error before any immutable mutation.
- Acceptance: AT-286.

## Unresolved questions

- Exact quota-aware strategy behavior after live quota evidence is pinned.
