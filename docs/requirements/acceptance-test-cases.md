# Acceptance Test Cases Catalogue

These are acceptance scenarios, not hand-maintained implementation test names. The RTM links requirements to scenario IDs; executable test paths are added as evidence when implemented.

| ID | Acceptance scenario | Expected result |
| --- | --- | --- |
| AT-001 | Start/reuse the gateway on deterministic ports | Only an attested compatible instance is reused; data and management listeners are loopback-only |
| AT-002 | Present foreign/stale listeners and interrupted runtime state | Fail closed without signaling foreign PIDs, auto-incrementing ports, or losing recoverable state |
| AT-003 | Create/update/disable a provider definition | Valid versioned mutation commits atomically and publishes a new policy revision |
| AT-004 | Submit unauthorized, stale, incompatible, or terms-incomplete provider mutations | Mutation is rejected with no partial state or secret-bearing error |
| AT-005 | Import a supported Codex credential | Project-owned restrictive record is created and source store remains byte-identical |
| AT-006 | Import malformed, changing, oversized, or unsupported source data | Import fails, temporary files are removed, and no secret appears in output/log/audit |
| AT-007 | Complete valid OAuth authorization | PKCE/state/exact callback validation succeeds and generation one is stored atomically |
| AT-008 | Replay/mismatch/expire state, collide callback, cancel, or return malformed OAuth data | Flow fails closed and leaves no usable partial credential |
| AT-009 | Trigger concurrent refresh for one generation | One upstream refresh/commit occurs and callers receive the committed generation |
| AT-010 | Complete stale/failed/interrupted refresh | Older result cannot overwrite newer generation; last valid record remains recoverable |
| AT-011 | Logout/revoke an account | New requests exclude it and no usable active/temp/backup project credential remains |
| AT-012 | Corrupt active credential or interrupt atomic replacement | Recovery selects only a schema-valid non-revoked generation or marks account unready |
| AT-013 | Pause/resume/pin account and inspect status | Versioned state changes apply; DTO contains pseudonym/readiness only |
| AT-014 | Use terms-gated account before/after acknowledgement revision changes | Unaccepted/stale acceptance is ineligible; current explicit acceptance enables eligibility |
| AT-015 | Create valid profile and pool | References, roles, strategy, affinity, retry budget, and ownership validate atomically |
| AT-016 | Submit duplicate/invalid/cross-provider/stale pool configuration | Mutation fails without changing current policy |
| AT-017 | Select from fixed eligible candidates under manual/RR/fill-first | Result and decision reason are deterministic and bind one credential generation |
| AT-018 | Include paused/expired/unready/cooling/incompatible/unaccepted candidates, plus quota-exhausted accounts that are still cooling | Every ineligible candidate is excluded before strategy evaluation; exhausted accounts whose cooldown has elapsed are recovery probes |
| AT-019 | Receive eligible pre-output auth/quota/transient failure | Transactional outcome update occurs and bounded rotation follows policy; quota success restores healthy and probe failure extends cooldown |
| AT-020 | Receive failure after first response byte or tool event | Failure propagates; no second account/provider invocation occurs |
| AT-021 | Run Claude text, stream, tool, thinking, helper, usage, and stop flows | Supported semantics and event order are preserved through the selected route, including a Codex OAuth Claude profile and a ClinePass Claude profile |
| AT-022 | Cancel Claude request and run concurrent profiles/sessions | Upstream aborts; route/account state does not contaminate sibling requests |
| AT-023 | Run Codex Responses text/function/reasoning/usage/continuation flows | Responses semantics are preserved without Anthropic flattening |
| AT-024 | Cancel/fail Codex Responses request or encounter unknown required event | Upstream aborts or route becomes unready; no silent loss/substitution occurs |
| AT-025 | Authenticate management CLI and browser | Separate bearer works; single-use fragment exchanges for a bounded secure cookie; fragment is removed from browser history and never appears in Referer, server access logs, or error output |
| AT-026 | Attempt cross-origin, invalid-CSRF, expired/replayed bootstrap, stale-version, logout/shutdown session use | Every attempt fails and no mutation occurs |
| AT-027 | Inspect status, trace, logs, audit, error, UI, export, and package | No raw credential, account identity, prompt, response, or tool argument is present |
| AT-028 | Adapt a substantial upstream module | Exact source/hash/license/path/classification/notice and verification owner are recorded in `docs/provenance.md`, `docs/provenance/adaptation-matrix.json`, and `docs/third-party-notices.md` |
| AT-029 | Bootstrap public baseline, then promote releases | Baseline snapshot contains approved public files in one commit and has no private `dev` ancestor; later reviewed promotion preserves normal ancestry |
| AT-030 | Execute release/traceability gate | Must requirements have evidence; verify/privacy/license/migration/recovery/package/clean-install gates pass |
| AT-031 | Operate every supported UI workflow with keyboard and accessibility inspection | Focus is visible/predictable; controls have programmatic labels; status/errors are textual; desktop/mobile layouts remain usable |
| AT-032 | Apply and interrupt retention/deletion policy across every durable class | Expired data becomes unreachable and is removed per policy; cleanup resumes idempotently after interruption; no revoked/expired secret becomes active |
| AT-033 | Launch `rly <profile>` as Claude Code alias | Named profile launches Claude Code with a child token; reserved commands stay reserved; unknown profile fails closed; `rly run codex` stays Codex CLI; global Claude/Codex config is unmodified |
| AT-035 | Bootstrap and operate the persistent per-user runtime service | `rly init` creates/validates the durable `~/.rly` home and registers the user service idempotently; generated macOS LaunchAgent and Linux `systemd --user` definitions and lifecycle commands are automated/platform-gated; the resident runtime starts after init without `rly gateway start`, stays alive after the last Claude/Codex child exits, and is reused by `rly <profile>` and concurrent launches with independent launch/session leases; a crash is restarted by the service manager and stale records recover without killing foreign listeners; a foreign/unattested listener fails closed; `/identity` distinguishes compatible vs incompatible resident runtime; closing a child releases only its lease; explicit shutdown revokes sessions, closes boundedly, and cleans owned artifacts; no credential/token/account identity appears in service definitions, logs, or diagnostics |
| AT-034 | Query the provider model intelligence registry (#67) | Exact access-provider identity, upstream model id, and model family are distinct; the same upstream id through two access providers stays separate and never satisfies cross-provider lookup; one aggregator exposes many families without parallel registries; compatibility state is typed and separate from raw capability support; discovery produces proposals without mutating trusted evidence; registry document revision changes are explicit and migration-covered |
| AT-038 | Refresh a provider catalogue into a propose-only drift report (#23 / BL-042) | A provider catalog source produces a normalized discovery snapshot without registry mutation access; identical trusted registry + identical snapshot yield a stable unchanged proposal on repeated runs; new/removed/model-family/reasoning/declared-capability drift each produce deterministic report entries; a large aggregator snapshot stays deterministic and never auto-activates every model; refresh cannot change trusted registry evidence, profile tier mappings, or `/v1/models` output; missing provider models are reported as drift with no silent substitute; proposal artifacts can reference #24 compatibility evidence but never treat skipped/unrun tests as a pass; `rly admin models refresh|proposals` surfaces proposals with a `trusted: false` marker without representing them as selectable/usable models |
| AT-039 | Inspect refresh errors, proposal output, and persisted artifacts (#23) | Upstream catalog errors are actionable and privacy-redacted; credentials, authorization headers, and account identity are absent from proposal/log/artifact output; malformed persisted artifacts fail closed rather than being trusted |
| AT-044 | Run deterministic model capability selection (#68) | Given an access provider, optional preferred family, optional exact pin, required protocol capabilities, and a reasoning intent, the engine selects one eligible physical model from the trusted registry before any account selection; required capabilities are evaluated before ranking using the existing capability primitives; compatibility filtering respects `VERIFIED`/`EXPERIMENTAL`/`BROKEN`; identical evidence + policy + request inputs produce the same selected model and the same decision reason; the selection trace contains only allowlisted model/capability/policy metadata and no prompts, responses, credentials, or account identity |
| AT-045 | Select a model target, then an account (two-stage boundary) | Model selection completes before `RouteSelector` account selection; the chosen physical model is frozen in the effective request/route so account retry/failover cannot alter it; route traces carry the model-selection decision alongside the account decision; existing pool eligibility, affinity, quota, pin order, and credential generation behavior stays unchanged |
| AT-046 | Fail closed with an actionable reason for every no-candidate case | Missing exact evidence, missing provider evidence, unsupported required capability, unsupported reasoning intent, rejected compatibility state, and empty provider/family tiers each produce a distinct typed reason that maps deliberately onto the existing profile/gateway error contract rather than a generic internal error; no silent substitution or downgrade occurs |
| AT-047 | Pin an exact physical model through the capability engine | An exact model pin resolves to the exact trusted registry entry without rerouting to another model, while still validating required capabilities and compatibility; `BROKEN` pins are rejected and `EXPERIMENTAL` pins act as an explicit opt-in for that exact model; unknown pins fail closed |

| AT-053 | Translate canonical reasoning intent into provider-native controls (#70) | The provider-owned translation boundary (`resolveReasoning(request, capability)`) deterministically maps `OFF`/`ECONOMY`/`BALANCED`/`DEEP`/`MAXIMUM`/`AUTO` onto the selected model's reviewed control shape from registry `ReasoningCapabilityEvidence` (discrete 6-level, 2-level, binary, adaptive, token-budget, unsupported). Exact same-family source effort is preserved (`exact`); cross-provider/fewer-level mappings are deterministic and produce explicit mapping metadata (canonical intent, effective native control, `exact|normalized|downgraded|default`, fallback reason); no requested explicit level is silently downgraded or disabled; unsupported or unrepresentable explicit intents fail closed unless a documented best-effort policy was explicitly enabled |
| AT-054 | Preserve source reasoning fidelity through decoding (#70) | Anthropic `thinking.type` and the documented additive `effort` field, plus OpenAI Responses `reasoning.effort`, decode into the canonical `ReasoningRequest` preserving `sourceMode`/`sourceEffort`; unknown additive fields stay recorded as ignored; the pinned supported-baseline fixture drives the decoder (no assumed field names); `high`/`xhigh`/`adaptive`/binary/token-budget are never collapsed into one boolean |
| AT-055 | Wire reasoning intent through routing and secret-free diagnostics (#70) | The deterministic translation result rides the route decision and the `/v1/route-traces` diagnostics with allowlisted fields (requested/canonical intent, effective control, mapping kind, fallback reason) and never includes reasoning text, prompts, responses, credentials, or account identity; the OpenRouter/direct adapter emits the provider-native parameter from the translation result and no longer collapses `enabled` and `adaptive` |
| AT-056 | Gate reasoning-with-tools through #68 evidence (#70) | An explicit non-`OFF`/non-`AUTO` reasoning intent combined with tool use requires `reasoningWithTools` evidence on the selected model; unsupported coexistence fails closed with the typed `reasoning-unsupported` reason instead of degrading the run |

| AT-065 | Compose the durable RLY Claude configuration overlay (#74) | RLY launches Claude with `CLAUDE_CONFIG_DIR` pointed at one durable private namespace under the RLY user state root (`<control-plane>/claude`, `~/.rly/claude` by default) with explicit ownership/precedence rules; directories are `0700` and files `0600`; no fresh throwaway empty config directory is used for the normal interactive launch contract; overlay files and RLY session/history state survive child exit and later RLY launches; composition uses a typed allowlist and never an unrestricted recursive copy of unknown Claude config files |
| AT-066 | Preserve native settings as input without modifying the native file (#74) | A native user setting unrelated to RLY (and the native default model) remains effective in an RLY-launched session via the composed overlay; native `env` keys that conflict with the RLY child-only gateway contract (`ANTHROPIC_BASE_URL`, `ANTHROPIC_AUTH_TOKEN`, `ANTHROPIC_API_KEY`, `OPENAI_*`, `CODEX_*`) are stripped from the overlay; the native `~/.claude/settings.json` file hash/content is unchanged after RLY launch and `/model` activity |
| AT-067 | Expose user agents/skills/commands to RLY sessions (#74) | Native user-level `agents/*.md`, `commands/*.md`, and `skills/**` fixtures are available to the RLY-launched Claude session according to the supported client layout via one-way refresh copies; unrelated files (unknown extensions, `plugins/cache`, `plugins/repos`, `history`, runtime state) are never copied; native files stay byte-identical |
| AT-068 | Preserve plugin enablement without copying credentials (#74) | `plugins/config.json` enablement declarations (`enabledPlugins`/`marketplaces`) are carried into the overlay; credential-bearing keys (`oauthAccounts`, token-like keys) and plugin cache/repos are never copied; RLY never copies native login/API credentials into overlay config to satisfy Claude; the plugin cache/runtime-state difference is documented |
| AT-069 | Isolate RLY-only model persistence from plain Claude (#74) | Selecting an RLY-only gateway projection model with persistent `/model` behavior (Enter/direct selection) changes only RLY-owned overlay model state; the native plain-Claude `model` default and file hash remain unchanged; a subsequent plain `claude` launch does not receive `ANTHROPIC_BASE_URL`, the RLY auth token, an RLY projection model id, or RLY-only available-model configuration; session-only `s` selection also behaves normally |
| AT-070 | Retain RLY session/model state and pick up native changes (#74) | A subsequent RLY launch retains its previously selected RLY model/session state when native input is unchanged; when native settings change, the overlay re-composes one-way while RLY-only projection model state is preserved; RLY session history/cache is no longer unconditionally destroyed at every normal exit; project-local `.claude` files remain discovered through normal working-directory behavior |
| AT-071 | Run concurrent RLY Claude sessions without native races (#74) | Two concurrent RLY Claude sessions never corrupt one another or the native config through whole-file copy-back/restore; RLY's own overlay writes are atomic and deterministic from native input (converging without locks), and an unchanged native input is not rewritten, so a sibling's `/model` write into the shared overlay settings survives; no post-exit “save old global model, rewrite it back” logic exists |
| AT-072 | Keep overlay creation, diagnostics, and permissions privacy-safe (#74) | Overlay creation/migration uses private permissions and persists no RLY launch/management/provider credential secrets; native Claude config changes made between RLY launches are picked up per the documented refresh/precedence policy; `rly status` reports overlay paths/version/composition status only and never dumps user settings, agent prompts, plugin content, session transcripts, or credentials |

| AT-073 | Discover the configured RLY model universe through the gateway (#72) | An authenticated `GET /v1/models` on the gateway listener returns the Anthropic-compatible discovery shape (`data[].type/id/display_name/created_at`, `has_more`, `first_id`, `last_id`) with `limit`/`before_id`/`after_id` pagination; the instance bearer serves the universe derived from the current policy and a launch-session token serves that session's pinned universe; unauthenticated requests are rejected; the body contains no credentials, authorization headers, account identity, prompts, or responses |
| AT-074 | Enable gateway model discovery child-only for RLY-launched Claude (#72) | RLY-launched Claude children receive `CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY=1` while the parent/global environment is unchanged; every discoverable id satisfies the Claude Code discovery filter (begins with `claude` or `anthropic`) as a regression canary; native settings cannot silently disable the flag in an RLY session and a plain `claude` launch never inherits RLY discovery env or projection ids |
| AT-075 | Resolve a projection id through the explicit reverse mapping (#72) | A `claude-rly-*` id resolves to one exact trusted registry access-provider/model target and the pinned provider pool; routing never parses id string segments; the same id always resolves to the same canonical target for the session |
| AT-076 | Keep same-model-different-provider projections distinct (#72) | The same upstream model exposed through two access providers appears as two distinct selectable targets with distinct ids and distinct display labels; selecting each routes through the correct provider's pool and account selector |
| AT-077 | Project only trusted models from enabled configured bindings (#72) | Discovery returns only models from providers that are enabled in the active policy with an explicit provider→pool route for the session (the profile's pool or a single eligible default pool per provider); `BROKEN`, unreviewed, revoked, unreachable, or proposed (#23) models are never returned as selectable entries |
| AT-078 | Gate compatibility states in discovery (#72) | Default discovery exposes `VERIFIED` targets only; `EXPERIMENTAL` targets appear only with the explicit `gateway.modelDiscovery.experimentalModels` config opt-in; `BROKEN` targets never appear even with the opt-in |
| AT-079 | Discover and route across Codex + ClinePass + DeepSeek without hand-mapping (#72) | A session configured with Codex, ClinePass, and DeepSeek bindings discovers models from all allowed bindings; selecting `GPT-5.6 Sol (Codex)` routes to the Codex pool, `GPT-5.6 Sol (ClinePass)` routes to the ClinePass pool, and `DeepSeek V4 Pro (DeepSeek)` routes to the DeepSeek pool, with account selection remaining inside each pool; an aggregator (ClinePass) exposes multiple model families and each projected exact model retains its correct family/provider identity |
| AT-080 | Pin the session universe against policy/registry drift (#72) | A projection id issued to an active session keeps resolving to the same canonical target and pool even after the control-plane policy changes (e.g. a new provider becomes eligible); the session's discovery output is unchanged; a model removed or marked ineligible in a newer registry revision fails explicitly rather than silently substituting another model |
| AT-081 | Fail closed on unavailable projection targets (#72) | An unknown, removed, revoked, or BROKEN selected projection target returns an actionable typed error (`model-unavailable`/`capability-rejected` with the typed reason) instead of switching models/providers silently |
| AT-082 | Pass #68 capability and #70 reasoning validation for projected exact models (#72) | An exact projected model selection runs the deterministic #68 exact-selection and #70 reasoning translation stages before pool/account selection; unsupported capabilities or untranslatable reasoning intents fail closed with the typed reason; route traces show projection id/display as allowlisted metadata plus the exact model-selection and account decisions, secret-free |
| AT-083 | Keep RLY projection selection isolated from plain Claude launches (#72) | An RLY-only `claude-rly-*` model selected via `/model` persists only inside the RLY overlay; a later plain `claude` launch reads native settings only and never receives RLY gateway env, auth, or projection ids; concurrent RLY sessions cannot race by rewriting/restoring one global `model` setting |

| AT-057 | Represent and parse logical model tiers (#69) | `haiku`, `sonnet`, `opus`, and `fable` are typed logical tiers separate from physical model ids; parsing is case-stable and rejects physical ids/unknown names; tier resolution receives both access-provider context and model-family/parent-model context when available |
| AT-058 | Resolve a tier for a direct single-family provider (#69) | A tier resolves only to an eligible trusted model under that provider (derived deterministically inside the provider+family when no reviewed mapping exists); a provider without an eligible same-family target and without explicit fallback policy fails closed; no silent cross-provider substitution |
| AT-059 | Keep aggregator tier resolution in the parent model family (#69) | ClinePass + Terra-family parent + `fable` resolves to the verified Sol-family candidate (not Anthropic/DeepSeek candidates); ClinePass + DeepSeek V4 Flash-family parent + `fable` resolves to the verified V4 Pro-family candidate; ClinePass + Anthropic-family parent + `fable` stays in the Anthropic family per its reviewed mapping |
| AT-060 | Fail closed and gate explicit fallback scopes (#69) | When no eligible same-family target exists and fallback is disabled, resolution fails closed with a specific `tier-unavailable` reason; cross-family and cross-provider fallback occur only when explicitly enabled (cross-provider requires an explicit provider list) and are visible in the decision trace |
| AT-061 | Validate user overrides and reviewed mappings (#69) | A user override can pin a tier target for a provider/family, but unknown/unverified/broken targets are rejected (`override-rejected`); a reviewed mapping entry without trusted/compatible evidence fails closed (`mapping-invalid`) instead of silently resolving elsewhere |
| AT-062 | Keep the effective tier mapping stable (#69) | The effective tier mapping is stable for an active session/policy revision: the frozen reviewed mapping and per-profile overrides are immutable per revision, resolution records the mapping/registry revisions, and a background catalog refresh (#23) cannot silently change trusted tier mappings mid-session |
| AT-063 | Feed tier output into the two-stage boundary (#69) | The resolved tier target passes #68 capability/compatibility validation and #70 canonical reasoning, then the existing account selector chooses the account/credential; tier resolution itself never chooses credentials/accounts and never emits provider-native fields like `reasoning_effort` |
| AT-064 | Surface tier decisions in secret-free traces and preserve role behavior (#69) | Route traces show requested tier, provider/family context, selected physical target, mapping source, and fallback reason without prompt/credential/account identity; existing `primary`/`fast`/`reasoning` profile behavior and helper mapping remain backward-compatible; the supported Claude Code baseline's native `fable` alias behavior remains classified by #24/#71 |

| AT-048 | Run `rly config` from an arbitrary working directory after `rly init` (#66) | The durable `~/.rly` installation record supplies the configuration, so no local `gateway.config.toml` is required on the normal installed path; explicit `--config` and the CWD file remain explicit dev/operator fallbacks; an uninitialized home returns an actionable `rly init` error |
| AT-049 | Ensure or recover the resident runtime from `rly config` | `rly config` reuses an attested compatible runtime (resident or launcher-owned), starts the registered per-user service and waits for readiness when it is down, falls back to a session-scoped foreground runtime for uninitialized checkouts, and fails closed with actionable errors on foreign or incompatible listeners; closing the config UI never stops the resident runtime |
| AT-050 | Configure providers, accounts, pools, and profiles through the user-facing control surface | First-run onboarding can authenticate exactly one provider or continue adding multiple; providers/accounts/pools/profiles support create/list through `rly config` and the local UI; multiple accounts per provider stay first-class and pool membership remains provider-scoped |
| AT-051 | Perform credential login/import/refresh/revoke through the credential broker | Flows persist only handle/generation metadata outside secret storage; stale versioned mutations fail explicitly (`stale-version`) with no partial state; credential failures surface actionable secret-free errors |
| AT-052 | Headless bootstrap URL and privacy of `rly config` output | `--headless` prints the single-use fragment bootstrap URL without opening a browser; the token never leaves the fragment; management stays loopback-only; browser/CLI output contains no raw credential, bearer token, authorization header, email, or account identity; `rly admin` and `rly config` observe the same policy revision after mutation |

| AT-036 | Operate the macOS LaunchAgent service lifecycle (#33) | `rly init` on macOS installs/repairs exactly one per-user `com.rly.gateway` LaunchAgent without root; the generated plist uses a stable label and absolute executable/state paths, enables login/reboot recovery, bounds crash-restart, and contains no secrets/tokens/account identity; re-running init is idempotent and never duplicates the label; changed executable/paths are repaired by unloading before reloading; the service adapter reports registration/load state and pid separately from runtime `/identity` readiness and tolerates legacy launchctl subcommands; uninstall removes only the RLY-owned service registration and plist, preserving durable RLY data; a foreign/unattested listener still fails closed and is never killed |

| AT-040 | Operate the Linux systemd user service lifecycle (#34) | `rly init` on a systemd Linux host installs/enables/starts exactly one per-user `rly-gateway.service` under `~/.config/systemd/user` without root or sudo; the generated unit uses absolute executable/entrypoint/config paths and the durable `~/.rly` working directory, bounds crash-restart with `Restart=on-failure` plus explicit `RestartSec` and a `StartLimitIntervalSec`/`StartLimitBurst` policy, and contains no secrets/tokens/account identity or `Environment=`; re-running init is idempotent (identical definition is a filesystem no-op) and a stale executable/path definition is repaired with a `daemon-reload` without duplicate units; a session without a reachable user systemd manager (containers/minimal distros/WSL) fails actionably with guidance that RLY never auto-enables `loginctl enable-linger`; the service adapter reports unit enabled/active/process state separately from runtime `/identity` readiness; uninstall `disable --now`s and removes only the RLY-owned unit, preserving durable RLY data; a foreign/unattested listener still fails closed and is never killed |

| AT-084 | Capture Claude Code agent attribution headers at ingress (#71) | The Anthropic decoder parses `X-Claude-Code-Session-Id`, `X-Claude-Code-Agent-Id`, and `X-Claude-Code-Parent-Agent-Id` into a typed agent context on the canonical request (case-insensitive; partial attribution allowed; empty/missing headers produce no context) without inspecting prompt/body content; gateway fixtures prove the headers round-trip through real HTTP requests |
| AT-085 | Operate the session-scoped agent execution-context registry (#71) | A typed execution context (launch session binding, access provider, frozen physical model id, model family, effective tier, mapping/registry revisions) is recorded after each successful resolution and is valid only while the owning lease is active; entries are removed on lease revocation (`dropLease`) and lease expiry, and never store credentials, account ids, or account identity; stale ephemeral agent ids cannot bind a future session |
| AT-086 | Resolve subagent tier requests in the parent execution context (#71) | A subagent `model: fable` request resolves against the exact recorded parent-agent context when available, else the session's main context, else the launch session's unambiguous profile-default model; the resolved parent model/family is carried into #69 tier resolution and the decision trace records how the parent context was derived; when no parent/session family can be determined on a multi-family provider, resolution fails closed (`tier-unavailable` + `family-unknown` cause) instead of choosing a global strongest model |
| AT-087 | Keep the parent session on its model while subagents resolve (#71) | Main Terra-family session → Kongming `fable` → verified Sol-family physical target through the gateway; after Kongming completes the parent/main request still resolves to Terra; a subsequent `haiku`-tier reviewer resolves to the family-appropriate lower-cost target without changing the Kongming/main mappings |
| AT-088 | Isolate nested and concurrent subagents (#71) | Two parallel subagents with different agent ids and different requested tiers resolve independently under one launch session without context leakage; nested subagents inherit their immediate parent's context; contexts are removed when the launch session/lease expires or is revoked |
| AT-089 | Pass subagent reasoning/effort and gate tool use (#71) | Explicit subagent `effort` (e.g. `high`) is preserved into the canonical reasoning request and the translated decision trace; a tool-using subagent with an explicit reasoning intent is rejected when the selected model/access path lacks reasoning-with-tools evidence (actionable `tier-unavailable`/`capability-rejected` with the underlying cause), never silently downgraded |
| AT-090 | Keep agent-context traces privacy-safe (#71) | Route traces carry allowlisted pseudonyms (hashes) for Claude session/agent/parent linkage plus the parent model/family used for tier resolution and never contain prompts, reasoning text, credentials, or durable user identity; no implementation relies on a global subagent model override, and the source agent/skill definition (`model: fable`) is never rewritten to a physical model |
| AT-091 | Detect exact installed client versions separately from `found` (#24) | Claude Code and Codex target detection report the executable path and the exact semantic/version string parsed from the client's own `--version` output; binary presence is `found`, never `compatible`; a silent/broken/missing binary reports `unknown` and version is never inferred from file timestamps or package directories |
| AT-092 | Pin the supported baseline's wire contract with redacted fixtures (#24) | Deterministic fixtures pin Claude Code session/agent/parent attribution headers, `GET /v1/models` request/auth/response selection including the `claude`/`anthropic` discovered-id prefix rule and startup cache behavior, `fable`/`haiku`/`sonnet`/`opus` alias semantics, the subagent/session `effort` additive field, streaming event framing, and `--no-session-persistence`; fixtures are redacted/synthetic and never contain credentials, prompts, or real responses |
| AT-093 | Run the fake gate matrix for text, streaming, and cancellation (#24) | For an exact access path the deterministic matrix proves a text Messages request/response, streaming framing/event order, and cancellation propagation against the pinned contract; capability-gated `not-run` states are never reported as passed |
| AT-094 | Run the fake gate matrix for single/multi/parallel tool loops (#24) | One tool call round-trips with valid JSON aggregation; multi-turn continuation preserves valid tool results; parallel tool calls are evaluated only when the access path evidences `parallelTools`; a model that handles one tool call but fails multi-tool/continuation is never advertised with stronger tool capability than proven |
| AT-095 | Run the fake gate matrix for reasoning and reasoning+tools (#24) | Reasoning requests are accepted and the requested effort is preserved through the decoder; reasoning+tool interleave is evaluated only when the access path evidences `reasoningWithTools`; a model that supports reasoning but fails reasoning+tools is not advertised as reasoning+tools compatible; unsupported/effort-clamped behavior fails with a typed reason |
| AT-096 | Pin gateway model discovery behavior (#24) | The canary pins `GET /v1/models` request/auth/query behavior, the discovered-id prefix filter (`claude`/`anthropic`), the `claude-rly-` projection namespace compatibility, and startup cache behavior used by #72; a changed model-filter contract fails the discovery gate with `gateway-model-filter-changed` |
| AT-097 | Prove subagent routing, parallel attribution, and the effort signal (#24) | The supported baseline's `fable` alias and session/agent/parent attribution headers parse into distinct typed contexts (parallel subagents never collide); the actual subagent/session effort signal reaches the gateway decoder faithfully (`sourceEffort` preserved); a missing agent header or lost effort signal fails its gate with a typed reason |
| AT-098 | Key evidence by exact client + provider + adapter + physical model (#24) | Every canary result identifies client kind/version, access provider, adapter/integration mode, physical model id, and family (when known); the same upstream model through two access providers never shares evidence and tests prove there is no cross-provider evidence reuse |
| AT-099 | Classify VERIFIED/EXPERIMENTAL/BROKEN and surface unknown clients (#24) | `VERIFIED` requires all required gates for the advertised use plus live evidence where the provider class requires it; `EXPERIMENTAL` covers partial/fake-only evidence; `BROKEN` covers a failed required contract; missing/unrun evidence is `unknown` and never reported as passed; a newly installed unknown Claude/Codex version produces a visible `unknown/not-tested` status and does not silently replace the tested baseline |
| AT-100 | Fail the correct gate with a typed reason on changed fixtures (#24) | A deliberately changed fake client fixture (missing agent header, changed model-filter behavior, malformed tool continuation, clamped effort) causes exactly the corresponding canary gate to fail with a typed reason instead of silently passing or failing an unrelated gate |
| AT-101 | Emit machine-readable evidence without mutating trusted evidence (#24) | `rly canary run` persists a secret-free evidence artifact under `<control-plane>/canary/` consumable by #23/#67 review/proposal tooling; proposals report drift and never mutate `directProviderRegistry`, trusted tier mappings, or `/v1/models` projection; malformed artifacts fail closed on read; `rly canary status` reports tested baselines and per-path verdicts |
| AT-102 | Keep canary artifacts and live gates privacy-safe (#24) | Canary artifacts/logs carry only client/provider/model ids, gate names, status, fixture revision, and redacted error categories — never prompts, model responses, reasoning text, credentials, authorization headers, email, or account identity; live provider runs remain opt-in (env-gated, skipped ≠ pass) and are never reported as passed when unrun; artifacts are created with private permissions |
| AT-103 | Separate candidate installation from activation in durable states (#73) | `rly update` records installation and activation as distinct durable states (`installing` → `pending-activation` → `activating` → `active`; `failed`/`rollback-required` on error); an installed candidate is never considered active until the restarted resident runtime passes identity/readiness verification; states survive CLI exit and are readable by later invocations |
| AT-104 | Report the actual serving runtime identity/version/state (#73) | The attested `/identity` handshake reports the serving runtime version, the durable state/schema version, the update state (with pending/previous versions), the active launch-session count, and the drain flag — never the package/CLI version on disk alone |
| AT-105 | Activate with zero active sessions through a controlled restart (#73) | With zero active launch sessions, a verified candidate activates through a single per-user service-manager restart and becomes the serving runtime; the update state is `active`, the new runtime version serves, and identity/readiness verification passed |
| AT-106 | Keep one active Claude session running while an update installs (#73) | With one active launch session, candidate installation completes and the old resident runtime keeps serving the session (an authorized round-trip through the session token succeeds — not proven by an idle token); activation waits for the session to end, and the session is never killed |
| AT-107 | Wait for multiple concurrent sessions to reach the safe zero count (#73) | With multiple concurrent sessions/subagents, activation stays pending until every launch session ends; each session keeps working on the old runtime; only the last session's release lets activation proceed |
| AT-108 | Never replay or move an in-flight session across activation (#73) | A session-token round-trip succeeds on the old runtime while activation is pending; after activation the same token is not accepted by the new runtime (session is not moved or replayed across the restart) |
| AT-109 | Apply the documented new-launch compatibility policy while pending (#73) | A compatible CLI/runtime pair may keep issuing launches on the old runtime while activation is pending; an incompatible pair refuses only NEW launches with an actionable `update-pending`/`runtime-version-mismatch` message and never touches existing sessions; `status`/`doctor` show the installed-but-not-active state |
| AT-110 | Refuse new issuance once drain begins (#73) | After an authenticated `POST /drain` on the attested resident runtime, `POST /v1/launch-sessions` is refused with `update-pending` and the runtime reports `draining`; existing sessions keep running to completion; unauthenticated drain requests are rejected |
| AT-111 | Verify candidate startup and roll back once on health failure (#73) | The activated candidate is accepted only after authenticated identity/readiness/state-open verification passes (serving version matches the pending candidate); a health/identity failure triggers exactly one bounded rollback to the previous known-good version whose success is verified and reported; no restart/rollback loop |
| AT-112 | Block a forward-only/unrollbackable migration before destructive activation (#73) | A candidate declaring a forward-only state migration is refused before changing durable state with an actionable message; the previous version keeps serving and the installed candidate is not activated |
| AT-113 | Serialize concurrent update invocations (#73) | Two concurrent `rly update` invocations serialize through the update lock: one completes, the other fails actionably with an in-progress message; no double restart, duplicate migration, or update-state corruption occurs |
| AT-114 | Recover crash/reboot update states deterministically (#73) | Stale `installing` becomes `failed` with retry guidance; `pending-activation` resumes activation once the candidate verifies; interrupted `activating` rolls back using the preserved previous-version reference; a stale update lock whose owner process is gone is reclaimed; the rollback reference survives a crash |
| AT-115 | Fail closed on a foreign port after restart (#73) | If the gateway port is owned by a foreign/unattested listener after restart, RLY fails closed, never signals or kills the listener, and enters rollback/error handling with a deterministic doctor action; no infinite retry |
| AT-116 | Expose allowlisted update metadata in status/doctor (#73) | `rly status`/`rly doctor` expose CLI version, resident runtime version, compatibility, pending version, update state, active-session count, and drain state using privacy-safe metadata only |
| AT-117 | Leave credential stores and account generations untouched by a no-schema update (#73) | A no-schema update does not change credential stores or account/credential generations; update state, artifacts, and logs contain no credentials, tokens, prompts, responses, or account identity |
| AT-118 | Cover candidate/restart/rollback success and failure paths (#73) | Tests cover candidate installation failure, candidate verification failure, restart failure, rollback success, rollback failure, and the incompatible CLI/runtime handshake; each ends in a deterministic, actionable state |

| AT-119 | Distinguish bare aliases from explicit RLY tier selectors (#125) | A bare `fable` selector classifies as `CLIENT_NATIVE_ALIAS` (source `client-native-alias-contract`) and the explicit `rly-tier:fable` classifies as `RLY_LOGICAL_TIER` (source `rly-tier-namespace`); both preserve the exact source selector for diagnostics; core invariant `fable != rly-tier:fable` holds by intent kind |
| AT-120 | Keep the RLY logical selector namespace explicit and collision-free (#125) | RLY logical tiers are reachable only through `rly-tier:haiku|sonnet|opus|fable`; an unknown/empty/role/exact-model `rly-tier:` value fails closed with the typed `unknown-namespace` reason and is never reinterpreted as an alias or exact model; projection ids and bare aliases never collide with the RLY namespace |
| AT-121 | Invoke the tier resolver only for typed tier intent (#125) | The #69 provider/family resolver runs only for an explicitly typed `RLY_LOGICAL_TIER` (or a client-native alias mapped through the explicit client-alias contract) — never through accidental string matching; exact model ids, `default`, and `inherit` resolve through the profile role/helper mapping with no tier resolver invocation |
| AT-122 | Migrate/compat existing bare-tier selectors and persisted values (#125) | Existing bare-tier selectors (`model: fable`) and `profile.modelRoles` tier keys keep their pre-#125 meaning through the explicit classification contract with tests; a persisted exact model id (e.g. `gpt-5.6-terra`) is classified `EXACT_CLIENT_MODEL` and keeps the exact #68 path — ambiguous persisted values never silently change meaning into tiers |
| AT-123 | Keep exact projected selection exact (#125) | A `claude-rly-*` selector is dispatched to the projection route before profile resolution and is never classified/reinterpreted as a tier/alias/exact model; an unknown projection id fails closed (`model-unavailable`) with no substitution |
| AT-124 | Support client-native aliases without rewriting user files (#125) | Bare Claude/Codex alias strings resolve through the explicit, version/compatibility-aware client-alias contract (pinned vocabulary, traceable mapping) and never require rewriting user agent/skill files to physical provider model ids; the supported client baseline's alias semantics remain classified by #24 |
| AT-125 | Apply deterministic selector precedence (#125) | Classification precedence is deterministic and documented (explicit `rly-tier:` → projection → client alias → inherit → default → exact model); identical selectors classify identically; `default`/`inherit` resolve through the profile default without a literal role lookup on the raw string |
| AT-126 | Retain aggregator tier affinity after intent migration (#125) | Aggregator same-access-provider + same-family default tier affinity is unchanged: Terra-family parent + `fable` (bare or `rly-tier:`) resolves to the verified Sol-family target (never Anthropic/DeepSeek); #68/#69 exact model, provider/family affinity, no-cross-family-fallback, and account-selection-after-model-selection tests stay green |
| AT-127 | Expose selector kind/source/resolution in diagnostics only (#125) | Route diagnostics (`ProfileDecisionTrace.intent`) show selector kind, source namespace/rule, and the resolved logical target (tier/alias/model id/role) only — never prompts, credentials, account identity, or settings contents |
| AT-128 | Distinguish the namespaces in architecture/model-control docs (#125) | ARCHITECTURE, protocol compatibility, project decisions, RTM, and this catalogue explicitly distinguish the client alias namespace from the RLY logical selector namespace (`fable != rly-tier:fable`), with the typed intent kinds, precedence, migration path, and error taxonomy documented |
| AT-129 | Preserve Anthropic thinking signatures across decode → internal representation → same-protocol encode (#119) | An Anthropic Messages request whose assistant thinking block carries a `signature` decodes with the signature retained in the fidelity envelope (`anthropic-thinking-signature`, association `message:block`), marked required, with `preserved-native` provenance and the thinking text still projected semantically; re-encode/aggregate produces the same thinking block signature; no signature loss on the same-protocol path |
| AT-130 | Emit streaming `signature_delta` in valid order in deterministic fixtures (#119) | The canonical event stream represents a `signature-delta`; the emitted Anthropic SSE places `signature_delta` after `thinking_delta` and before `content_block_stop` for the same block, byte-for-byte matching the deterministic golden fixture; a signature delta on a non-thinking block or before its content block fails closed (`invalid_event_order`) |
| AT-131 | Preserve Responses reasoning identity + encrypted content through continuation storage (#119) | A Responses reasoning item with `id` and opaque `encrypted_content` decodes with the identity on the semantic reasoning content and the encrypted content in the fidelity envelope (`openai-reasoning-encrypted-content`, required); a `previous_response_id` continuation round-trips both through storage and re-encode with the exact artifact-to-item association; opaque content is never reconstructed from summary text |
| AT-132 | Route semantically without inspecting opaque payloads (#119) | Semantic routing/capability/reasoning decisions operate on the canonical projection only; `describeFidelity()` exposes kinds/associations/dispositions/counts without decrypting or interpreting artifact values; no code path reads artifact values for routing decisions |
| AT-133 | Preserve unknown additive native fields by documented policy (#119) | Same-protocol decode of a request with an unknown top-level additive field records an `ignored` disposition note (field name + "not required for continuation") in the envelope and the existing ignored-fields list; the value is never used for routing and never silently treated as required continuation state |
| AT-134 | Report unsupported fidelity on cross-protocol paths without fabrication (#119) | A request carrying a required opaque artifact (signature or encrypted content) routed onto a translation path that cannot represent it (Chat Completions transport via OpenRouter/DeepSeek) fails with `unsupported-fidelity` before any upstream call; no signature/encrypted content is fabricated or reconstructed; the semantic projection alone (no required artifacts) still routes normally |
| AT-135 | Distinguish translation provenance dispositions (#119) | The fidelity envelope records each wire-significant field/artifact under exactly one disposition: `preserved-native` (thinking signature, encrypted content), `translated` (thinking text, reasoning summary text), `ignored` (unknown additive fields), or `unsupported` (required artifact on an incapable path); diagnostics surface the disposition without the value |
| AT-136 | Keep opaque artifacts out of traces, diagnostics, logs, and privacy snapshots (#119) | Route traces, diagnostics, logs, redacted output, and privacy snapshots contain provenance metadata only (kinds, associations as stable keys, dispositions, counts) — never signature values, encrypted content, or other opaque artifact values; the observability redactor treats artifact-bearing keys as sensitive; privacy scans pass |
| AT-137 | Keep artifact association on the correct item/block across multi-turn reasoning+tool continuation (#119) | Multi-turn Responses continuation interleaving reasoning (with identity + encrypted content), text, function calls, and function call outputs keeps each opaque artifact attached to its own reasoning item id across decode → continuation storage → subsequent request construction → re-encode; tool call ids and reasoning item ids remain distinct and correctly associated; nothing is merged or reordered |
| AT-138 | Identify native rail, semantic projection, and fidelity envelope as separate authorities in docs (#119) | Protocol compatibility docs, ARCHITECTURE, project decisions, RTM, and this catalogue identify the native protocol rail, the semantic projection, and the versioned fidelity envelope as separate authorities with their own ownership and fail-closed policy; FR-025 / SR-F-029 added with non-colliding IDs |
| AT-150 | Classify Claude/gateway/model settings with explicit ownership and precedence (#126) | Relevant settings classify into exactly one typed ownership category (RLY-owned / conflicting / safe pass-through / unsupported / user override) with deterministic precedence (child-only gateway contract > RLY-owned persisted projection > explicit RLY/profile settings > native input > client persistence > defaults); a conflicting native gateway/auth/model setting never overrides RLY's scoped launch contract silently; unsupported credential-bearing shapes (`oauthAccounts`) are never composed into a view |
| AT-151 | Give two RLY profiles distinct durable views with no state leakage (#126) | Two RLY profiles resolve to distinct deterministic view directories; a `claude-rly-*` projection model persisted into one profile's view never becomes another profile's default; each profile re-prepare retains its own RLY-only model/default/cache/history state; the same immutable profile id always maps to the same view id (renames keep it); the reserved `default` view is never produced by profile derivation |
| AT-152 | Keep plain non-RLY Claude unaffected (#126) | Plain `claude` reads native `~/.claude` only and never inherits RLY view model/gateway state; view settings/history never contain RLY gateway env, auth, or projection ids that a plain launch could pick up; project-local `.claude` behavior is unchanged |
| AT-153 | Record an ownership manifest distinguishing native-imported, RLY-generated, and view-owned state (#126) | The per-view manifest records each surface with exactly one category; native-imported entries carry the native-relative source path and sha256 hashes; RLY-generated entries appear when RLY recomposes state (e.g. after native settings removal); view-owned entries appear for durable/divergent state; the manifest is metadata/hashes only — never credentials, settings content, prompts, or account identity |
| AT-154 | Reconcile imported deletions/renames without touching unrelated state (#126) | Deleting or renaming a native agent/command/skill removes the corresponding owned import on the next prepare (view copy hash matches the import); unrelated imported files and RLY/view-owned state survive; a view copy that diverged from the import is reclassified view-owned and never deleted; RLY never deletes a file it does not own as an import and leaves no additive-only ghost files |
| AT-155 | Run concurrent RLY launches without racing native config or profile views (#126) | Concurrent prepares for the same and different profile views converge to consistent views (atomic temp+rename writes; bounded per-view reconcile lock; no whole-file copy-back/restore of native config); the native settings file is never rewritten; no sibling `/model` write is lost or corrupted |
| AT-156 | Retain the persisted RLY projection model for the owning profile only (#126) | A `claude-rly-*` model persisted via `/model` survives re-compose for its owning profile view; another RLY profile and plain `claude` never inherit it unless explicitly configured; native model input stays native |
| AT-157 | Reflect native allowlisted changes with documented freshness precedence (#126) | Changes to allowlisted native settings/agents/commands/skills/plugins compose per documented precedence; ownership wins over naive newer-mtime copy (a view/RLLY-owned value is never overwritten by a newer native timestamp alone); unchanged native input is not rewritten |
| AT-158 | Migrate the shared overlay deterministically without touching `~/.claude` (#126) | The legacy `<control-plane>/claude` overlay moves into `views/default` exactly once (crash-safe two-phase move); native `~/.claude` and `~/.claude.json` remain byte-identical; ambiguous shared persisted model/history state stays in the unprofiled `default` view and is surfaced by `rly status`/`rly doctor`, never silently assigned to a profile |
| AT-159 | Keep profile views, manifests, and diagnostics private; black-box sentinel proof (#126) | View/manifest/marker permissions stay private (`0700`/`0600`); no RLY launch/management/provider credential, auth token, settings content, prompt, or account identity is persisted into any view/manifest; `rly status`/`rly doctor` expose view id/path, composition version, ownership/reconciliation status, and conflicting key categories only; black-box sentinel tests prove native config/resources remain byte-identical while profile views compose/reconcile; privacy scan passes |

| AT-139 | Installing a candidate never changes the serving `active` reference (#92) | `installCandidate` writes the candidate as an immutable deployment and updates only `refs/staged`; `refs/active` and `refs/previous` are untouched by installation; `active` switches only through the explicit activation transition and is restored by rollback |
| AT-140 | Content-addressed immutable deployments with distinct identities for distinct bytes (#92) | Two byte-distinct candidates that declare the same semantic version receive distinct SHA-256 artifact ids and coexist under distinct `runtime/versions/<artifactId>` directories; neither overwrites nor aliases the other; semantic version is metadata only, never the storage key |
| AT-141 | Idempotent reinstall of the identical artifact (#92) | Reinstalling the exact same artifact bytes is an idempotent no-write: the immutable deployment directory is never recursively replaced, its contents are byte-identical, and `staged` points at the same artifact id |
| AT-142 | References point only at validated immutable deployments (#92) | `staged`/`active`/`previous` are explicit ownership-separated references that resolve to a deployment whose layout (private 0700 directory, matching artifact id, valid manifest/entrypoint) was validated before exposure; a ref escaping the immutable store or pointing at a non-immutable name fails closed with a doctor action |
| AT-143 | Atomic reference replacement without a gap (#92) | Reference changes are temp-reference create + atomic rename + parent-directory fsync, never `rm + symlink`; crash/failpoint and rapid-replacement tests observe only the old or the new valid reference, never an intentional missing intermediate state, and stale temp references are cleaned by the next replace. The rapid-replacement test harness is deterministic (generation-counter barrier, #142): the writer cannot advance past a generation the reader has not observed, so every replacement is exercised under a concurrently active reader with no sleeps or timing luck; the invariant assertion is unchanged (each read must resolve to the old or the new valid reference, never a gap) |
| AT-144 | Interrupted copy never exposes a partial deployment (#92) | An interruption/failure during artifact copy leaves no partial deployment visible through `staged` or `active`; leftover staging directories are removed and a later install succeeds cleanly |
| AT-145 | Legacy semver/current/previous layout migrates without deleting the serving runtime (#92) | A legacy `runtime/current`/`previous` + `versions/<semver>` layout migrates in place: serving deployments are renamed to content-addressed artifact ids (bytes preserved, never deleted), refs are established, and the durable `migrating`/`committed` marker survives a crash so recovery resumes idempotently; the last known-good serving runtime is never removed before the new ref state is durable |
| AT-146 | Malformed/conflicting metadata fails closed (#92) | Unknown or malformed legacy state (dangling/unknown `current`, non-symlink legacy refs, metadata identity/directory conflicts, incomplete layouts) fails closed with an actionable typed error/recovery instruction (`run rly doctor`) and never mutates or deletes legacy bytes |
| AT-147 | Deployment manifests and refs are secret-free (#92) | Deployment metadata (`runtime/versions/<artifactId>/.rly-deployment.json`) and refs carry version/build/digest/path identifiers only — never credentials, auth headers, account identity, prompts, responses, or reasoning text; metadata and refs are private (0700 dir / 0600 file) and current-user owned |
| AT-148 | Concurrent/idempotent staging preserves existing regressions (#92) | Staging is concurrency-safe and idempotent; existing update lifecycle, privacy, service-manager, and installer regression suites stay green after the immutable store replaces the mutable semver store |
| AT-149 | Document immutable deployment identity and INSTALL != ACTIVATE ownership (#92) | ARCHITECTURE, project decisions, FRS/SRS, RTM, and this catalogue describe content-addressed immutable deployment identity, explicit `staged`/`active`/`previous` reference ownership, atomic reference replacement, legacy migration, and the rule that installation never activates |
| AT-160 | Real process-start identity in the update lock (#93) | The update lock records the actual OS process-start identity from the process-attestation subsystem (never acquisition wall-clock time); a second live updater with the same identity cannot reclaim the lock; a lock whose owner identity is proven stale/dead (pid gone from the process table) is reclaimed; a lock whose identity is unverifiable (wall-clock fallback) is conservatively held and never reclaimed from a mismatched timestamp alone; `lockStatus()` reports held/ownerPid/stale with no PID-only evidence |
| AT-161 | Durable activation transaction phases and deterministic recovery (#93) | Activation moves through durable journal phases STAGED → DRAINING → SWITCHING → PROBATION → COMMITTING → COMMITTED (or ROLLING_BACK → COMMITTED | RECOVERY_REQUIRED), each written before the action it fences, carrying candidate/previous artifact ids and a bounded rollback counter; crash recovery reads ONLY the durable phase: STAGED/DRAINING resume activation, SWITCHING/PROBATION/COMMITTING roll back to the durable previous known-good reference (never guessing a candidate committed), COMMITTED promotes to active, and an interrupted or duplicate rollback is terminal RECOVERY_REQUIRED |
| AT-162 | Fence-first drain and no-replay rule (#93) | Once activation begins the old generation refuses new launch-session issuance (actionable `update-pending`) BEFORE the serving ref switch while existing sessions keep working on the old runtime; in-flight streams/tool loops are never replayed or moved; the same session token is rejected (401) by the new runtime after activation |
| AT-163 | Migration compatibility classes preflight (#93) | `none`, `backward-compatible-expand`, and `transactional-replace` candidates pass preflight and activate; `forward-only` (or a legacy `migrationForwardOnly: true` manifest) is blocked before any destructive state change with an actionable message while the previous version keeps serving; legacy `migrationForwardOnly: false` maps to `backward-compatible-expand` and activates |
| AT-164 | Probation verifies exact identity, protocol, and state before commit (#93) | The candidate is provisional until the serving runtime version equals the staged candidate, the management/data protocol version matches, authenticated readiness (management/state open) passes, and the durable state/schema version is compatible; only then does the transaction become COMMITTED and the old deployment eligible for later cleanup |
| AT-165 | Bounded single rollback with verified restore (#93) | A candidate failure triggers exactly one bounded rollback attempt that re-establishes `active`/`previous` from the durable journal evidence (previous written before active; never losing the known-good reference), restarts at most once, verifies the restored runtime identity/readiness, and reports the outcome; no rollback loop |
| AT-166 | Rollback failure terminates in explicit RECOVERY_REQUIRED (#93) | When the single rollback attempt fails (restart failure, foreign listener, or verification failure), the transaction terminates in the explicit `recovery-required` state with an actionable `rly doctor` path; a subsequent `rly update` performs no restarts, no rollback retry, and no loop |
| AT-167 | Crash/reboot windows preserve previous/staged/active reference integrity (#93) | Simulated interruption before/after the fence, the active-ref switch, the service restart, migration, probation, commit, and rollback leaves `previous`/`staged`/`active` resolving to validated immutable deployments at every boundary; the previous known-good reference is never lost before COMMITTED is durable; recovery restores refs idempotently from journal evidence |
| AT-168 | Allowlisted transactional diagnostics stay secret-free (#93) | `rly update`, `rly status`, `rly doctor`, and `/identity` expose the transaction phase, active/staged/previous build identity, lock-owner status, drain state, and last activation/rollback outcome — versions, digests, counts, and identifiers only, never credentials, tokens, prompts, responses, reasoning text, or raw account identity; privacy scans pass |

| AT-169 | Key a Compatibility Claim by the exact execution path and feature (#122) | A stable versioned claim key includes client kind, exact client version/baseline, source protocol/revision (#119 vocabulary), adapter/integration surface, access provider, auth mode, endpoint contract, exact physical model, and feature/capability claim; model family is metadata only and never part of the key; identical inputs produce identical keys and no two distinct paths/features collide |
| AT-170 | Keep same-model-different-provider claims and evidence histories independent (#122) | The same upstream model through two access providers produces distinct claim keys, distinct claim documents, and distinct evidence histories; an exact lookup for one provider never returns another provider's evidence |
| AT-171 | Carry different feature results on the same path independently (#122) | Text, streaming, cancellation, tools (incl. parallel/multi-tool continuation), reasoning, reasoning+tools, model discovery, session/subagent attribution, and config-overlay behaviors each resolve as their own claim for the same path; passing text never implies tool/reasoning/discovery compatibility and a capability-gated feature stays `not-run`/`missing` rather than passed |
| AT-172 | Remove `livePassed`/`liveEvidence` from the authoritative classification path (#122) | No boolean input exists on classification; a full deterministic pass classifies `EXPERIMENTAL` with `production-claim-not-established` and no observation path can emit `VERIFIED` — production trust requires Layer B/C evidence plus reviewed promotion (#124), which is out of scope here |
| AT-173 | Let an env/CLI runner switch enable execution without creating evidence (#122) | `RLY_LIVE_CANARY`/`liveRunnerEnabled` only reports `enabled` and can never fabricate a PASS: with the switch on, a run still emits Layer A evidence only, no Layer B/C records, and no VERIFIED verdict |
| AT-174 | Reclassify the deterministic canary matrix as Layer A Evidence v2 (#122) | `rly canary run` persists secret-free per-feature Evidence Artifact v2 records (`evidenceSchemaVersion` 2, layer A, kind `deterministic-fake-matrix`, runner version, fixture revision, typed failure reasons) and appends claim documents under `<control-plane>/claims/`; a Layer A pass is never mislabeled as real live access-path proof |
| AT-175 | Represent missing/skipped/unrun evidence distinctly from PASS and FAIL (#122) | A claim with no observations is `missing`; a required layer without a passing record is `not-run`; `not-run`/`missing` are never reported as `passed`; per-layer status shows explicit presence/result (A/B/C) |
| AT-176 | Keep evidence artifacts secret-free with typed failure categories (#122) | Claim/evidence documents carry client/provider/model ids, claim keys, layer/kind, gate names, results, fixture revision, runner version, environment, and typed failure reasons only — never credentials, auth headers, account identity, prompts, real responses, or reasoning text; claim files are `0700`/`0600` and the privacy scan passes |
| AT-177 | Apply an explicit legacy migration/invalid-for-v2 policy (#122) | Pre-v2 canary artifacts are flagged legacy/untrusted (`legacy-v1-artifact-untrusted-for-v2-claims`); they are readable for diagnostics but can never satisfy a v2 claim — a claim lookup returns `missing` until real v2 observation; registry rows without a `claimRef` (pre-revision-5) are untrusted for v2 claim authority |
| AT-178 | Prove evidence identity, serialization, layer semantics, and no cross-reuse in tests (#122) | Unit/contract tests cover claim key determinism, same-model-two-provider separation, feature independence, Layer A/B/C status derivation, schema round-trip/fail-closed reads, append/audit persistence with dedupe, deterministic lookup by claim+feature, and no cross-provider/model/feature reuse |
| AT-179 | Define claim identity and Layer A/B/C semantics in docs (#122) | protocol-compatibility, ARCHITECTURE, project decisions, SPEC, FRS/SRS, RTM, and this catalogue define the versioned claim identity, feature scoping, evidence layers A/B/C, the removal of `livePassed` trust semantics, and the legacy v1 policy |

| AT-180 | Run the Layer B black-box matrix with the actual installed Claude Code binary and record exact versions separately (#123) | `rly canary run-b` (or the runner directly) detects the installed Claude Code binary, records its exact probe version (`cli-output`, never inferred) and executable path separately from the reviewed supported baseline, launches the real binary against the controlled local fixture server with child-only environment isolation, and emits per-feature Evidence v2 (layer B / `installed-client`) with runner version, fixture revision, timing, and typed failure reasons |
| AT-181 | Run the Layer B black-box matrix with the actual installed Codex CLI binary for the supported RLY surface (#123) | The runner launches the real `codex` binary against the controlled OpenAI Responses fixture server and emits typed, version-keyed Evidence v2 per feature; a binary that does not route through the configured surface (e.g. hardcoded transport) yields typed failures/`not-run` keyed to that exact version — never a silent pass and never a supported baseline |
| AT-182 | Cover the Claude behaviors RLY relies on in the black-box matrix (#123) | The Layer B matrix exercises request/stream framing, cancellation, tool round-trip, multi-tool continuation, parallel tools, reasoning/effort, reasoning+tools, `/v1/models` discovery/selection, session/agent/parent attribution, tier alias behavior, subagent concurrency, config-overlay behavior, and long sessions; capability-dependent gates stay `not-run` with a typed reason when the client sends no such config |
| AT-183 | Run the Layer C exact access path with feature-scoped Evidence v2 rather than a boolean (#123) | `rly canary run-c` executes one exact configured (client, client version, protocol/revision, adapter, provider, auth mode, endpoint, physical model, feature) path through the real gateway translation stack with explicit opt-in and an environment credential, emitting per-feature Evidence v2 (layer C / `live-access-path`) — text, streaming, cancellation, tools, reasoning, effort, discovery, and attribution each carry their own record; text success never promotes tools/reasoning/discovery |
| AT-184 | Keep same-model-different-provider Layer C evidence independent (#123) | The same upstream model through two access providers produces distinct claim keys, claim documents, and evidence histories; evidence from one provider never appears in the other provider's claim lookup |
| AT-185 | Never present missing credentials, skipped runs, environment inability, or unexecuted gates as PASS (#123) | A missing credential env emits `not-run`/`authentication-credentials-unavailable` for every gate with zero upstream calls; a missing client binary emits `not-run`/`client-not-installed`; an unrun gate has no `passed` record; `rly canary run-c` refuses without `RLY_LIVE_CANARY=1`; skipped/live tests stay skipped (skipped ≠ pass) |
| AT-186 | Yield a typed exact gate failure keyed to the client version when client behavior drifts (#123) | A changed client wire behavior (e.g. dropped attribution headers) fails the exact gate with a typed reason (`missing-agent-header`, `client-contract-drift`, ...) on the Evidence v2 record keyed to that observed client version; the drift test with a deliberately broken fixture proves the taxonomy |
| AT-187 | Test newly observed client versions without auto-promotion (#123) | Layer B evidence is keyed to the exact observed version and the reviewed supported baseline is recorded separately; an observed-version claim document cannot reach `passed` without reviewed Layer A/C evidence plus promotion, so a new version never becomes a supported baseline by a black-box pass alone |
| AT-188 | Keep live/black-box artifacts secret-free (#123) | Runner raw results, Evidence v2 records, and claim documents carry gate names, results, typed reasons, timing, wire booleans, and identity metadata only — never credentials, auth headers, account identity, prompts, real responses, or reasoning text; claim/runner files are private (`0700`/`0600`) and the privacy scan passes |
| AT-189 | Never mutate trusted registry/effective state from runner failures (#123) | Runner runs are observation-only: they do not write the trusted registry, tier mappings, or `/v1/models` projection; failures, timeouts, and not-run gates leave trusted compatibility state byte-identical |
| AT-190 | Separate Layer A, Layer B, and Layer C execution in CI/manual docs (#123) | CI and manual docs distinguish deterministic Layer A (`rly canary run`), installed-client Layer B (`rly canary run-b`, real-binary sentinels opt-in via `RLY_CLAUDE_E2E=1`/`RLY_CODEX_E2E=1`), and real-path Layer C (`rly canary run-c`, `RLY_LIVE_CANARY=1` opt-in) — including which gates are deterministic, which need installed binaries, and which need credentials/opt-in |
| AT-191 | Positive PASS evidence alone cannot make a claim trusted without reviewed promotion (#124) | A claim with full Layer A/B/C passing observations but NO explicit promote decision resolves `experimental` (trust `none`, enforcement blocked for required features); an explicit promote decision tied to the exact evidence revision makes it `trusted`; the decision metadata (reviewer/source/decidedAt/revision) is carried in the effective result |
| AT-192 | Quarantine disables one exact claim without disabling another provider or feature (#124) | Quarantining `(provider A, model M, feature F)` resolves that exact claim `quarantined` (enforcement blocked for required features) while the same provider's other features and another provider's same-feature claim stay untouched (quarantine `none`, trust preserved); lifting the quarantine restores evaluation; quarantine never deletes the historical claim evidence |
| AT-193 | Freshness marks evidence stale when a configured identity/revision dependency changes (#124) | Evidence is marked stale (with a distinct freshness reason) on client version/baseline drift, protocol/adapter revision drift, provider endpoint/auth-mode drift, physical model fingerprint change, fixture/corpus revision drift, material RLY build change, and evidence age; matching dependencies stay fresh and no fabricated staleness occurs without a configured dependency |
| AT-194 | Effective resolution exposes trust, health, freshness, quarantine, and enforcement as distinct fields (#124) | The effective result per exact claim/feature exposes trust state, observed health, freshness, quarantine status, and enforcement reason as separate fields plus per-layer evidence status (A/B/C) — never a single persisted boolean; label precedence is deterministic (quarantine → missing → failed → rejected → review-stale → trusted/stale/experimental) |
| AT-195 | `src/routing/model-selection/selector.ts` no longer treats static `model.compatibility.state` as the sole final production trust authority (#124) | With an ECR snapshot supplied the selector's compatibility pass is driven by effective answers (authority `ecr` in the trace) and static state is seed/reference only; the selector/tier/projection/runtime consumers recompute enforcement per request context and fail closed on blocked required features |
| AT-196 | Model projection/discovery excludes paths lacking effective trusted claims for required Claude/Codex features by default (#124) | `GET /v1/models`/`projectModelUniverse` project only paths whose required RLY features are effectively `trusted` by default; evidence-backed `experimental`/`stale` paths appear only with the explicit opt-in; quarantined, rejected, failed-evidence, and missing paths never appear (quarantine excludes even with the opt-in) |
| AT-197 | Tier/reasoning/tool selection consumes effective feature-scoped compatibility and cannot infer stronger support from a text-only PASS (#124) | Tier resolution and reasoning/tool eligibility run their #68 selections through the ECR snapshot; a text-only PASS never implies tool/reasoning/discovery compatibility — each required feature gates independently and a quarantined/blocked required feature fails the tier/selection closed |
| AT-198 | Experimental override is explicit, visible in trace/doctor, and cannot bypass a hard quarantine (#124) | The exact-pin opt-in / `allowExperimental` elevates `experimental`/`stale` with an explicit `experimental-override` enforcement reason visible in the route trace; it can never elevate `untrusted`/`missing`; a quarantined required feature remains blocked unless the separately documented administrative `allowQuarantineBypass` policy is set, and the bypass is visible (`quarantine-bypass` + reason) |
| AT-199 | Legacy registry compatibility states have an explicit migration/seed policy and cannot silently satisfy v2 reviewed trust (#124) | Static `VERIFIED` rows without a `claimRef` derive `experimental` at best (seed-reference-only; enforcement blocked for required features) and can never silently become reviewed v2 trust; `BROKEN` remains a hard negative seed; a reviewed claim + decision resolves through the ECR independently of the static state |
| AT-200 | Diagnostics explain why a target is trusted, stale, quarantined, experimental, or blocked without emitting secrets or user content (#124) | `rly compat explain`, `rly compat status`, and doctor expose claim identity, latest decision metadata, evidence layers, health, freshness reason, quarantine reason, and enforcement reason; decision/quarantine records carry reviewer/source/reason/timestamp/revision metadata only — never credentials, account identity, prompts, responses, or reasoning text; privacy scan passes |
| AT-201 | Tests cover review promotion, evidence update without promotion, quarantine precedence, staleness, version drift, provider-path isolation, feature isolation, and runtime-consumer integration (#124) | Unit/contract/lifecycle suites cover: PASS without promotion stays experimental; promote makes trusted; evidence updated after review makes untrusted (re-review) without auto-promotion; quarantine precedence over decisions; staleness triggers (build/version/fixture/auth-mode/fingerprint/age); provider-path and feature isolation; selector/tier/projection/resolve-route consume ECR snapshots; gateway lifecycle fails closed on quarantine and honors the traceable override |
| AT-202 | Architecture/protocol compatibility/project decisions/RTM/AT identify the Effective Compatibility Registry as the sole runtime compatibility authority (#124) | ARCHITECTURE, protocol-compatibility, project-decisions, RTM, FRS/SRS, and this catalogue identify the ECR (evidence + review + quarantine + freshness + enforcement) as the SOLE runtime compatibility authority; FR-030 / SR-F-034 / AT-191–AT-202 added with non-colliding IDs (checked against merged dev max FR-029 / SR-F-033 / AT-190 from #123) |
| AT-203 | Produce one typed EffectiveModelDecision before account selection for every supported RLY model-routing request (#127) | Profile and projected model routes each assemble exactly one `EffectiveModelDecision` (`src/routing/model-decision/`) after model selection/reasoning translation and BEFORE the pool/account `RouteSelector` runs; the decision rides the route trace; no second model registry/tier resolver/account selector/compatibility authority is introduced |
| AT-204 | Record selector intent, frozen target, compatibility authority, reasoning mapping, revisions, and pool binding in the decision (#127) | The decision carries the #125 typed intent + provenance, the frozen physical access provider/model/family/logicalId/adapter target, the #124 ECR authority result (authority/label/enforcement/seed/per-feature), the #70 reasoning intent→native mapping, policy/profile/session/registry/mapping revisions, and the #72 provider→pool binding — with NO account/credential identity anywhere in the object |
| AT-205 | Let the #124 Effective Compatibility Registry, not static registry compatibility alone, gate production model eligibility/exposure in the decision (#127) | The decision's compatibility reference reports the ECR authority (`authority: ecr`) with effective label + enforcement reason computed per request; static `model.compatibility.state` is seed/reference only (`seedState`) and never the final production trust authority |
| AT-206 | Resolve exact projection, explicit RLY tier, client alias/model, inherit/default, profile, persisted view, and env/settings conflicts deterministically (#127) | The decision's precedence bookkeeping applies the documented order (explicit exact selection → RLY logical tier → client-native alias/model → subagent inherit/override → profile/session policy → env/settings ownership → persisted RLY-view state → defaults); identical inputs produce identical winners/conflicts; conflicts (persisted-view vs request, launch-policy vs request, subagent vs parent, foreign projection, gateway env) are visible with allowlisted detail and the request's explicit selection always wins |
| AT-207 | Keep profile-view isolation so another profile/plain Claude state never silently influences the decision (#127) | A decision reads persisted RLY-owned state ONLY from its owning profile view (`readPersistedViewModel`); another profile's persisted `claude-rly-*` model never appears in this profile's decision (not even as routing metadata); a profile-less/plain state contributes no persisted-view input |
| AT-208 | Freeze the physical provider/model/reasoning before `RouteSelector`; account failover cannot change them (#127) | The decision is assembled before pool/account selection and its target/reasoning is identical across every account attempt; with two pool accounts where the first fails pre-output, retry rotates to the second account and the physical model + reasoning decision are unchanged; account pseudonym/credential generation never appears in the decision |
| AT-209 | Keep child model/effort decisions isolated from the parent with provider/family affinity retained (#127) | A subagent request resolves inside the parent's frozen execution context (exact parent → session main → profile default); the child's own intent wins (visible `subagent-request-vs-parent-context` conflict when it differs) and the parent/main execution context and model are never mutated by the child's decision; provider/family affinity rules from #69/#71 are preserved |
| AT-210 | Reject or explain stale/foreign persisted model state instead of silently remapping it (#127) | A persisted/requested projection id resolves only through the session's pinned universe reverse mapping; a stale/foreign/removed projection fails closed (`model-unavailable`) and is never silently remapped; a selector that differs from the owning view's persisted model is recorded as a `projection-vs-view-state`/`persisted-view-model-vs-request` conflict and resolved through the universe/policy only |
| AT-211 | Apply the same decision vocabulary to Codex-supported paths without forcing Claude-specific aliases onto Codex (#127) | A Codex profile request produces the same typed `EffectiveModelDecision` (intent/target/compatibility/reasoning/pool) with the same precedence and fail-closed rules; client-specific behavior remains gated by exact compatibility evidence (#124) and no Claude-only alias/config semantics are imposed on Codex |
| AT-212 | Explain the selected model and blocked alternatives/conflicts with safe metadata in `rly doctor`/model query (#127) | `rly route-trace` carries a secret-free `effectiveModelDecision` per trace (selector, intent type, precedence sources, winner, profile/view, compatibility/trust result, selected provider/model/family/tier/projection, reasoning mapping, pool binding, revisions, blocked alternatives, rejection/fallback reasons); `rly doctor` reports the model-control contract (schema version, precedence order, ECR final authority); actionable reasons cover conflicting env/settings, stale projection, unsupported alias, unavailable tier, and incompatible/expired evidence |
| AT-213 | Prove diagnostics never leak prompts, responses, reasoning text, credentials, auth headers, raw account identity, or settings contents (#127) | Decision/describe output passes `assertSecretFree`; unit/lifecycle/privacy suites assert no prompt/response/reasoning-text/credential/auth-header/account-identity/settings-content markers (env/settings ownership is counts + key names only, never values); the privacy scan passes |
| AT-214 | Keep existing model selection/tier/reasoning/projection/account-routing regressions green (#127) | #68 selection, #69 tier resolution, #70 reasoning, #71 subagent, #72 projection/discovery, #124 ECR consumer, and pool/account routing suites stay green after the decision is wired into `resolve-route` |
| AT-215 | Identify EffectiveModelDecision as the final model-control output before account selection in docs (#127) | ARCHITECTURE, protocol-compatibility, project-decisions, SPEC, FRS/SRS, RTM, and this catalogue identify `EffectiveModelDecision` as the FINAL model-control output produced before account selection; FR-031 / SR-F-035 / AT-203–AT-215 added with non-colliding IDs (checked against merged dev max FR-030 / SR-F-034 / AT-202 from #124) |

| AT-216 | launchd/systemd execute one stable RLY-owned bootstrap contract (#94) | The generated LaunchAgent plist / systemd user unit ExecStart references ONLY `<control-plane>/bootstrap/rly-gateway` plus the gateway start contract (`gateway start --config <abs-config>`); it never references `dist/cli/init.js`, a direct `runtime/refs/...` path, or a Node executable path; the bootstrap script is installed at `0755` under a `0700` directory and is self-locating; `rly init` registers against it and `rly update` keeps the definition on it |
| AT-217 | Bootstrap resolves only the committed #92 active deployment and refuses staged/uncommitted candidates (#94) | With `refs/active` pointing at a valid immutable deployment, the bootstrap (script and `resolveActiveDeployment`) resolves it and executes the real `dist/cli/main.js` dispatcher with the gateway start arguments; with only `refs/staged` present, with no refs, with an active ref pointing at a non-immutable target, or with a mismatched deployment metadata identity, the bootstrap REFUSES with an actionable message (script exit 78; TS resolver throws `BootstrapResolutionError`) and never falls back to staged/uncommitted bytes; `RLY_SERVING_ARTIFACT` is exported to the dispatcher |
| AT-218 | Runtime identity distinguishes two artifacts sharing the same semantic version (#94) | `/identity` carries the exact build identity incl. the serving artifact digest; two identities with the same semantic version but different artifact digests (or different buildId/commit when no digest) are distinguishable (`sameSemanticVersionDifferentArtifact` true, `exactIdentityMatch` false); an exact match requires every field equal; an identity with a digest never exact-matches one without |
| AT-219 | `/identity`, updater probation, diagnostics, and release-candidate metadata compare the same exact build/artifact identity fields (#94) | The runtime's `/identity.build`, the CLI's `rly --version`, doctor/status `update.buildIdentity`/`bootstrap` output, the candidate manifest (`rly.json`), and deployment metadata all use the same versioned fields (semantic version, commit revision, build ID, release channel, control/data protocol versions, state schema version, artifact digest); update probation requires the serving artifact digest to equal the candidate's immutable deployment identity whenever the serving runtime identifies its artifact |
| AT-220 | Existing attestation/foreign-listener checks continue to fail closed on an unexpected process/build (#94) | Reuse of an attested instance requires the serving build identity digest to match the ownership record; a record/process pair that is same-semantic-version-different-artifact (or a stale legacy record without a matching build identity) resolves `attested-incompatible` and is never reused; a foreign/unattested listener still fails closed and is never signaled; the identity challenge/proof contract is unchanged |
| AT-221 | Re-running `rly init` or doctor detects and idempotently repairs a stale service definition (#94) | A missing, stale, or path-drifted launchd/systemd definition is detected (rendered-content comparison) and repaired through the platform adapter — re-init/doctor never duplicates the definition and never changes provider/account configuration; an already-correct definition is a filesystem no-op; `rly status` reports reconciliation state read-only; legacy definitions (dist/cli/init.js, Node binary, direct runtime/refs/... path) migrate to the bootstrap contract |
| AT-222 | Crash/recovery tests cover definition-write/reload/restart boundaries and never target a deleted/staged artifact (#94) | Lifecycle tests cover: definition written but daemon not reloaded, active ref switched but restart not run, restart booting the committed candidate, and rollback restoring the known-good ref; at every boundary the bootstrap resolves ONLY the committed `refs/active` deployment and the definition never encodes a deployment path; a deleted/staged target is refused |
| AT-223 | Legacy service definitions migrate to the bootstrap contract safely on supported macOS/Linux user-service paths (#94) | A legacy plist/unit pointing at `dist/cli/init.js` or a Node binary + direct refs path is detected as stale/migrated and repaired to the bootstrap contract on re-init/doctor without provider reconfiguration; no-root per-user semantics are preserved; unsupported platforms never falsely report registration success |
| AT-224 | Service definitions and identity metadata contain no secrets (#94) | The bootstrap script, rendered plist/unit, deployment metadata, `/identity.build`, `rly --version`, and doctor/status bootstrap/build-identity output contain no credentials, auth headers, tokens, account identity, prompts, responses, or reasoning text; privacy scan and `assertSecretFree` pass |
| AT-225 | Platform smoke tests prove service bootstrap → exact identity → controlled restart where available (#94) | The opt-in macOS LaunchAgent / Linux systemd user live smokes (`RLY_LIVE_LAUNCHAGENT_SMOKE=1` / `RLY_LIVE_SYSTEMD_SMOKE=1`) register the stable bootstrap script as the sole executable, reach a running state, receive the gateway start contract, and complete restart/stop/unregister; skipped ≠ pass |
| AT-226 | Architecture/install-update docs, project decisions, RTM, and acceptance tests describe stable bootstrap and exact build attestation (#94) | ARCHITECTURE, project-decisions, SPEC §6.7, FRS/SRS, RTM Phase 94 evidence rows, and this catalogue describe the RLY-owned bootstrap contract, the exact build identity fields and their shared comparison, the extended attestation, and idempotent definition reconciliation; FR-032 / SR-F-036 / AT-216–AT-226 added with non-colliding IDs (checked against merged dev max FR-031 / SR-F-035 / AT-215 from #127) |
| AT-227 | Neither streaming route retains `seen` solely to re-encode all previous events (#120) | `src/routes/anthropic-messages-route.ts` and `src/routes/openai-responses-route.ts` stream through the single-pass pump and per-stream incremental encoders; no full canonical-event history is retained to re-encode prior output (the Anthropic encoder keeps only open-block state; the Responses encoder keeps only the aggregate projection needed for the terminal `response.completed` and continuation persistence); source inspection and instrumented tests prove no `seen -> encode(all) -> slice` path remains |
| AT-228 | Instrumented N-event tests demonstrate linear encoder work within bounded protocol state (#120) | Pushing N text/tool deltas through each incremental encoder performs one push per event (total frames equal the batch encoder's) and `retainedStateSize()` stays bounded by open blocks / aggregate projection regardless of N (proven for N = 1, 10, 1000+); a stream ending with open content fails closed in `finish()` exactly like the batch `final = true` encoder |
| AT-229 | Incremental output remains wire-equivalent for valid text/reasoning/tool fixtures, including #119 fidelity events (#120) | For text, tool, reasoning + signature, reasoning + tool interleave, and redacted-reasoning canonical streams, the incremental encoder path is byte-identical to `encodeAnthropicSse` / `encodeResponsesSse`; `signature_delta` ordering relative to `thinking_delta`/`content_block_stop` is preserved; the route-level streamed body equals the batch encoder output byte-for-byte; #119 golden fixtures stay green |
| AT-230 | Slow consumers trigger backpressure handling without unbounded memory growth or frame corruption (#120) | A real HTTP client reading a multi-megabyte stream slowly (with a deliberate pause longer than the idle window) receives the byte-identical body with no spurious idle timeout — the idle clock is disarmed while suspended on downstream backpressure; the streamed response reports `backpressureCount > 0` and `terminalKind: completed` in its metadata; frames arrive complete, in order, without duplication |
| AT-231 | Client disconnect/cancel aborts upstream work and performs one-time bounded cleanup (#120) | A client destroying the connection mid-stream causes the upstream to observe the merged AbortSignal abort promptly; no error frames or partial frames are emitted after close (frames fully received before the disconnect are complete and valid); the pump, encoder, timers, and abort listeners terminate exactly once |
| AT-232 | Healthy long streams are not terminated by a generic whole-request timeout; setup/idle/progress policies are tested (#120) | A paced stream with continuous progress completes even with a small idle window (progress resets the clock); an upstream that never yields is aborted by the setup timeout with exactly one in-band structured error frame; an upstream that stalls mid-stream is aborted by the idle timeout with exactly one error frame (Anthropic `api_error`, Responses `timeout_error`); no whole-request timer exists |
| AT-233 | Error, timeout, disconnect, and normal completion terminate the stream exactly once (#120) | Unit/pump/lifecycle tests cover normal completion (onComplete + onFinished once, `completed`), encoder error (one error frame, `error`), timeout (one in-band frame, `timeout` + category), client disconnect (no frames, `cancelled`), and teardown (generator `return()` reports `cancelled`, not completed); `finish()` is idempotent, clears timers, and removes the client abort listener exactly once; a clean end overrides a spurious close-after-end cancellation |
| AT-234 | Soak tests cover thousands of synthetic events without superlinear route behavior (#120) | A 3000+ event stream routes to a byte-identical body within a bounded wall-clock window; the instrumented encoder tests prove O(N) work with bounded retained state; no route retains the event history |
| AT-235 | Diagnostics remain secret/user-content free (#120) | Stream metrics contain only `eventCount`, `frameCount`, `backpressureCount`, `durationMs`, `terminalKind`, and `timeoutCategory`; no prompt, response, reasoning text, tool payload, signature, or credential value is recorded; the metrics object's key set is fixed and content-free; privacy/redaction tests pass |
| AT-236 | Protocol/architecture/acceptance docs describe incremental stream lifecycle semantics (#120) | ARCHITECTURE, protocol-compatibility, SPEC, FRS/SRS, RTM Phase 120 evidence rows, and this catalogue describe the incremental encoder API, O(N) route execution with bounded state, backpressure suspension, cancellation propagation, setup/idle/progress timeout policy (no whole-request timer), exactly-once cleanup, and metadata-only diagnostics; FR-033 / SR-F-037 / AT-227–AT-236 added with non-colliding IDs (checked against merged dev max FR-032 / SR-F-036 / AT-226 from #94) |
| AT-237 | Exercise a true native OpenAI Responses upstream rail with item/continuation semantics preserved (#121) | At least one supported provider/access path (OpenRouter direct, exact contract `POST https://openrouter.ai/api/v1/responses`) receives a first-class Responses request/stream; `item.id` identity, `previous_response_id`/response-id continuation, tool/reasoning item ordering, and #119 `encrypted_content` artifacts (requested via `include: ["reasoning.encrypted_content"]`) survive provider invocation onto the client wire; provider-returned artifacts persist into the continuation store and re-attach on the next turn |
| AT-238 | Same-protocol Responses fixtures never route through a lossy Chat-Completions approximation (#121) | A same-protocol Responses request to an adapter that declares the exact Responses endpoint contract calls `POST {endpoint}/responses` (never `/chat/completions`); the conformance corpus proves outbound item shape, item ids on the client wire, and artifact passthrough; a provider WITHOUT the native contract keeps the cross-protocol path and still fails closed on required artifacts (`unsupported-fidelity`, zero upstream calls) |
| AT-239 | Safe provider error status/code/type/retry-after/rate-limit metadata survives to the client/error policy (#121) | `parseProviderError` extracts allowlisted status/code/type/message/param/retry-after/rate-limit fields from a bounded provider body; both routes map them onto protocol-correct client errors (Responses `{type, code, message, param}`; Anthropic via deterministic `translateProviderError`) and surface the `retry-after` header; generic normalization is the fallback, never the only path |
| AT-240 | Error redaction proves credentials, auth headers, secret-bearing bodies, prompts, responses, and reasoning text are excluded (#121) | Secret-bearing provider error bodies fed through `parseProviderError`, route error payloads, `retryTrace`, and `retryReasonOf` never expose the secret values, raw bodies, authorization headers, prompts, or reasoning text; `ProviderErrorInfo`/trace key sets are fixed and allowlisted; privacy scan passes |
| AT-241 | Retry decisions consume an explicit provider commitment state in addition to client-visible output/tool boundaries (#121) | `canRotate` requires `commitment === "not-sent"`; the pool advances commitment on `response-started` (provider-accepted), output deltas (client-output-started), and tool-call events (tool-boundary); a failure after provider acceptance with no output byte still never rotates (new gap vs output-only gating) |
| AT-242 | Network failure with unknown provider outcome defaults to no replay/failover (#121) | A post-send network failure (unknown/ambiguous) is never replayed: `streamPoolRequest` does not rotate and `collectWithSafeRetry` does not retry; only provably-not-sent failures (connection-level, before request write) rotate within budget |
| AT-243 | Failure after tool/client/provider commitment never rotates the account or replays the request (#121) | Chaos tests at the tool boundary, during streaming, and after provider acceptance invoke exactly ONE account and execute the tool side effect exactly once — no duplicate tool execution, no failover after commitment |
| AT-244 | Conformance fixtures cover Anthropic/Responses text, stream, tool, reasoning, continuation, stop, error, and cancellation semantics with WIRE expectations (#121) | The corpus records expected wire frames for the native Responses stream (reasoning + text + tool interleave with item ids and artifacts), the 429 error wire (retry-after + rate-limit + commitment), disconnect ambiguity, continuation outbound body, and Anthropic supported-path stop/error frames |
| AT-245 | Chaos tests cover failures at every commitment stage and prove no duplicate tool side effects (#121) | Failures injected before send (rotates), after send/before response (no replay), after provider acceptance (no replay), during streaming (no replay), and around tool boundaries (no replay, tool executed once); a clean run after a not-sent rotation executes the tool exactly once on the surviving account |
| AT-246 | Long-session soak combines #120 incremental transport with retry/error handling without state leakage or unbounded resource growth (#121) | A 3000+ event native stream routes twice with byte-identical bodies and bounded encoder retained state (independent of event count); encoder instances are isolated; a not-sent retry inside the pool produces a single byte-identical completed response; continuation storage grows by exactly the completed responses |
| AT-247 | Protocol compatibility docs distinguish exact native rails from weaker provider-specific compatibility surfaces (#121) | `docs/protocol-compatibility.md` lists the exact native Responses endpoint contract (OpenRouter `POST /api/v1/responses`) separately from Chat-Completions translation paths; `OpenAI-compatible` is never a sufficient compatibility class — every claimed Responses path identifies the exact endpoint/adapter contract |
| AT-248 | Wave 1 completion evidence is sufficient for Wave 2 claim keying to exact protocol/adapter/access-path contracts (#121) | The corpus, adapter contract tests, and docs record the exact protocol + adapter + access path per scenario (including the #122 claim-key vocabulary), so Wave 2 can key compatibility claims to exact contracts without re-deriving them; FR-034 / SR-F-038 / AT-237–AT-248 added with non-colliding IDs (checked against merged dev max FR-033 / SR-F-037 / AT-236 from #120) |
| AT-249 | A clean machine executes the packaged `rly` runtime without a user Node/npm/pnpm toolchain (#35) | The self-locating `rly` launcher runs the BUNDLED pinned Node (`bin/node`) against the packaged runtime tree; the clean-artifact smoke (`rly --version`) passes from the unpacked artifact with no `node`/`npm`/`pnpm` on PATH and from a working directory other than the artifact; normal execution never depends on the source repo, the pnpm workspace, or the invoking CWD |
| AT-250 | The platform matrix explicitly covers or excludes each supported/experimental target; no silent byte reuse (#35) | `darwin-arm64`, `darwin-x64`, `linux-x64`, and `linux-arm64` each appear in the matrix with an explicit status (`supported` = built AND smoke-tested on a qualified runner; `experimental` = built deterministically, smoke-testing requires an unprovisioned runner) and a reason; every target maps to its OWN Node distribution tarball (distinct dist names) and records its own `targetPlatform`; no target's artifact is served as another target's bytes |
| AT-251 | Package content comes from a positive allowlist and absence tests prove forbidden content is excluded (#35) | Assembly/verification run the positive top-level allowlist (rly, bin/, dist/, node_modules/, package.json, LICENSE, docs/, rly.json, rly-build.json, rly-artifact.json) and fail on ANY unexpected file, forbidden marker (`.git`, `.env`, `.rly`, `.agent-gateway`, `plans/`, `tests/`, `coverage/`, `reports/`, snapshots, sqlite, logs, keys/certs, pnpm metadata), escaping/absolute symlink, or secret content in RLY-owned files; hermetic tests plant forbidden files and prove packaging fails listing them, and prove a clean artifact passes the absence scan |
| AT-252 | The bundled Node/runtime version is pinned and appears in build metadata used by qualification/SBOM (#35) | `scripts/standalone/node-version.json` pins the exact Node version; the artifact records `bundledNodeVersion` + `bundledNodeVersionSource` in `rly-artifact.json` (derived from the ACTUAL bundled binary, verified by executing it); downloaded Node distributions are sha256-verified against nodejs.org SHASUMS256 and the extracted binary's `--version` must equal the pin; `rly-artifact.json` is the SBOM input consumed by #128 qualification |
| AT-253 | Semantic version, source commit, build ID, platform target, and artifact digest are consistent with #94 and not split across package.json/tag/runtime identity (#35) | `rly-build.json`, `rly.json`, and `rly-artifact.json` carry identical semantic version/commit/buildId/channel/protocol/state fields; `dist/rly-build.json` matches the artifact-root copy; the canonical version authority is `RLY_RELEASE_VERSION` → exact git tag → `package.json` (`write-build-identity.mjs` consumes the same input) and a build whose identity version diverges from the resolved release version FAILS; `rly --version` from the artifact reports the same identity |
| AT-254 | Two artifacts with different bytes cannot claim the same exact build/artifact identity (#35) | The artifact digest is content-addressed over the exact tree bytes (sorted relative paths + per-file digests + relative symlink targets, same rules as the #92 immutable store) and is recorded in `rly-artifact.json`; verification recomputes it and fails on mismatch; tests prove two byte-distinct trees (same semantic version) receive different digests and different tarball sha256 while identical trees produce byte-identical tarballs and identical digests |
| AT-255 | Installed runtime/bootstrap paths are deterministic and consumable by #94 without a source checkout or PATH assumptions (#35) | The artifact layout is fixed (`rly`, `bin/node`, `dist/`, `node_modules/`, `package.json`, `LICENSE`, `docs/third-party-notices.md`, `rly.json`, `rly-build.json`, `rly-artifact.json`); the launcher is self-locating; verification asserts the exact top-level set and the required files; the layout is consumable by the #94 initial-deployment materialization and by the #129 installer |
| AT-256 | Clean-artifact smoke executes `rly --version`/identity-oriented command from the unpacked artifact for every supported CI target (#35) | The CI matrix job builds + verifies every target and runs the smoke for every SUPPORTED target (linux-x64 on the repository runner: unpack, `rly --version`, identity fields match the artifact metadata); experimental targets (darwin-arm64/darwin-x64/linux-arm64) are built + verified + absence-checked and their unprovisioned smoke is explicitly `skipped`, never reported as passed |
| AT-257 | npm/Homebrew secondary channels, if introduced, consume the canonical artifact lineage rather than rebuilding different bytes under the same version (#35) | Packaging/release docs and the version-identity contract state that secondary channels must point at/wrap the SAME canonical artifact lineage (tarball + sha256 + digest + identity); the artifact identity is content-addressed so a different byte set under the same semantic version is detectable; docs identify standalone artifacts as PRIMARY and external Node as a development concern only |
| AT-258 | Packaging/release docs and architecture identify standalone artifacts as the primary production distribution (#35) | ARCHITECTURE, SPEC, project-decisions, FRS/SRS, RTM Phase 35 evidence rows, release-governance, README/CONTRIBUTING, and this catalogue identify standalone RLY-owned artifacts as the PRIMARY production distribution (GitHub Releases is the artifact origin; npm/Homebrew are secondary convenience channels; external Node is a development concern only); FR-035 / SR-F-039 / AT-249–AT-258 added with non-colliding IDs (checked against merged dev max FR-034 / SR-F-038 / AT-248 from #121) |
| AT-259 | Every advertised platform artifact has a canonical digest/build identity and a release manifest entry consistent with #94/#35 (#128) | `rly-release.json` (canonical release manifest, `scripts/release/manifest.mjs`) carries one entry per target binding filename/size/sha256/content-addressed digest, bundled runtime version, state/protocol compatibility, and required signatures/attestations; `releaseManifestMatchesIdentity` fails a manifest whose version/commit/buildId/channel/protocol/state diverge from the packaged `rly-build.json`; publish + verify pipeline and unit suite prove the exact-byte binding (FR-036; SR-F-040) |
| AT-260 | SBOM and provenance/attestation are generated from the exact release artifact lineage and reference the exact artifact digest (#128) | `rly-<target>.sbom.json` is generated from the ACTUAL packaged bytes (RLY components, bundled pinned Node, third-party `node_modules` packages; deterministic, SPDX-2.3-style) and `rly-provenance.json` ties every artifact digest to the exact source revision + release workflow/toolchain inputs; `verifySbomArtifactRef`/`verifyProvenanceSubjects` fail on any digest mismatch; SBOM/provenance are sibling release assets, never embedded in the tarball (FR-037; SR-F-041) |
| AT-261 | macOS production artifacts pass the documented signing/notarization/stapling verification gate before stable promotion (#128) | The `platform-signing` qualification gate runs `codesign --verify --deep --strict` + `stapler validate` on a provisioned macOS host with the Apple certificate secrets; a missing/not-run macOS signing gate is recorded `skipped` with the reason and `qualificationBlocksStable` blocks promotion for that target — a missing required platform signature BLOCKS stable (FR-037; SR-F-041) |
| AT-262 | Signed channel metadata maps beta/stable to exact build/artifact identities and cannot be replaced undetectably by changing a GitHub `latest` target (#128) | `rly-channel-<channel>.json` + `.sig` (Ed25519, key fingerprint bound) maps each channel to exact release/build/artifact digests; evaluation refuses rollback (version < highest observed), staleness (> maxAgeDays), and freeze (explicit marker); signature/key-fingerprint verification fails for a different key or tampered payload — the updater never trusts a mutable `latest` redirect alone (FR-036; SR-F-040) |
| AT-263 | Qualification installs and exercises the exact digest that is subsequently published; no rebuild occurs between qualification and publication (#128) | `rly-qualification.json` binds `qualifiedBytes` = filename + tarball sha256 + content-addressed artifact digest; the qualification step runs against the SAME unpacked bytes and tarball later attached to the GitHub Release (single workflow: build → verify → qualify → publish → verify); hermetic tests prove the qualification record references the exact digest (FR-038; SR-F-042) |
| AT-264 | Clean install/init/service/readiness/update-handoff/uninstall qualification exists for each advertised stable target (#128) | The qualification matrix covers clean-install, identity, permissions, platform-signing, runtime-readiness (doctor ok), update-handoff (the #73/#92/#94 candidate contract), init-service-registration, and uninstall (self-containment); host-aware gates record `skipped` with a reason when the runner cannot execute the target — a target without full qualification evidence is NOT stable-qualified (FR-038; SR-F-042) |
| AT-265 | A missing/failed required signing, SBOM/provenance, compatibility, or platform qualification gate prevents stable channel promotion (#128) | `qualificationBlocksStable` returns blocking reasons for every missing/failed/skipped required gate (including platform-signing and SBOM/provenance references); `verify-release.mjs --channel stable` exits non-zero without full qualification; the channel metadata records `not-qualified` for stable with gaps (FR-036/037/038; SR-F-040/041/042) |
| AT-266 | Stable artifact bytes cannot be silently replaced under the same exact build identity without verification failure (#128) | `assertReleaseImmutable` refuses republishing the same release version with different digests; `detectAssetReplacement` + verify recompute actual tarball sha256 against the signed metadata and report byte replacement as an immutability violation (pipeline integration test tampers a tarball and verification fails) (FR-036; SR-F-040) |
| AT-267 | Release-critical third-party actions use reviewed pinned revisions or a documented equivalent supply-chain control (#128) | `actions/checkout`, `pnpm/action-setup`, `actions/setup-node`, `actions/upload-artifact`, and `actions/create-github-app-token` are pinned to reviewed immutable commit SHAs in `standalone-artifacts.yml`/`release-beta.yml`/`release-stable.yml`; `scripts/check-release-supply-chain.mjs` (part of `pnpm test:release`) fails on any unpinned third-party action (FR-038; SR-F-042) |
| AT-268 | Release workflow permissions are minimal and secrets are not exposed in artifacts/logs/metadata (#128) | Release workflows declare least-privilege `permissions:` blocks (`contents: write` only where release attachment requires it, `contents: read` otherwise), never configure npm credentials, and reference the signing private key ONLY as the repository secret `RLY_RELEASE_SIGNING_KEY` via `env:`; the gate scans workflow YAML and the privacy scan proves manifests/SBOMs/provenance/channel metadata/qualification records carry public build metadata only (FR-036/037; SR-F-040/041) |
| AT-269 | Beta/stable gate differences are machine-readable/documented and do not let beta evidence masquerade as stable qualification (#128) | `qualificationStatusForChannel` derives `qualified`/`experimental-gaps`/`not-qualified` per channel; beta permits documented experimental gaps recorded in `rly-qualification.json` + channel metadata, stable requires `qualified` for every advertised target; unit tests prove beta gaps never satisfy stable (FR-036; SR-F-040) |
| AT-270 | Release/supply-chain/installation docs and RTM/AT identify exact-byte qualification as the publication authority (#128) | SPEC §6.14 + the release supply chain acceptance gate, ARCHITECTURE release-supply-chain section, project-decisions, release-governance, FRS/SRS, RTM Phase 128 evidence rows, and this catalogue identify exact-byte qualification as the publication authority (FR-038; SR-F-042) |
| AT-271 | Release supply-chain evidence is hermetic, deterministic, and regression-safe (#128) | `tests/unit/release-supply-chain.test.ts` (29 hermetic tests) covers signing, manifest, SBOM, provenance, channel metadata, qualification, immutability, and the publish+verify pipeline; `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm test:privacy`, `pnpm build`, `git diff --check`, and the release supply-chain gate stay green (FR-036/037/038; SR-F-040/041/042) |

## Phase 129 acceptance scenarios (#129 — verified installer/updater/uninstaller)

| ID | Acceptance scenario | Expected result |
| --- | --- | --- |
| AT-272 | Clean supported machine install from the documented bootstrap | `sh install.sh` (or `rly install --channel <c>`) installs on a clean per-user home WITHOUT a source checkout, npm, pnpm, or a preinstalled Node (bundled pinned Node + self-locating launcher; the shell only needs curl/tar/sha256sum|shasum + an OpenSSL 3 with Ed25519 `pkeyutl -rawin`, auto-detected on macOS via Homebrew/`RLY_OPENSSL_BIN`) and WITHOUT sudo; the stable #94 bootstrap + #35 artifact layout is installed and `rly config` is guided (FR-039; SR-F-043) |
| AT-273 | Installer resolves signed channel metadata and verifies the exact artifact target/digest + authenticity before install | `rly install`/`rly update --channel` resolve `rly-channel-<c>.json` + `.sig` and `rly-release.json` + `.sig` (Ed25519 against the committed release public key), evaluate rollback/staleness/freeze, select the exact platform artifact, and verify sha256/size + digest statement + unpacked-tree digest + identity consistency BEFORE any install/staging mutation (FR-039; SR-F-043) |
| AT-274 | Digest/signature/metadata/platform mismatch fails before installed/active runtime mutation | Tampered tarball, wrong-key signature, missing signature, stale/frozen/rolled-back channel metadata, unknown release identity, wrong platform, and not-qualified-for-stable targets each fail with an actionable secret-free typed error and leave `refs/active` and the resident service untouched (FR-039; SR-F-043) |
| AT-275 | First install registers the stable #94 bootstrap/service path; no service points at `dist/cli/init.js` | The generated launchd/systemd definition ExecStart references ONLY `<control-plane>/bootstrap/rly-gateway` (+ the gateway start contract); `dist/cli/init.js`, direct `runtime/refs/...` paths, and incidental Node paths never appear; the initial committed `active` deployment is materialized from the verified #35 artifact tree (FR-039; SR-F-043; FR-032) |
| AT-276 | Update acquisition produces/hands off a verified immutable candidate without changing the serving active reference or restarting | `rly update --channel <c>` (and `--install-only`) installs the verified candidate into the #92 store updating ONLY `refs/staged` and reports a `VerifiedCandidate`; `refs/active`, the resident service, and active sessions are unchanged (INSTALL != ACTIVATE; Wave 4 activates via a later `rly update`) (FR-039; SR-F-043; FR-023/FR-027) |
| AT-277 | `rly update` reports current exact build, candidate exact build/channel, verification state, and pending/activation status | The acquisition report carries current version/artifact digest, candidate version/channel/artifact digest/qualification, channel metadata verification, and the durable update state (`pending-activation`/`staged`); `rly update` without arguments resumes/reports activation state (FR-039; SR-F-043) |
| AT-278 | Reinstall/repair is idempotent and cannot delete/replace foreign/unowned paths or kill unknown port owners | Re-running `rly install`/`rly init` detects the existing installation, verifies ownership/build identity, and repairs missing/stale bootstrap + service definitions idempotently (a valid committed deployment is never rewritten); a foreign `~/.local/bin/rly` occupant is refused; no foreign listener is ever signaled (FR-039; SR-F-043; FR-032) |
| AT-279 | Uninstall removes RLY-owned service registration and product artifacts while preserving `~/.rly` durable data | `rly uninstall` stops/disables and removes ONLY the RLY-owned launchd/systemd definition, removes `bootstrap/`, `runtime/`, `installer/`, installation + update state, and the RLY-owned launcher symlink; `~/.rly` configuration/accounts/credential store/control-plane DB/overlay/logs remain intact (FR-039; SR-F-043) |
| AT-280 | Explicit purge requires unambiguous destructive intent and never deletes native Claude/Codex configuration | `rly uninstall --purge` without `--yes` refuses; `--purge --yes` destroys the RLY control plane and leaves `~/.claude`/`~/.claude.json`/Codex config byte-identical (FR-039; SR-F-043) |
| AT-281 | Supported platform/arch matrix matches #35/#128; unsupported environments fail explicitly | darwin-arm64/darwin-x64/linux-x64/linux-arm64 resolve and install; unsupported OS/arch/target and unsupported service-manager environments fail with actionable messages, never partial success (FR-039; SR-F-043; FR-035) |
| AT-282 | Install/update/uninstall logs and artifacts contain no credentials/tokens/identity/conversation content | `acquisition-log.json`, `observed-channels.json`, `VerifiedCandidate` output, installer/update CLI output, and the bootstrap installer logs carry channel/version/build/digest/platform/path/status metadata only; `assertSecretFree` and the privacy scan pass (FR-039; SR-F-043; SR-NF-002/009) |
| AT-283 | #128 exact-byte qualification exercises the installer/update/uninstall path on the exact artifact digests | The qualification matrix includes a `verified-install` gate that verifies the EXACT artifact bytes through the installer acquisition machinery (positive verified + tampered-refused before mutation); the real release signatures are verified post-publish by `verify-release.mjs` on the same lineage (FR-039; SR-F-043; FR-038) |
| AT-284 | README/install/update/uninstall/project-decision/RTM/AT documentation matches the implemented lifecycle and the INSTALL != ACTIVATE boundary | README, SPEC §6.15, ARCHITECTURE, project-decisions, FRS/SRS, RTM Phase 129 rows, and this catalogue describe the verified acquisition, bootstrap install, channel policy, uninstall/purge, repair, and the rule that acquisition never activates; FR-039 / SR-F-043 / AT-272–AT-285 added with non-colliding IDs (checked against merged dev max FR-038 / SR-F-042 / AT-271 from #128) |
| AT-285 | Installer/updater/uninstaller evidence is hermetic, deterministic, and regression-safe (#129) | `tests/unit/installer-{signing,acquisition,state}.test.ts`, `tests/unit/cli-{install,uninstall,update-acquisition}.test.ts`, `tests/integration/installer-shell.test.ts`, `tests/privacy/installer.test.ts`, and the `verified-install` qualification gate stay green; `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm test:privacy`, `pnpm build`, `git diff --check`, and the release supply-chain gate stay green (FR-039; SR-F-043) |

## Phase 144 acceptance scenarios (#144 — materializeRuntimeTree symlink preservation)

| ID | Acceptance scenario | Expected result |
| --- | --- | --- |
| AT-286 | Materialize a runtime tree preserving pnpm symlinks so transitive dependencies resolve (#144) | A two-level pnpm virtual-store fixture (top-level link → `.pnpm/<pkg>@<ver>/node_modules/<pkg>` whose sibling links resolve the level-1 and level-2 deps, mirroring fastify→avvio) materialized by `materializeRuntimeTree` keeps every relative in-tree link with its original relative target and the deployed tree RUNS: real Node resolution through the preserved virtual store succeeds; the pre-fix dereferencing behavior fails with `Cannot find module '<dep>'`; absolute/escaping symlinks are refused (self-contained deployments); pnpm metadata, `.bin` shims, and dependency test artifacts are pruned; `computeArtifactId` digests the relative links deterministically (`link:<target>`) and the candidate installer stages the symlinked candidate preserving the layout (FR-040; SR-F-044; FR-032)

## Evidence rules

- Automated evidence records command, tested revision, result, and redacted artifact where needed.
- Live provider evidence is opt-in, date/version/model-specific, and never stores prompt/response or secrets.
- A skipped scenario is not passing evidence. Its owning requirement remains unverified unless the baseline explicitly excludes it.
- Narrative reports cannot substitute for a failing or absent executable gate.
