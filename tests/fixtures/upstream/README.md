# Upstream compatibility fixtures

Synthetic shapes extracted from frozen artifacts. They document planned
behavior only.

Rules:

- No live credentials, tokens, emails, account identifiers, prompts, or responses.
- No machine-local paths or user-owned state.
- Use `acct-fixture-*` and `fixture-*` identifiers.
- Token fields are omitted, never filled with plausible secrets.
